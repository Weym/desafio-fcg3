---
phase: 01-infrastructure-schema
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01-PLAN.md
  - 01-03-PLAN.md
files_modified:
  - backend/src/main.py
autonomous: true
gap_closure: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Running `docker compose up --build -d` brings all four containers to healthy state."
    - "`GET /health` on the FastAPI container returns 200 instead of failing during app import."
    - "The backend can import `get_settings` without breaking `uvicorn src.main:app` startup."
  artifacts:
    - path: "backend/src/main.py"
      provides: "Import-safe FastAPI entrypoint for Docker runtime"
      contains: "from src.infrastructure.config import get_settings"
    - path: "docker-compose.yml"
      provides: "Backend runtime command and healthcheck path used for verification"
      contains: "uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload"
  key_links:
    - from: "backend/src/main.py"
      to: "backend/src/infrastructure/config.py"
      via: "import of get_settings"
      pattern: "from src\\.infrastructure\\.config import get_settings"
    - from: "docker-compose.yml"
      to: "backend/src/main.py"
      via: "uvicorn src.main:app"
      pattern: "uvicorn src\\.main:app"
---

<objective>
Close the Phase 1 verification gap by repairing the backend import path that currently crashes the FastAPI container before startup.

Purpose: Restore the last blocked Phase 1 truth so the four-service stack actually starts cleanly, per the verified gap in `01-VERIFICATION.md`.
Output: A corrected backend entrypoint that boots under Docker and a repeatable compose-based proof that all four services are healthy.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-schema/01-CONTEXT.md
@.planning/phases/01-infrastructure-schema/01-VERIFICATION.md
@.planning/phases/01-infrastructure-schema/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-schema/01-03-SUMMARY.md
@backend/src/main.py
@backend/src/infrastructure/config.py
@docker-compose.yml

<interfaces>
From backend/src/main.py:
```python
app = FastAPI(title="Desafio FCG3 - API", version="0.1.0")

@app.get("/health")
async def health() -> dict[str, str]:
    return {"status": "ok"}
```

From backend/src/infrastructure/config.py:
```python
@lru_cache
def get_settings() -> Settings:
    return Settings()
```
</interfaces>

## Locked decisions to preserve

- **D-05:** keep Docker bind mounts + `uvicorn --reload` untouched.
- **D-08:** keep the FastAPI backend as a minimal Phase 1 health stub; do not expand it into real feature wiring while closing this gap.

## Gap source

- `01-VERIFICATION.md` reports `ModuleNotFoundError: No module named 'infrastructure'` because `backend/src/main.py` imports `infrastructure.config` while the container boots via `uvicorn src.main:app`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair backend config import for Docker package root</name>
  <files>backend/src/main.py</files>
  <read_first>
    - backend/src/main.py
    - backend/src/infrastructure/config.py
    - docker-compose.yml
    - .planning/phases/01-infrastructure-schema/01-VERIFICATION.md
    - .planning/phases/01-infrastructure-schema/01-03-SUMMARY.md
  </read_first>
  <action>
    Update `backend/src/main.py` so the lazy config import uses the same package root as the compose command: replace `from infrastructure.config import get_settings  # noqa: F401` with `from src.infrastructure.config import get_settings  # noqa: F401`.

    Keep the file intentionally minimal per D-08: preserve the existing `FastAPI(title="Desafio FCG3 - API", version="0.1.0")` declaration and the current `/health` handler returning `{"status": "ok"}`. Do not instantiate `get_settings()`, do not add route imports, and do not change `docker-compose.yml`; the goal is to fix the import path mismatch only, not broaden Phase 1 scope.
  </action>
  <acceptance_criteria>
    - `backend/src/main.py` contains `from src.infrastructure.config import get_settings`.
    - `backend/src/main.py` still contains `@app.get("/health")`.
    - `docker compose run --rm fastapi-app python -c "from src.main import app; print(app.url_path_for('health'))"` prints `/health`.
    - `docker compose up --build -d` followed by `docker compose ps` shows `fcg3-api` with `healthy` status.
    - `curl -fsS http://localhost:8000/health` returns `{"status":"ok"}` or equivalent JSON with HTTP 200.
  </acceptance_criteria>
  <verify>
    <automated>docker compose build fastapi-app &amp;&amp; docker compose run --rm fastapi-app python -c "from src.main import app; print(app.url_path_for('health'))" &amp;&amp; docker compose up --build -d &amp;&amp; docker compose ps &amp;&amp; curl -fsS http://localhost:8000/health</automated>
  </verify>
  <done>
    The backend container no longer crashes on import, compose reports all four services healthy, and the FastAPI `/health` endpoint responds successfully from localhost.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker runtime → FastAPI import chain | Container startup loads application code and environment-driven config. |
| Localhost health probe → API process | Healthcheck reaches a public HTTP endpoint on the running backend container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | D | `backend/src/main.py` startup path | mitigate | Keep the entrypoint import-safe by importing `get_settings` from `src.infrastructure.config` without calling it, so missing package resolution cannot crash the container before `/health` is served. |
| T-01-05-02 | T | `backend/src/main.py` health stub | accept | `/health` remains a non-sensitive stub in Phase 1 with no secrets or mutable actions; exposure is limited to liveness reporting already required by compose healthchecks. |
</threat_model>

<verification>
Run the task-level automated command exactly as written. Confirm the backend import succeeds inside the container, `docker compose ps` reports `fcg3-api` healthy, and `curl http://localhost:8000/health` succeeds.
</verification>

<success_criteria>
- The verification gap in `01-VERIFICATION.md` is fully addressed with no remaining backend import failure.
- `INFRA-01` is re-proven by a successful compose startup and healthy backend healthcheck.
- No other Phase 1 behavior changes beyond the import-path repair.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-schema/01-05-SUMMARY.md`
</output>
