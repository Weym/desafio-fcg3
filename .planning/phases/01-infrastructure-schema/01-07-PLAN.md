---
phase: 01-infrastructure-schema
plan: 07
type: execute
wave: 3
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
  - 01-04-PLAN.md
files_modified:
  - docker-compose.yml
  - backend/docker/postgres/reconcile_dev_credentials.sh
  - backend/src/infrastructure/config.py
  - backend/src/infrastructure/database.py
  - backend/alembic/env.py
  - backend/tests/phase_01/conftest.py
  - backend/tests/phase_01/test_phase_01_schema_seed.py
autonomous: true
gap_closure: true
requirements:
  - INFRA-02
  - INFRA-04
user_setup: []
must_haves:
  truths:
    - "Running `docker compose exec -T fastapi-app alembic upgrade head` succeeds against the current Docker PostgreSQL volume."
    - "Running `docker compose exec -T fastapi-app alembic check` reports no pending upgrade operations."
    - "Running `docker compose exec -T fastapi-app python -m scripts.seed` succeeds twice and preserves the expected 40-course, 8-semester fixture snapshot."
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL service wiring that reconciles persisted-volume credentials before app containers depend on health"
      contains: "reconcile_dev_credentials.sh"
    - path: "backend/docker/postgres/reconcile_dev_credentials.sh"
      provides: "Idempotent role/password/database reconciliation for reused Docker volumes"
      contains: "ALTER ROLE"
    - path: "backend/src/infrastructure/database.py"
      provides: "Async runtime DSN builder sourced from env without stale hardcoded credentials"
      contains: "POSTGRES_PASSWORD"
    - path: "backend/alembic/env.py"
      provides: "Sync Alembic DSN builder aligned with runtime credentials"
      contains: "ALEMBIC_DATABASE_URL"
    - path: "backend/tests/phase_01/test_phase_01_schema_seed.py"
      provides: "Container-backed regression coverage for alembic upgrade/check and seed rerun"
      contains: "docker compose exec -T fastapi-app"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/docker/postgres/reconcile_dev_credentials.sh"
      via: "postgres startup/bootstrap command"
      pattern: "reconcile_dev_credentials"
    - from: "backend/src/infrastructure/database.py"
      to: "postgres"
      via: "asyncpg DSN built from POSTGRES_* or DATABASE_URL"
      pattern: "postgresql\\+asyncpg://"
    - from: "backend/alembic/env.py"
      to: "postgres"
      via: "sync DSN derived from the same credential source"
      pattern: "postgresql://"
    - from: "backend/tests/phase_01/test_phase_01_schema_seed.py"
      to: "fastapi-app"
      via: "docker compose exec -T fastapi-app alembic/seed commands"
      pattern: "docker compose exec -T fastapi-app"
---

<objective>
Close the Phase 1 verification gaps by restoring working FastAPI/Alembic authentication against the live Docker PostgreSQL volume and re-proving the destructive seed path.

Purpose: Eliminate the current runtime credential drift so Phase 1 once again satisfies INFRA-02 and INFRA-04 from the same `fastapi-app` container path promised in the roadmap.
Output: Credential-reconciliation runtime wiring plus regression tests that prove `alembic upgrade head`, `alembic check`, and `python -m scripts.seed` all succeed from `fastapi-app`.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infrastructure-schema/01-CONTEXT.md
@.planning/phases/01-infrastructure-schema/01-VERIFICATION.md
@.planning/phases/01-infrastructure-schema/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-schema/01-04-SUMMARY.md
@docker-compose.yml
@.env.example
@backend/src/infrastructure/config.py
@backend/src/infrastructure/database.py
@backend/alembic/env.py
@backend/tests/phase_01/conftest.py
@backend/tests/phase_01/test_phase_01_schema_seed.py

## Locked decisions to preserve

- **D-04:** keep the seed destructive and repeatable; do not replace reseeding with a non-destructive shortcut.
- **D-05:** keep the local workflow centered on Docker Compose and in-container execution.
- **D-09:** keep pgvector as migration `#001`; this gap closure repairs connectivity, not migration ordering.
- **D-10 / D-11:** preserve ORM + Alembic as the schema source of truth; do not bypass Alembic with ad-hoc SQL-only schema creation.

## Gap source

- `01-VERIFICATION.md` reports both failed truths are caused by password-authentication drift for user `fcg3` from inside `fastapi-app`.
- `01-04-SUMMARY.md` already documented that persisted Docker volumes can drift from the current `.env` credentials.
- The fix must restore the exact promised commands instead of replacing them with manual one-off recovery steps.

<interfaces>
From `backend/src/infrastructure/config.py`:

```python
class Settings(BaseSettings):
    database_url: PostgresDsn
    alembic_database_url: PostgresDsn | None
    database_url_ai: PostgresDsn | None
    database_url_mcp: PostgresDsn | None

@lru_cache
def get_settings() -> Settings: ...
```

From `backend/src/infrastructure/database.py`:

```python
def get_database_url() -> str: ...
DATABASE_URL = get_database_url()
engine = create_async_engine(DATABASE_URL, echo=False)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

From `backend/alembic/env.py`:

```python
def get_sync_database_url() -> str: ...
config.set_main_option("sqlalchemy.url", get_sync_database_url())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile persisted-volume PostgreSQL credentials with the Compose runtime</name>
  <files>docker-compose.yml, backend/docker/postgres/reconcile_dev_credentials.sh, backend/src/infrastructure/config.py, backend/src/infrastructure/database.py, backend/alembic/env.py</files>
  <read_first>docker-compose.yml, .env.example, backend/src/infrastructure/config.py, backend/src/infrastructure/database.py, backend/alembic/env.py, .planning/phases/01-infrastructure-schema/01-VERIFICATION.md, .planning/phases/01-infrastructure-schema/01-04-SUMMARY.md</read_first>
  <action>Add a PostgreSQL bootstrap/reconciliation script at `backend/docker/postgres/reconcile_dev_credentials.sh` that runs inside the `postgres` container on every start, uses local `psql` access as the container superuser, and makes the configured `${POSTGRES_USER}` / `${POSTGRES_DB}` usable even when the Docker volume was initialized with older credentials: create the role if missing, `ALTER ROLE` to the current `${POSTGRES_PASSWORD}`, create the database if missing, and ensure ownership/privileges point at `${POSTGRES_USER}`. Wire that script through `docker-compose.yml` so the postgres service executes it during startup and do not mark postgres healthy until authenticated access with `${POSTGRES_USER}` and `${POSTGRES_DB}` works over the same network path app containers use.

Replace stale hardcoded fallback DSNs in `backend/src/infrastructure/config.py`, `backend/src/infrastructure/database.py`, and `backend/alembic/env.py`. Keep `DATABASE_URL` and `ALEMBIC_DATABASE_URL` as explicit overrides, but when they are absent derive the exact URLs from `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `POSTGRES_HOST`, and `POSTGRES_PORT` with defaults that match `.env.example` and Docker Compose. Remove the old `changeme` / `localhost` fallbacks that silently diverge from the active stack. Preserve the runtime split required by the project constraints: asyncpg for FastAPI runtime and plain `postgresql://` for Alembic.</action>
  <acceptance_criteria>
    - `backend/docker/postgres/reconcile_dev_credentials.sh` exists and contains SQL that creates/alters the configured role and database.
    - `docker-compose.yml` references `reconcile_dev_credentials.sh` from the postgres service startup path.
    - `backend/src/infrastructure/database.py` no longer contains `changeme` and builds an asyncpg URL from env-backed values.
    - `backend/alembic/env.py` no longer contains the stale `localhost:5432/fcg3` fallback and derives the sync DSN from the same credential source as runtime.
    - `docker compose exec -T fastapi-app alembic upgrade head` exits 0.
    - `docker compose exec -T fastapi-app alembic check` exits 0 and prints `No new upgrade operations detected.`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose up -d postgres fastapi-app &amp;&amp; docker compose exec -T fastapi-app alembic upgrade head &amp;&amp; docker compose exec -T fastapi-app alembic check</automated>
  </verify>
  <done>FastAPI runtime and Alembic can authenticate from `fastapi-app` against the current persisted PostgreSQL volume without manual volume deletion or ad-hoc operator recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the fix with container-backed Phase 1 schema/seed regression coverage</name>
  <files>backend/tests/phase_01/conftest.py, backend/tests/phase_01/test_phase_01_schema_seed.py</files>
  <read_first>backend/tests/phase_01/conftest.py, backend/tests/phase_01/test_phase_01_schema_seed.py, docker-compose.yml, backend/src/infrastructure/database.py, backend/alembic/env.py, backend/scripts/seed.py, .planning/phases/01-infrastructure-schema/01-VERIFICATION.md</read_first>
  <action>Update the Phase 1 test helpers so they stop hardcoding `postgresql+asyncpg://fcg3:changeme@localhost:5432/fcg3`. Build test DSNs from `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, and a host default that matches the command being exercised. Add helper functions that run `docker compose exec -T fastapi-app alembic upgrade head`, `docker compose exec -T fastapi-app alembic check`, and `docker compose exec -T fastapi-app python -m scripts.seed` directly.

Then rewrite `backend/tests/phase_01/test_phase_01_schema_seed.py` so the Phase 1 regression only passes when the live container-backed commands succeed and the expected seed snapshot remains intact after a second reseed: 21 application tables, Alembic head `006a`, 40 courses, 8 semesters, 5 students, 2 staff, 1 inactive student, 3 failed grades, 4 in-progress grades, 1 resource, 5 scheduling slots, 1 active enrollment period, and prerequisite `SCC0102` for `SCC0201`.</action>
  <acceptance_criteria>
    - `backend/tests/phase_01/conftest.py` contains helper(s) that execute `docker compose exec -T fastapi-app` commands instead of relying only on host-side `python -m alembic` / `python -m scripts.seed`.
    - `backend/tests/phase_01/test_phase_01_schema_seed.py` asserts success for `alembic upgrade head`, `alembic check`, and `python -m scripts.seed` from `fastapi-app`.
    - The test file still asserts `21` application tables and Alembic head `006a`.
    - `python -m pytest backend/tests/phase_01/test_phase_01_schema_seed.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest backend/tests/phase_01/test_phase_01_schema_seed.py -x</automated>
  </verify>
  <done>The authentication regression is covered by an executable test path, and future Phase 1 verification will fail immediately if Alembic or reseeding stops working from the Dockerized backend runtime again.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `fastapi-app` / Alembic → PostgreSQL | Unauthenticated network clients must become authenticated with the exact configured role/password before schema or seed operations run. |
| Docker Compose env → PostgreSQL bootstrap script | Environment-provided credentials are promoted into live database role state during startup. |
| Regression tests → live Docker stack | Verification commands exercise the real container path that previously failed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | T | `docker-compose.yml` + postgres credential bootstrap | mitigate | Reconcile `${POSTGRES_USER}` / `${POSTGRES_PASSWORD}` / `${POSTGRES_DB}` on every start so persisted-volume drift cannot silently leave app credentials stale. |
| T-01-07-02 | D | `backend/src/infrastructure/database.py`, `backend/alembic/env.py` | mitigate | Remove stale fallback DSNs and derive runtime + Alembic URLs from one credential source so auth failures do not block migrations or seeding. |
| T-01-07-03 | R | `backend/tests/phase_01/test_phase_01_schema_seed.py` | mitigate | Require container-backed `alembic upgrade head`, `alembic check`, and reseed commands in automated regression coverage so future breakage is attributable and reproducible. |
| T-01-07-04 | I | startup/verification logs | accept | This gap closure must not print secrets into committed files or plan text; residual risk is limited to local operator logs during Compose runs. |

</threat_model>

<verification>
Run `docker compose up -d postgres fastapi-app`, then prove the full recovery path with `docker compose exec -T fastapi-app alembic upgrade head`, `docker compose exec -T fastapi-app alembic check`, and `docker compose exec -T fastapi-app python -m scripts.seed`. Finish with `python -m pytest backend/tests/phase_01/test_phase_01_schema_seed.py -x`.
</verification>

<success_criteria>
- `INFRA-02` is restored from the live Docker backend runtime, not only from direct `psql` inspection.
- `INFRA-04` is restored from the live Docker backend runtime, with the destructive seed still rerunnable per D-04.
- The credential-drift failure mode recorded in `01-VERIFICATION.md` is covered by an automated regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-schema/01-07-SUMMARY.md`
</output>
