---
phase: 01-infrastructure-schema
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-infrastructure-schema/01-UAT.md
  - .planning/phases/01-infrastructure-schema/01-VALIDATION.md
  - README.md
autonomous: true
gap_closure: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Cold-start verification is not failed from immediate `docker compose up --build -d` output alone."
    - "Phase 1 cold-start acceptance requires a post-start `docker compose ps` health re-check."
    - "Phase 1 cold-start acceptance requires a successful backend `GET /health` request before the stack is judged healthy."
  artifacts:
    - path: ".planning/phases/01-infrastructure-schema/01-UAT.md"
      provides: "Corrected Cold Start Smoke Test procedure and acceptance evidence"
      contains: "docker compose ps"
    - path: ".planning/phases/01-infrastructure-schema/01-VALIDATION.md"
      provides: "Phase 1 verification contract that requires post-start health checks"
      contains: "docker compose ps"
    - path: "README.md"
      provides: "Bootstrap instructions that explain detached compose output is intermediate state"
      contains: "curl http://localhost:8000/health"
  key_links:
    - from: ".planning/phases/01-infrastructure-schema/01-UAT.md"
      to: ".planning/phases/01-infrastructure-schema/01-VALIDATION.md"
      via: "matching cold-start verification steps"
      pattern: "docker compose ps"
    - from: "README.md"
      to: ".planning/phases/01-infrastructure-schema/01-UAT.md"
      via: "shared post-start health-check commands"
      pattern: "curl http://localhost:8000/health"
---

<objective>
Close the diagnosed Phase 1 UAT documentation gap for Cold Start Smoke Test by making the verification sequence explicit in the phase artifacts.

Purpose: Prevent false failures caused by judging detached compose startup output before healthchecks finish, while keeping scope limited to the Test 1 procedure gap already diagnosed in `01-UAT.md`.
Output: Updated UAT/validation/bootstrap docs that require `docker compose ps` and backend `/health` checks before a cold-start failure is declared.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-schema/01-CONTEXT.md
@.planning/phases/01-infrastructure-schema/01-UAT.md
@.planning/phases/01-infrastructure-schema/01-VALIDATION.md
@.planning/phases/01-infrastructure-schema/01-05-SUMMARY.md
@.planning/debug/cold-start-smoke-test-uat-gap.md
@README.md

## Locked decisions to preserve

- **D-05:** keep the documented local workflow centered on Docker Compose startup plus post-start verification; do not replace it with a different bootstrap flow.
- **D-08:** keep the AI and MCP services framed as Phase 1 healthcheck stubs; this gap closure updates verification wording only and must not expand service implementation scope.

## Gap source

- `01-UAT.md` Test 1 was marked failed from incomplete evidence.
- Root cause already diagnosed: immediate detached compose output is an intermediate startup snapshot, not a final health verdict.
- `README.md` already lists the needed checks; this plan makes the cold-start acceptance contract explicit where the failure was recorded and where future verification will look first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 1 UAT cold-start acceptance around post-start health evidence</name>
  <files>.planning/phases/01-infrastructure-schema/01-UAT.md, .planning/phases/01-infrastructure-schema/01-VALIDATION.md</files>
  <action>
    Update Cold Start Smoke Test language in `01-UAT.md` so Test 1 explicitly requires this sequence before any failure is recorded: start detached with `docker compose up --build -d`, re-check service status with `docker compose ps`, then call `curl http://localhost:8000/health` and judge the test only from those post-start results.

    In the same pass, update `01-VALIDATION.md` so the Phase 1 validation contract mirrors that requirement: detached compose output alone is not sufficient evidence, a post-start health re-check is mandatory, and backend `/health` is required proof for the stack smoke path. Keep this tightly scoped to the diagnosed Test 1 gap; do not reopen unrelated review findings, seed safety work, or schema/runtime behavior.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; uat=Path('.planning/phases/01-infrastructure-schema/01-UAT.md').read_text(encoding='utf-8'); val=Path('.planning/phases/01-infrastructure-schema/01-VALIDATION.md').read_text(encoding='utf-8'); assert 'docker compose ps' in uat and 'curl http://localhost:8000/health' in uat and 'intermediate startup state' in uat; assert 'docker compose ps' in val and 'curl http://localhost:8000/health' in val"</automated>
  </verify>
  <done>
    Phase 1 UAT and validation artifacts now require post-start health evidence before the cold-start test can fail, and the diagnosed false-negative path from Test 1 is no longer permitted by the written procedure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align bootstrap README wording with the cold-start verification contract</name>
  <files>README.md</files>
  <action>
    Refine the README startup section so it explicitly states that `docker compose up --build -d` ends before app-service healthchecks finish, and that final startup status must be judged only after `docker compose ps` plus the backend `/health` check. Keep the existing commands and scope intact per D-05; this is a clarification pass, not a bootstrap rewrite.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('README.md').read_text(encoding='utf-8'); assert 'docker compose up --build -d' in text; assert 'docker compose ps' in text; assert 'curl http://localhost:8000/health' in text; assert 'intermediate' in text.lower() or 'healthcheck' in text.lower()"</automated>
  </verify>
  <done>
    README cold-start guidance matches the UAT/validation contract and warns future testers not to treat immediate detached compose output as final failure evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tester → Phase 1 UAT contract | A human verifier translates written cold-start instructions into pass/fail evidence. |
| README bootstrap docs → local verification run | Local startup guidance informs what evidence is collected before judging stack health. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | R | `01-UAT.md` Cold Start Smoke Test | mitigate | Require explicit `docker compose ps` and backend `/health` evidence so a failure verdict is backed by reproducible post-start observations instead of incomplete detached-start output. |
| T-01-06-02 | T | `README.md` bootstrap procedure | mitigate | Clarify that detached compose output is intermediate startup state, preventing accidental procedural drift that would misrepresent stack health. |
| T-01-06-03 | D | Phase 1 verification workflow | accept | This gap is documentation-only and does not expose secrets or change runtime behavior; residual risk is limited to future false-negative verification if docs drift again. |
</threat_model>

<verification>
Run both task-level automated checks. Confirm the phase artifacts consistently require `docker compose ps` and backend `curl http://localhost:8000/health` before Test 1 can be marked failed.
</verification>

<success_criteria>
- The diagnosed UAT gap for Test 1 is addressed without changing Phase 1 implementation code.
- `01-UAT.md`, `01-VALIDATION.md`, and `README.md` all describe the same cold-start acceptance sequence.
- Future cold-start verification cannot declare failure from detached compose startup output alone.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-schema/01-06-SUMMARY.md`
</output>
