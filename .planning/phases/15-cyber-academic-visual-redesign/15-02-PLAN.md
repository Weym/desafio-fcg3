---
phase: 15-cyber-academic-visual-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/auth/screens/login_screen.dart
  - mobile/lib/features/splash/screens/splash_screen.dart
  - mobile/lib/features/client/screens/client_shell.dart
  - mobile/lib/features/staff/screens/staff_shell.dart
  - mobile/lib/features/client/screens/client_home_screen.dart
  - mobile/lib/main.dart
  - mobile/lib/shared/widgets/app_bar_actions.dart
autonomous: true
requirements:
  - UI-NFR-02
  - UI-NFR-04

must_haves:
  truths:
    - "No references to 'plusJakartaSans' or 'GoogleFonts.inter' remain in screen files"
    - "App title remains 'Alpha Connect' — no rename"
    - "Navigation bar uses neon glow accent color for selected state"
    - "Login and splash screens use Montserrat for branding text"
    - "Bottom nav glass panel adapts to Cyber-Academic obsidian palette"
  artifacts:
    - path: "mobile/lib/features/auth/screens/login_screen.dart"
      provides: "Login screen with Montserrat branding"
      min_lines: 200
    - path: "mobile/lib/features/splash/screens/splash_screen.dart"
      provides: "Splash screen with Montserrat branding"
      min_lines: 30
    - path: "mobile/lib/features/client/screens/client_shell.dart"
      provides: "Client navigation shell with Cyber-Academic glassmorphism nav"
      min_lines: 200
  key_links:
    - from: "mobile/lib/core/theme/app_colors.dart"
      to: "mobile/lib/features/client/screens/client_shell.dart"
      via: "AppColors.neonTeal for nav glow"
      pattern: "neonTeal"
---

<objective>
Update screen-level files to replace old Alpha Connect branding and font references with Cyber-Academic equivalents.

Purpose: Ensure all screen-level code is consistent with the new design system — no stale Alpha Connect references, all hardcoded font calls use Montserrat, navigation bar uses new color accents.
Output: All screens use the new design system without any references to old branding or fonts.
</objective>

<execution_context>
@./desafio-fcg3/.opencode/get-shit-done/workflows/execute-plan.md
@./desafio-fcg3/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@mobile/lib/features/auth/screens/login_screen.dart
@mobile/lib/features/splash/screens/splash_screen.dart
@mobile/lib/features/client/screens/client_shell.dart
@mobile/lib/features/staff/screens/staff_shell.dart
@mobile/lib/features/client/screens/client_home_screen.dart
@mobile/lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace font references and branding in login/splash/main</name>
  <files>mobile/lib/features/auth/screens/login_screen.dart, mobile/lib/features/splash/screens/splash_screen.dart, mobile/lib/main.dart, mobile/lib/features/client/screens/client_home_screen.dart</files>
  <read_first>
    - mobile/lib/features/auth/screens/login_screen.dart
    - mobile/lib/features/splash/screens/splash_screen.dart
    - mobile/lib/main.dart
    - mobile/lib/features/client/screens/client_home_screen.dart
  </read_first>
  <action>
**1. `mobile/lib/features/auth/screens/login_screen.dart`:**
- Find all `GoogleFonts.plusJakartaSans(...)` calls (lines 273, 411) and replace with `GoogleFonts.montserrat(...)` keeping the same parameters (fontSize, fontWeight, color, etc.).

**2. `mobile/lib/features/splash/screens/splash_screen.dart`:**
- Find `GoogleFonts.plusJakartaSans(...)` call (line 48) and replace with `GoogleFonts.montserrat(...)` keeping same parameters.

**3. `mobile/lib/main.dart`:**
- Keep `title: 'Alpha Connect'` unchanged. Do NOT rename the app.

**4. `mobile/lib/features/client/screens/client_home_screen.dart`:**
- Keep `title: const Text('Alpha Connect')` unchanged. Do NOT rename.
  </action>
  <acceptance_criteria>
    - mobile/lib/features/auth/screens/login_screen.dart does NOT contain `plusJakartaSans`
    - mobile/lib/features/auth/screens/login_screen.dart contains `GoogleFonts.montserrat`
    - mobile/lib/features/splash/screens/splash_screen.dart does NOT contain `plusJakartaSans`
    - mobile/lib/features/splash/screens/splash_screen.dart contains `GoogleFonts.montserrat`
    - mobile/lib/main.dart contains `title: 'Alpha Connect'` (unchanged)
    - mobile/lib/features/client/screens/client_home_screen.dart contains `Text('Alpha Connect')` (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && ! grep -rq "plusJakartaSans" lib/features/ && grep -q "montserrat" lib/features/auth/screens/login_screen.dart && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All screen-level files use Montserrat instead of Plus Jakarta Sans. App title "Alpha Connect" preserved throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation shells with Cyber-Academic neon glow accent</name>
  <files>mobile/lib/features/client/screens/client_shell.dart, mobile/lib/features/staff/screens/staff_shell.dart</files>
  <read_first>
    - mobile/lib/features/client/screens/client_shell.dart
    - mobile/lib/features/staff/screens/staff_shell.dart
    - mobile/lib/core/theme/app_colors.dart
  </read_first>
  <action>
**1. `mobile/lib/features/client/screens/client_shell.dart`:**

Import AppColors at top: `import '../../../core/theme/app_colors.dart';`

In `_GlassBottomNav.build()`:
- Update the blur sigma from `sigmaX: 16, sigmaY: 16` → `sigmaX: 20, sigmaY: 20` (per Cyber-Academic 20px blur spec).
- Update container fill color: In dark mode, change `colors.surfaceContainerLowest.withValues(alpha: 0.7)` → `AppColors.darkSurface.withValues(alpha: 0.85)` (obsidian base for nav). In light mode, change `Colors.white.withValues(alpha: 0.7)` → `Colors.white.withValues(alpha: 0.9)`.
- Update border top color: Dark mode → `AppColors.neonTeal.withValues(alpha: 0.15)` (subtle neon glow line at top). Light mode → `colors.outline.withValues(alpha: 0.1)`.
- Update selected item indicator: change the container decoration color when `isSelected` from `colors.primary` to a neon glow style: `AppColors.neonTeal.withValues(alpha: 0.2)` (transparent teal pill for selected) instead of opaque primary. Change icon color for selected from `colors.onPrimary` to `AppColors.neonTeal`. Change label color for selected from `colors.onPrimary` to `AppColors.neonTeal`.
- Add a subtle `BoxShadow` to the selected item container: `BoxShadow(color: AppColors.neonTeal.withValues(alpha: 0.3), blurRadius: 12, spreadRadius: 0)`.
- Update the `boxShadow` on the main container: in dark mode use `AppColors.neonTeal.withValues(alpha: 0.08)` for a subtle neon under-glow.

**2. `mobile/lib/features/staff/screens/staff_shell.dart`:**
- Read the file first to understand its structure.
- Apply the same pattern changes as client_shell if it has a glass bottom nav.
- If staff_shell uses the same _GlassBottomNav pattern, apply identical color/blur changes.
- If staff_shell uses a different navigation pattern, ensure it at least imports and uses the Cyber-Academic accent colors for selected state indicators.
  </action>
  <acceptance_criteria>
    - mobile/lib/features/client/screens/client_shell.dart contains `sigmaX: 20`
    - mobile/lib/features/client/screens/client_shell.dart contains `neonTeal`
    - mobile/lib/features/client/screens/client_shell.dart contains `import` of `app_colors.dart`
    - mobile/lib/features/client/screens/client_shell.dart contains `blurRadius: 12` (neon glow on selected item)
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && grep -q "sigmaX: 20" lib/features/client/screens/client_shell.dart && grep -q "neonTeal" lib/features/client/screens/client_shell.dart && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both client and staff navigation shells use Cyber-Academic styling — 20px blur, obsidian nav background, neon teal glow accent on selected items.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme → Widget | Design tokens flow from theme to widget rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | D (Denial of Service) | GoogleFonts.montserrat in screen files | accept | Same risk as Plan 01 — GoogleFonts caches locally. Cosmetic-only failure mode. |

</threat_model>

<verification>
- `flutter analyze` shows no errors in modified screen files
- Grep confirms no `plusJakartaSans` in `lib/features/` directory
- Grep confirms no `Alpha Connect` string in `lib/` directory
- Navigation shells use neonTeal for selected accent
</verification>

<success_criteria>
- Zero references to old font (plusJakartaSans) in screen files
- Zero references to "Alpha Connect" branding in code
- Navigation bars use Cyber-Academic neon teal accent
- All files compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-cyber-academic-visual-redesign/15-02-SUMMARY.md`
</output>
