---
phase: 15-cyber-academic-visual-redesign
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - mobile/test/widgets_test.dart
autonomous: true
requirements:
  - UI-NFR-02
  - UI-NFR-04

must_haves:
  truths:
    - "flutter analyze passes with no errors on all modified files"
    - "All existing tests pass (theme_test, widgets_test, auth_tokens_test, auth_login_flow_test)"
    - "Widget tests pass with the new GlassCard elevation parameter"
    - "No compilation errors exist in the mobile project"
  artifacts:
    - path: "mobile/test/widgets_test.dart"
      provides: "Updated widget tests for new GlassCard elevation and PillButton ghost"
      min_lines: 150
  key_links:
    - from: "mobile/test/widgets_test.dart"
      to: "mobile/lib/shared/widgets/glass_card.dart"
      via: "imports GlassCard and tests elevation parameter"
      pattern: "elevation"
---

<objective>
Update widget tests to cover new GlassCard elevation parameter and verify the full test suite passes with the Cyber-Academic design system applied.

Purpose: Ensure the design system changes don't break any existing functionality and new parameters are covered by tests.
Output: Updated widget tests passing, full `flutter test` green, `flutter analyze` clean.
</objective>

<execution_context>
@./desafio-fcg3/.opencode/get-shit-done/workflows/execute-plan.md
@./desafio-fcg3/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@mobile/test/widgets_test.dart
@mobile/test/theme_test.dart
@mobile/lib/shared/widgets/glass_card.dart
@mobile/lib/shared/widgets/pill_button.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update widget tests for new GlassCard elevation and verify full suite</name>
  <files>mobile/test/widgets_test.dart</files>
  <read_first>
    - mobile/test/widgets_test.dart
    - mobile/lib/shared/widgets/glass_card.dart
    - mobile/lib/shared/widgets/pill_button.dart
  </read_first>
  <action>
**1. Update `mobile/test/widgets_test.dart`** — add tests for the new `elevation` parameter:

Add test cases in the `GlassCard` group:

```dart
testWidgets('accepts elevation parameter without error', (tester) async {
  await tester.pumpWidget(
    MaterialApp(
      home: Scaffold(
        body: GlassCard(
          elevation: 2,
          child: const Text('Elevated'),
        ),
      ),
    ),
  );

  expect(find.text('Elevated'), findsOneWidget);
});

testWidgets('accepts glowColor parameter without error', (tester) async {
  await tester.pumpWidget(
    MaterialApp(
      home: Scaffold(
        body: GlassCard(
          glowColor: Colors.purple,
          child: const Text('Custom glow'),
        ),
      ),
    ),
  );

  expect(find.text('Custom glow'), findsOneWidget);
});
```

Add a test for PillButton ghost variant verifying it renders with transparent background:

```dart
testWidgets('ghost variant renders with transparent background', (tester) async {
  await tester.pumpWidget(
    MaterialApp(
      home: Scaffold(
        body: PillButton(
          label: 'Ghost',
          variant: PillButtonVariant.ghost,
          onPressed: () {},
        ),
      ),
    ),
  );

  expect(find.text('Ghost'), findsOneWidget);
  // Button should render without error in ghost mode
  expect(find.byType(ElevatedButton), findsOneWidget);
});
```

**2. Run `flutter analyze`** to check for any lint/type errors across all modified files.

**3. Run `flutter test`** to verify the full test suite passes:
- `test/theme_test.dart` (updated in Plan 01)
- `test/widgets_test.dart` (updated here)
- `test/auth_tokens_test.dart` (unchanged — should pass)
- `test/auth_login_flow_test.dart` (unchanged — should pass)

**4. If any tests fail:** Fix the failing tests by correcting the implementation or test expectations. Common issues:
- GlassCard constructor change may need default value for `elevation` (ensure it defaults to `1`)
- Import statements may need updating if file paths changed
- GoogleFonts may trigger font loading warnings in tests — ensure `TestWidgetsFlutterBinding.ensureInitialized()` is called

**5. Fix any `flutter analyze` warnings** — particularly:
- Unused imports from old font references
- Type mismatches from new color values
- Missing const annotations
  </action>
  <acceptance_criteria>
    - mobile/test/widgets_test.dart contains `elevation: 2`
    - mobile/test/widgets_test.dart contains `glowColor`
    - mobile/test/widgets_test.dart contains `PillButtonVariant.ghost`
    - `flutter test` exits with code 0 (all tests pass)
    - `flutter analyze` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-fatal-infos --no-fatal-warnings 2>&1 | tail -3 && flutter test 2>&1 | tail -5</automated>
  </verify>
  <done>All widget tests pass including new GlassCard elevation and PillButton ghost tests. Full flutter analyze clean. No compilation errors across the project.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Widget | Tests verify widget contracts still hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | I (Information Disclosure) | Test output | accept | Tests don't expose sensitive data. Standard Flutter test runner. |

</threat_model>

<verification>
- `flutter test` passes all test files
- `flutter analyze` reports 0 errors
- All 4 test files execute successfully
</verification>

<success_criteria>
- Full test suite green (all theme, widget, and auth tests pass)
- Flutter analyze clean (no errors)
- New widget tests cover GlassCard elevation and PillButton ghost variant
- No functional regressions from the visual redesign
</success_criteria>

<output>
After completion, create `.planning/phases/15-cyber-academic-visual-redesign/15-03-SUMMARY.md`
</output>
