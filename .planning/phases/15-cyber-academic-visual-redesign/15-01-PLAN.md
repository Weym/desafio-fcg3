---
phase: 15-cyber-academic-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/core/theme/app_colors.dart
  - mobile/lib/core/theme/app_theme.dart
  - mobile/lib/core/theme/app_spacing.dart
  - mobile/lib/shared/widgets/glass_card.dart
  - mobile/lib/shared/widgets/pill_button.dart
  - mobile/pubspec.yaml
  - mobile/test/theme_test.dart
autonomous: true
requirements:
  - UI-NFR-02
  - UI-NFR-04

must_haves:
  truths:
    - "App uses Electric Teal (#00E5FF) as primary color in both light and dark modes"
    - "App uses Cyber Violet (#7209B7) as secondary color"
    - "App uses obsidian (#111317) as dark surface base"
    - "Typography uses Montserrat for headings and JetBrains Mono for technical/code text"
    - "GlassCard uses backdrop blur 20px with 5% white fill and neon outer glow"
    - "Primary buttons display Electric Teal background with black text"
    - "Spacing system uses 8px base unit with named tokens"
  artifacts:
    - path: "mobile/lib/core/theme/app_colors.dart"
      provides: "Cyber-Academic color palette tokens"
      contains: "0xFF00E5FF"
    - path: "mobile/lib/core/theme/app_theme.dart"
      provides: "Theme configuration with Montserrat + JetBrains Mono"
      contains: "montserrat"
    - path: "mobile/lib/shared/widgets/glass_card.dart"
      provides: "Glassmorphism card with neon glow"
      contains: "sigmaX: 20"
    - path: "mobile/lib/shared/widgets/pill_button.dart"
      provides: "Pill-shaped buttons with Cyber-Academic style"
      contains: "onPrimary"
    - path: "mobile/test/theme_test.dart"
      provides: "Updated tests validating new Cyber-Academic tokens"
      contains: "0xFF00E5FF"
  key_links:
    - from: "mobile/lib/core/theme/app_colors.dart"
      to: "mobile/lib/core/theme/app_theme.dart"
      via: "AppColors constants imported by AppTheme"
      pattern: "AppColors\\."
    - from: "mobile/lib/core/theme/app_theme.dart"
      to: "mobile/lib/shared/widgets/glass_card.dart"
      via: "Theme.of(context).colorScheme used for colors"
      pattern: "Theme\\.of\\(context\\)"
---

<objective>
Replace the Alpha Connect color palette, typography, and design tokens with the Cyber-Academic design system.

Purpose: Transform the app's visual identity from corporate blue (Alpha Connect) to a striking cyberpunk-academic aesthetic with neon accents and deep obsidian surfaces.
Output: Updated theme foundation files (colors, theme, spacing), enhanced glass_card with neon glow, updated pill_button with new color mapping, and passing tests.
</objective>

<execution_context>
@./desafio-fcg3/.opencode/get-shit-done/workflows/execute-plan.md
@./desafio-fcg3/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@mobile/lib/core/theme/app_colors.dart
@mobile/lib/core/theme/app_theme.dart
@mobile/lib/core/theme/app_spacing.dart
@mobile/lib/shared/widgets/glass_card.dart
@mobile/lib/shared/widgets/pill_button.dart
@mobile/pubspec.yaml
@mobile/test/theme_test.dart
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mobile/lib/core/theme/app_colors.dart (CURRENT — to be replaced):
```dart
class AppColors {
  static const Color primary = Color(0xFF3B608F);  // → becomes Electric Teal #00E5FF
  static const Color secondary = Color(0xFF6A548A); // → becomes Cyber Violet #7209B7
  static const Color tertiary = Color(0xFF676001);  // → becomes Magenta #F72585
  static const Color surface = Color(0xFFF9F9FD);   // Light surface stays light
  static const Color darkSurface = Color(0xFF0F1114); // → becomes Obsidian #111317
}
```

From mobile/lib/core/theme/app_theme.dart (CURRENT — fonts to change):
```dart
// Current: GoogleFonts.interTextTheme() + GoogleFonts.plusJakartaSansTextTheme()
// Target:  GoogleFonts.montserratTextTheme() + GoogleFonts.jetBrainsMonoTextTheme()
```

From mobile/lib/core/theme/app_spacing.dart (CURRENT — mostly fine, just add explicit comment about 8px grid):
```dart
class AppSpacing {
  static const double xs = 4;   // 0.5 units
  static const double sm = 8;   // 1 unit (base)
  static const double md = 16;  // 2 units
  static const double lg = 24;  // 3 units
  static const double xl = 32;  // 4 units
  static const double xxl = 48; // 6 units
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace color palette and typography with Cyber-Academic tokens</name>
  <files>mobile/lib/core/theme/app_colors.dart, mobile/lib/core/theme/app_theme.dart, mobile/lib/core/theme/app_spacing.dart, mobile/pubspec.yaml</files>
  <read_first>
    - mobile/lib/core/theme/app_colors.dart
    - mobile/lib/core/theme/app_theme.dart
    - mobile/lib/core/theme/app_spacing.dart
    - mobile/pubspec.yaml
  </read_first>
  <action>
**1. Update `mobile/lib/core/theme/app_colors.dart`** — Replace the entire color palette:

LIGHT THEME colors:
- `primary` = `Color(0xFF00E5FF)` (Electric Teal)
- `onPrimary` = `Color(0xFF000000)` (black text on teal)
- `primaryContainer` = `Color(0xFF004D57)` (darker teal container)
- `onPrimaryContainer` = `Color(0xFF97F0FF)` (light teal text on container)
- `secondary` = `Color(0xFF7209B7)` (Cyber Violet)
- `onSecondary` = `Color(0xFFFFFFFF)` (white text on violet)
- `secondaryContainer` = `Color(0xFF3D0066)` (deep violet container)
- `onSecondaryContainer` = `Color(0xFFE8CDFF)` (light violet on container)
- `tertiary` = `Color(0xFFF72585)` (Magenta/Hot Pink)
- `onTertiary` = `Color(0xFFFFFFFF)`
- `tertiaryContainer` = `Color(0xFF7A0036)` (dark magenta container)
- `onTertiaryContainer` = `Color(0xFFFFD9E3)` (light pink on container)
- `error` = `Color(0xFFFF453A)` (bright red for visibility on dark)
- `onError` = `Color(0xFFFFFFFF)`
- `errorContainer` = `Color(0xFF930006)`
- `onErrorContainer` = `Color(0xFFFFDAD6)`
- `surface` = `Color(0xFFF5F5F7)` (very light gray for light mode)
- `onSurface` = `Color(0xFF111317)` (obsidian text)
- `surfaceContainerLowest` = `Color(0xFFFFFFFF)`
- `surfaceContainerLow` = `Color(0xFFF0F0F2)`
- `surfaceContainer` = `Color(0xFFE8E8EA)`
- `surfaceContainerHigh` = `Color(0xFFDDDDE0)`
- `surfaceContainerHighest` = `Color(0xFFD2D2D5)`
- `onSurfaceVariant` = `Color(0xFF44474F)`
- `outline` = `Color(0xFF74777F)`
- `outlineVariant` = `Color(0xFFC4C6D0)`

DARK THEME colors:
- `darkPrimary` = `Color(0xFF00E5FF)` (Electric Teal — same in dark mode for maximum neon pop)
- `darkOnPrimary` = `Color(0xFF000000)` (black text on bright teal)
- `darkPrimaryContainer` = `Color(0xFF003740)` (deep teal)
- `darkOnPrimaryContainer` = `Color(0xFF97F0FF)` (light teal)
- `darkSecondary` = `Color(0xFFBB86FC)` (lighter violet for dark mode legibility)
- `darkOnSecondary` = `Color(0xFF1A0033)` (very dark violet)
- `darkSecondaryContainer` = `Color(0xFF4A148C)` (mid violet)
- `darkOnSecondaryContainer` = `Color(0xFFE8CDFF)` (light violet)
- `darkTertiary` = `Color(0xFFFF79A8)` (lighter magenta for dark legibility)
- `darkOnTertiary` = `Color(0xFF3B0019)` (very dark pink)
- `darkTertiaryContainer` = `Color(0xFF5C0029)` (dark magenta)
- `darkOnTertiaryContainer` = `Color(0xFFFFD9E3)`
- `darkError` = `Color(0xFFFF453A)`
- `darkOnError` = `Color(0xFF690005)`
- `darkErrorContainer` = `Color(0xFF93000A)`
- `darkOnErrorContainer` = `Color(0xFFFFDAD6)`
- `darkSurface` = `Color(0xFF111317)` (Obsidian — the brand surface)
- `darkOnSurface` = `Color(0xFFE6E6EA)` (light gray text)
- `darkSurfaceContainerLowest` = `Color(0xFF0D0E12)` (deeper than obsidian)
- `darkSurfaceContainerLow` = `Color(0xFF1A1C20)` (one step up)
- `darkSurfaceContainer` = `Color(0xFF222428)` (two steps up)
- `darkSurfaceContainerHigh` = `Color(0xFF2C2E33)` (three steps up)
- `darkSurfaceContainerHighest` = `Color(0xFF37393E)` (four steps up)
- `darkOnSurfaceVariant` = `Color(0xFFC4C6D0)` (muted light)
- `darkOutline` = `Color(0xFF8E9099)`
- `darkOutlineVariant` = `Color(0xFF44474F)` (subtle borders)

Add new semantic color constants for neon effects:
```dart
// === Neon Glow Colors (for BoxShadow/effects — NOT part of ColorScheme) ===
static const Color neonTeal = Color(0xFF00E5FF);
static const Color neonViolet = Color(0xFF7209B7);
static const Color neonMagenta = Color(0xFFF72585);
```

**2. Update `mobile/lib/core/theme/app_theme.dart`** — Change typography to Montserrat + JetBrains Mono:

Replace `GoogleFonts.interTextTheme()` with `GoogleFonts.montserratTextTheme()`.
Replace `GoogleFonts.plusJakartaSansTextTheme()` with `GoogleFonts.montserratTextTheme()` (for headings — same font family with different weights).

Update heading weights per spec:
- `displayLarge/Medium/Small`: `FontWeight.w900` (Black)
- `headlineLarge/Medium`: `FontWeight.w800` (ExtraBold)
- `headlineSmall`: `FontWeight.w700` (Bold)
- `titleLarge/Medium/Small`: `FontWeight.w600` (SemiBold)
- Body text: `FontWeight.w400` (Regular) — default from montserratTextTheme

Add a static getter for the monospace text style used for technical data:
```dart
static TextStyle get monoStyle => GoogleFonts.jetBrainsMono(
  fontSize: 13,
  fontWeight: FontWeight.w400,
  letterSpacing: -0.2,
);
```

Update tracking (letterSpacing):
- Headlines (display/headline): `letterSpacing: -0.5`
- Titles: `letterSpacing: -0.3`
- Body: default (0)

Update all `GoogleFonts.plusJakartaSans(...)` calls within AppTheme to `GoogleFonts.montserrat(...)`.
Update all `GoogleFonts.inter(...)` calls within AppTheme to `GoogleFonts.montserrat(...)`.

In the light theme `appBarTheme.titleTextStyle`: change color from `AppColors.primary` to `AppColors.onSurface` (headings are obsidian, not primary colored — the neon is for accents/buttons/indicators only).

In the dark theme `appBarTheme.titleTextStyle`: change color from `AppColors.darkPrimary` to `AppColors.darkOnSurface`.

In `elevatedButtonTheme` light: change `backgroundColor` to `AppColors.primary`, `foregroundColor` to `AppColors.onPrimary` (black on teal). Keep pill shape.

In `elevatedButtonTheme` dark: change `backgroundColor` to `AppColors.darkPrimary`, `foregroundColor` to `AppColors.darkOnPrimary` (black on teal).

In `outlinedButtonTheme` (both light and dark): change `side` color to use primary color (teal border for ghost buttons) — light: `AppColors.primary`, dark: `AppColors.darkPrimary`.

In `navigationBarTheme` labels: update `GoogleFonts.inter(...)` to `GoogleFonts.montserrat(...)`.

**3. Update `mobile/lib/core/theme/app_spacing.dart`** — Add documentation comment about 8px grid and add any missing tokens. Current values are already correct (8px base). Just update the doc comment:
```dart
/// Cyber-Academic Design System — 8px base unit spacing tokens.
///
/// Grid: 8px base unit. All spacing is a multiple of 8.
/// xs=4 (half-unit), sm=8 (1 unit), md=16 (2), lg=24 (3), xl=32 (4), xxl=48 (6).
```

**4. Update `mobile/pubspec.yaml`** — No new dependencies needed. `google_fonts` (^6.2.1) already supports Montserrat and JetBrains Mono. No changes required to pubspec.yaml.
  </action>
  <acceptance_criteria>
    - mobile/lib/core/theme/app_colors.dart contains `Color(0xFF00E5FF)` for primary
    - mobile/lib/core/theme/app_colors.dart contains `Color(0xFF7209B7)` for secondary
    - mobile/lib/core/theme/app_colors.dart contains `Color(0xFFF72585)` for tertiary
    - mobile/lib/core/theme/app_colors.dart contains `Color(0xFF111317)` for darkSurface
    - mobile/lib/core/theme/app_colors.dart contains `neonTeal`
    - mobile/lib/core/theme/app_theme.dart contains `montserrat` (lowercase, from GoogleFonts call)
    - mobile/lib/core/theme/app_theme.dart contains `jetBrainsMono` or `jetBrainsMonoTextTheme`
    - mobile/lib/core/theme/app_theme.dart does NOT contain `plusJakartaSans`
    - mobile/lib/core/theme/app_theme.dart does NOT contain `interTextTheme`
    - mobile/lib/core/theme/app_spacing.dart contains `8px base unit`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && grep -q "0xFF00E5FF" lib/core/theme/app_colors.dart && grep -q "montserrat" lib/core/theme/app_theme.dart && ! grep -q "plusJakartaSans" lib/core/theme/app_theme.dart && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Color palette fully replaced with Cyber-Academic tokens, typography switched to Montserrat + JetBrains Mono, spacing documented as 8px grid. No references to old Alpha Connect palette or fonts remain in theme files.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance GlassCard and PillButton with neon glow and Cyber-Academic styling</name>
  <files>mobile/lib/shared/widgets/glass_card.dart, mobile/lib/shared/widgets/pill_button.dart</files>
  <read_first>
    - mobile/lib/shared/widgets/glass_card.dart
    - mobile/lib/shared/widgets/pill_button.dart
    - mobile/lib/core/theme/app_colors.dart
  </read_first>
  <action>
**1. Update `mobile/lib/shared/widgets/glass_card.dart`** per Cyber-Academic spec:

Change `ImageFilter.blur(sigmaX: 16, sigmaY: 16)` → `ImageFilter.blur(sigmaX: 20, sigmaY: 20)` (backdrop blur 20px per spec).

Update container decoration:
- Fill color: change alpha from 0.7/0.85 → `0.05` (5% white fill per spec). Use `Colors.white.withValues(alpha: 0.05)` for both light and dark modes (the glassmorphism spec is consistent).
- Border: change to `Colors.white.withValues(alpha: 0.12)` (1px 12% white border per spec) for both modes.
- BoxShadow: Replace current shadow with neon outer glow. Use `AppColors.neonTeal.withValues(alpha: 0.15)` as glow color in dark mode, and `AppColors.primary.withValues(alpha: 0.08)` in light mode. Set `blurRadius: 20`, `spreadRadius: 1` for the neon effect.

Add optional `glowColor` parameter to GlassCard constructor for per-card glow customization:
```dart
final Color? glowColor; // defaults to neonTeal glow
```

Add optional `elevation` parameter (1, 2, 3) that controls glow intensity:
- Level 1 (default): alpha 0.15
- Level 2: alpha 0.25 (for "Layer 2" elements per spec)
- Level 3: alpha 0.35 (hero cards)

```dart
final int elevation; // 1, 2, or 3 — controls neon glow intensity
```

Import AppColors for neon constants.

**2. Update `mobile/lib/shared/widgets/pill_button.dart`** per Cyber-Academic spec:

The primary variant should already work with new theme since it uses `colors.primary` / `colors.onPrimary` from ColorScheme. But verify:
- Primary: solid Electric Teal background with black text ← already correct via theme
- Ghost variant: change `side` to use `colors.primary` instead of `colors.outlineVariant` (teal border for ghost per spec)

Update ghost variant:
```dart
case PillButtonVariant.ghost:
  backgroundColor = Colors.transparent; // was surfaceContainer — now transparent
  foregroundColor = colors.primary; // was onSurfaceVariant — now teal text
  side = BorderSide(color: colors.primary); // was outlineVariant — now teal border
```

Keep secondary and error variants using ColorScheme values (they'll auto-update with new theme).
  </action>
  <acceptance_criteria>
    - mobile/lib/shared/widgets/glass_card.dart contains `sigmaX: 20`
    - mobile/lib/shared/widgets/glass_card.dart contains `alpha: 0.05`
    - mobile/lib/shared/widgets/glass_card.dart contains `alpha: 0.12`
    - mobile/lib/shared/widgets/glass_card.dart contains `neonTeal`
    - mobile/lib/shared/widgets/glass_card.dart contains `elevation` parameter
    - mobile/lib/shared/widgets/pill_button.dart ghost case contains `colors.primary`
    - mobile/lib/shared/widgets/pill_button.dart ghost case contains `Colors.transparent`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && grep -q "sigmaX: 20" lib/shared/widgets/glass_card.dart && grep -q "neonTeal" lib/shared/widgets/glass_card.dart && grep -q "Colors.transparent" lib/shared/widgets/pill_button.dart && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>GlassCard has 20px backdrop blur, 5% white fill, 12% white border, neon teal outer glow with 3 elevation levels. PillButton ghost variant uses transparent bg with teal border and teal text.</done>
</task>

<task type="auto">
  <name>Task 3: Update theme tests to validate Cyber-Academic tokens</name>
  <files>mobile/test/theme_test.dart</files>
  <read_first>
    - mobile/test/theme_test.dart
    - mobile/lib/core/theme/app_colors.dart
  </read_first>
  <action>
Update `mobile/test/theme_test.dart` to validate the new Cyber-Academic palette values:

Replace all Alpha Connect color assertions with Cyber-Academic values:
- `AppColors.primary` → expect `Color(0xFF00E5FF)` (was 0xFF3B608F)
- `AppColors.darkPrimary` → expect `Color(0xFF00E5FF)` (was 0xFFA1C8FF)
- `AppColors.surface` → expect `Color(0xFFF5F5F7)` (was 0xFFF9F9FD)
- `AppColors.darkSurface` → expect `Color(0xFF111317)` (was 0xFF0F1114)
- `AppColors.error` → expect `Color(0xFFFF453A)` (was 0xFFBA1A1A)
- `AppColors.secondary` → expect `Color(0xFF7209B7)` (was 0xFF6A548A)
- `AppColors.tertiary` → expect `Color(0xFFF72585)` (was 0xFF676001)

Update test group names/descriptions:
- Change `'light primary matches alpha-connect prototype value'` → `'light primary matches Cyber-Academic Electric Teal'`
- Change `'dark primary matches alpha-connect prototype value'` → `'dark primary matches Cyber-Academic Electric Teal'`
- etc.

Add new test cases:
```dart
test('neon glow colors are defined', () {
  expect(AppColors.neonTeal, const Color(0xFF00E5FF));
  expect(AppColors.neonViolet, const Color(0xFF7209B7));
  expect(AppColors.neonMagenta, const Color(0xFFF72585));
});
```

Keep spacing and radius tests unchanged (values haven't changed).

Keep `responsiveTextTheme` tests unchanged (behavior hasn't changed, just font family).

Update `appBarTheme.titleTextStyle` test expectations if any: should no longer reference `Colors.transparent` but still check `backgroundColor` is transparent.

Ensure all tests pass by running `flutter test test/theme_test.dart`.
  </action>
  <acceptance_criteria>
    - mobile/test/theme_test.dart contains `0xFF00E5FF`
    - mobile/test/theme_test.dart contains `0xFF7209B7`
    - mobile/test/theme_test.dart contains `0xFFF72585`
    - mobile/test/theme_test.dart contains `neonTeal`
    - mobile/test/theme_test.dart contains `Cyber-Academic`
    - mobile/test/theme_test.dart does NOT contain `0xFF3B608F`
    - mobile/test/theme_test.dart does NOT contain `alpha-connect prototype`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter test test/theme_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>All theme tests pass with Cyber-Academic token values. Tests validate Electric Teal primary, Cyber Violet secondary, Magenta tertiary, obsidian dark surface, and neon glow constants.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme → Screen | Color tokens consumed by all screen widgets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | D (Denial of Service) | GoogleFonts network fetch | accept | GoogleFonts caches fonts locally after first fetch; fallback to system sans-serif. Low risk — purely cosmetic degradation. |
| T-15-02 | I (Information Disclosure) | Color contrast | accept | No sensitive data exposed through colors. Accessibility contrast verified via success criteria SC-6 (functional behavior intact). |

</threat_model>

<verification>
- `flutter test test/theme_test.dart` passes all assertions
- `flutter analyze` shows no errors in theme files
- Grep confirms no references to old Alpha Connect values (0xFF3B608F, 0xFF6A548A, plusJakartaSans) in theme files
</verification>

<success_criteria>
- Cyber-Academic color tokens fully replace Alpha Connect palette
- Montserrat + JetBrains Mono replace Plus Jakarta Sans + Inter
- GlassCard enhanced with spec-compliant glassmorphism (blur 20, 5% fill, 12% border, neon glow)
- PillButton ghost variant uses teal border
- All theme tests pass
- No compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-cyber-academic-visual-redesign/15-01-SUMMARY.md`
</output>
