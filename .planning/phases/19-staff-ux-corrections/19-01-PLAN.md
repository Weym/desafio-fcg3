---
phase: 19-staff-ux-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/staff/screens/staff_shell.dart
  - mobile/lib/features/staff/screens/staff_dashboard_screen.dart
  - mobile/lib/core/router/route_names.dart
  - mobile/lib/core/router/app_router.dart
autonomous: true
requirements: [SFUX-01, SFUX-02, SFUX-03]
must_haves:
  truths:
    - "Staff bottom nav shows 'Chats' label with chat_bubble icon at position 2 (was 'Intervenção')"
    - "Dashboard KPI 'Docs Pendentes' navigates to /staff/documents?filter=pendentes"
    - "Dashboard KPI 'Chats Hoje' navigates to /staff/chats?filter=hoje"
    - "Dashboard AI rate shows truncated decimal (e.g. 95.3%, not 95.33333%)"
    - "'Ações Rápidas' section exists below KPI grid with 'Gerenciar Alunos' button navigating to /staff/cadastro"
  artifacts:
    - path: "mobile/lib/features/staff/screens/staff_shell.dart"
      provides: "Renamed tab 'Intervenção' → 'Chats' with chat_bubble icon"
      contains: "Chats"
    - path: "mobile/lib/features/staff/screens/staff_dashboard_screen.dart"
      provides: "Truncated AI rate, query param navigation, Ações Rápidas section"
      contains: "_QuickActionsSection"
    - path: "mobile/lib/core/router/route_names.dart"
      provides: "staffChats and staffCadastro route paths"
      contains: "staffChats"
  key_links:
    - from: "staff_dashboard_screen.dart"
      to: "/staff/documents?filter=pendentes"
      via: "context.go with query params"
      pattern: "filter=pendentes"
    - from: "staff_dashboard_screen.dart"
      to: "/staff/cadastro"
      via: "context.go(RoutePaths.staffCadastro)"
      pattern: "staffCadastro"
---

<objective>
Fix staff dashboard navigation and rename bottom nav tab from "Intervenção" to "Chats"

Purpose: Staff dashboard KPIs should be clickable with pre-applied filters via query params, AI rate should be truncated, and a "Ações Rápidas" section gives access to Cadastro de Alunos. Bottom nav tab rename prepares the shell for the unified Chats screen.
Output: Updated staff_shell.dart with renamed tab, updated dashboard with query param navigation and quick actions section, new route paths registered.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-staff-ux-corrections/19-CONTEXT.md
@mobile/lib/features/staff/screens/staff_shell.dart
@mobile/lib/features/staff/screens/staff_dashboard_screen.dart
@mobile/lib/core/router/route_names.dart
@mobile/lib/core/router/app_router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename shell tab + add route constants</name>
  <files>mobile/lib/features/staff/screens/staff_shell.dart, mobile/lib/core/router/route_names.dart, mobile/lib/core/router/app_router.dart</files>
  <read_first>
    - mobile/lib/features/staff/screens/staff_shell.dart
    - mobile/lib/core/router/route_names.dart
    - mobile/lib/core/router/app_router.dart
  </read_first>
  <action>
1. In `route_names.dart`:
   - Add `static const String staffChats = 'staff-chats';` in RouteNames (staff tabs section)
   - Add `static const String staffCadastro = 'staff-cadastro';` in RouteNames
   - Add `static const String staffChats = '/staff/chats';` in RoutePaths
   - Add `static const String staffCadastro = '/staff/cadastro';` in RoutePaths

2. In `staff_shell.dart`:
   - Change `_destinations` index 2 from:
     `_NavItem(icon: Icons.support_agent_outlined, activeIcon: Icons.support_agent, label: 'Intervenção')`
     to:
     `_NavItem(icon: Icons.chat_bubble_outline, activeIcon: Icons.chat_bubble, label: 'Chats')`
   - Change `_railDestinations` index 2 icon/label from `support_agent` / 'Intervenção' to `chat_bubble` / 'Chats'
   - Update `_currentIndex` method: replace `RoutePaths.staffIntervention` with `RoutePaths.staffChats`
   - Update `_onTap` case 2: replace `context.go(RoutePaths.staffIntervention)` with `context.go(RoutePaths.staffChats)`
   - Update `_prefetchAdjacentTabs`: keep `interventionSessionsProvider` read (data still needed), add `staffChatSessionsProvider`

3. In `app_router.dart`:
   - Add route for `/staff/chats` pointing to the existing `StaffAiScreen` (temporary until Plan 03 replaces it with unified chats screen)
   - Add route for `/staff/cadastro` pointing to a placeholder screen (will be built in Plan 06). For now: `Scaffold(appBar: AppBar(title: Text('Cadastro de Alunos')), body: Center(child: Text('Em construção')))`
   - Ensure `/staff/chats` supports optional `?filter=` query parameter (GoRouter queryParams are automatic)
   - Ensure `/staff/documents` supports optional `?filter=` query parameter

Per D-01, D-02, D-03, D-05.
  </action>
  <acceptance_criteria>
    - route_names.dart contains `static const String staffChats = '/staff/chats';`
    - route_names.dart contains `static const String staffCadastro = '/staff/cadastro';`
    - staff_shell.dart contains `label: 'Chats'` at position 2 in _destinations
    - staff_shell.dart contains `Icons.chat_bubble_outline` (not `Icons.support_agent_outlined` at index 2)
    - staff_shell.dart `_currentIndex` references `RoutePaths.staffChats`
    - staff_shell.dart `_onTap` case 2 uses `RoutePaths.staffChats`
    - app_router.dart contains route definition for `/staff/chats`
    - app_router.dart contains route definition for `/staff/cadastro`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5</automated>
  </verify>
  <done>Bottom nav tab renamed, routes registered, app compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard KPI navigation with query params + AI rate truncation + Ações Rápidas</name>
  <files>mobile/lib/features/staff/screens/staff_dashboard_screen.dart</files>
  <read_first>
    - mobile/lib/features/staff/screens/staff_dashboard_screen.dart
    - mobile/lib/core/router/route_names.dart
  </read_first>
  <action>
1. Fix `_calculateAiRate` to truncate to 1 decimal place:
   - Replace the mock calculation with: `double.parse(((total - 1) / total * 100).clamp(0, 100).toStringAsFixed(1))`
   - This produces 95.3 instead of 95.33333... (per D-06)

2. Fix KPI navigation to use query params (per D-07, D-08, D-09):
   - "Chats Hoje" KPI: change `onTap: () => context.go(RoutePaths.staffAI)` to `onTap: () => context.go('${RoutePaths.staffChats}?filter=hoje')`
   - "Docs Pendentes" KPI: change `onTap: () => context.go(RoutePaths.staffDocuments)` to `onTap: () => context.go('${RoutePaths.staffDocuments}?filter=pendentes')`

3. Add "Ações Rápidas" section AFTER the AI Insights GlassCard (per D-04, D-10):
   - Add `const SizedBox(height: AppSpacing.lg)` after the AI Insights card
   - Add a new section title: `Text('Ações Rápidas', style: Theme.of(context).textTheme.titleMedium?.copyWith(fontWeight: FontWeight.bold, color: colors.onSurface))`
   - Add `const SizedBox(height: AppSpacing.md)`
   - Add a `GlassCard` with `onTap: () => context.go(RoutePaths.staffCadastro)` containing:
     - Row with `Icon(Icons.people_outlined, color: colors.primary)`, `SizedBox(width: 12)`, `Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text('Gerenciar Alunos', style: titleSmall bold), Text('Cadastrar, editar e gerenciar alunos', style: bodySmall onSurfaceVariant)]))`
     - Trailing `Icon(Icons.chevron_right, color: colors.onSurfaceVariant)`
   - Wrap in padding: `const EdgeInsets.all(AppSpacing.md)`
  </action>
  <acceptance_criteria>
    - staff_dashboard_screen.dart contains `toStringAsFixed(1)` in _calculateAiRate
    - staff_dashboard_screen.dart contains `filter=pendentes` string
    - staff_dashboard_screen.dart contains `filter=hoje` string
    - staff_dashboard_screen.dart contains `RoutePaths.staffCadastro`
    - staff_dashboard_screen.dart contains string `Ações Rápidas`
    - staff_dashboard_screen.dart contains string `Gerenciar Alunos`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5</automated>
  </verify>
  <done>Dashboard truncates AI rate, KPIs navigate with query params, Ações Rápidas section visible with Gerenciar Alunos button</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Client→Router | Query params from navigation are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-19-01 | Tampering | Query param `filter` | accept | Filter values are client-side only, validated against known enum values in provider (no API call with raw param) |

</threat_model>

<verification>
- `flutter analyze --no-pub` passes with 0 errors
- Bottom nav shows "Chats" at position 2 with chat_bubble icon
- Dashboard "Docs Pendentes" card navigates to documents with filter pre-applied
- "Ações Rápidas" section visible below AI Insights
</verification>

<success_criteria>
Staff shell has renamed tab, dashboard navigates with filters, AI rate truncated, quick actions section present
</success_criteria>

<output>
After completion, create `.planning/phases/19-staff-ux-corrections/19-01-SUMMARY.md`
</output>
