---
phase: 19-staff-ux-corrections
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - mobile/lib/features/staff/screens/staff_chats_screen.dart
  - mobile/lib/features/staff/screens/staff_chat_detail_screen.dart
  - mobile/lib/features/staff/providers/staff_chat_provider.dart
  - mobile/lib/core/router/app_router.dart
autonomous: true
requirements: [SFUX-08, SFUX-09, SFUX-10, SFUX-11, SFUX-12, SFUX-13]
must_haves:
  truths:
    - "Staff bottom nav 'Chats' tab opens unified chats screen (not old AI screen)"
    - "Chats screen has 4 sub-tabs: Todos, Pendentes, Em atendimento, Concluídos"
    - "Chat cards show student name + formatted phone number"
    - "Entering a chat shows informative header with student name, RA, session date, and status"
    - "SearchBar at top of chats screen filters by Nome/RA/Telefone"
    - "'Concluídos' tab shows completed/closed intervention sessions"
  artifacts:
    - path: "mobile/lib/features/staff/screens/staff_chats_screen.dart"
      provides: "Unified chats screen with 4 sub-tabs and search"
      contains: "class StaffChatsScreen"
    - path: "mobile/lib/features/staff/screens/staff_chat_detail_screen.dart"
      provides: "Informative header with student name, RA, session data"
      contains: "studentName"
  key_links:
    - from: "app_router.dart"
      to: "StaffChatsScreen"
      via: "route /staff/chats"
      pattern: "StaffChatsScreen"
    - from: "staff_chats_screen.dart"
      to: "staff_chat_provider.dart"
      via: "ref.watch(staffChatSessionsProvider)"
      pattern: "staffChatSessionsProvider"
---

<objective>
Create unified Staff Chats screen merging AI chats and intervention into sub-tabs

Purpose: Staff need a single "Chats" screen that unifies all chat interactions. The old "Intervenção" screen and "AI" screen merge into one with sub-tabs: Todos (all sessions), Pendentes (awaiting human intervention), Em atendimento (staff actively handling), Concluídos (resolved). Each card shows student name + phone. Chat detail header shows student context.
Output: New StaffChatsScreen with 4 tabs, updated routing, informative chat detail header.
</objective>

<context>
@.planning/phases/19-staff-ux-corrections/19-CONTEXT.md
@mobile/lib/features/staff/screens/staff_intervention_screen.dart
@mobile/lib/features/staff/screens/staff_ai_screen.dart
@mobile/lib/features/staff/screens/staff_chat_detail_screen.dart
@mobile/lib/features/staff/providers/staff_chat_provider.dart
@mobile/lib/features/staff/providers/staff_intervention_provider.dart
@mobile/lib/features/staff/models/intervention_session_model.dart
@mobile/lib/core/router/app_router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified StaffChatsScreen with 4 sub-tabs + search</name>
  <files>mobile/lib/features/staff/screens/staff_chats_screen.dart, mobile/lib/features/staff/providers/staff_chat_provider.dart, mobile/lib/core/router/app_router.dart</files>
  <read_first>
    - mobile/lib/features/staff/screens/staff_intervention_screen.dart
    - mobile/lib/features/staff/screens/staff_ai_screen.dart
    - mobile/lib/features/staff/providers/staff_chat_provider.dart
    - mobile/lib/features/staff/providers/staff_intervention_provider.dart
    - mobile/lib/features/staff/models/intervention_session_model.dart
    - mobile/lib/core/router/app_router.dart
    - mobile/lib/shared/widgets/staff_search_bar.dart
  </read_first>
  <action>
1. Create `mobile/lib/features/staff/screens/staff_chats_screen.dart`:
   - Class `StaffChatsScreen extends ConsumerStatefulWidget`
   - Uses `TabController(length: 4, vsync: this)`
   - AppBar title: 'Chats', actions: `[AppBarActions()]`
   - AppBar `bottom: TabBar` with tabs: ['Todos', 'Pendentes', 'Em atendimento', 'Concluídos']
   - Below AppBar: `StaffSearchBar(hintText: 'Buscar por nome, RA ou telefone...', onChanged: (q) => ref.read(staffChatsSearchProvider.notifier).setQuery(q))`
   - Read GoRouterState query param `filter`: if `filter == 'hoje'`, auto-select "Todos" tab and filter by today's date
   - TabBarView with 4 children:
     - **Todos**: Shows ALL chat sessions (from staffChatSessionsProvider + interventionSessionsProvider merged) sorted by date
     - **Pendentes**: intervention sessions where `isPending == true`
     - **Em atendimento**: intervention sessions where `isActive == true`
     - **Concluídos**: intervention sessions where status == 'closed' or 'resolved'
   - Each tab renders a `_ChatSessionList` widget that applies the current search filter
   - Filter query param `?filter=hoje`: when detected in `initState`, filter all sessions to today's date only

2. Card design for each session (per D-22):
   - Row: CircleAvatar with initial → Column(name: titleSmall bold, phone: bodySmall formatted) → StatusBadge
   - Phone formatting: `(XX) XXXXX-XXXX` pattern from `session.studentPhone ?? session.displayIdentifier`
   - Bottom row: relative time (reuse `_relativeTime` pattern from intervention screen)
   - onTap: navigate to `/staff/chats/{sessionId}` for normal chats, `/staff/intervention/{sessionId}` for intervention

3. In `staff_chat_provider.dart`, add:
   ```dart
   @riverpod
   class StaffChatsSearch extends _$StaffChatsSearch {
     @override
     String build() => '';
     void setQuery(String query) => state = query;
   }
   ```

4. In `app_router.dart`:
   - Replace the route for `/staff/chats` (currently pointing to StaffAiScreen placeholder from Plan 01) with `StaffChatsScreen`
   - Import `staff_chats_screen.dart`

Per D-20, D-21, D-22, D-24, D-25, D-26.
  </action>
  <acceptance_criteria>
    - File `mobile/lib/features/staff/screens/staff_chats_screen.dart` exists
    - staff_chats_screen.dart contains `class StaffChatsScreen`
    - staff_chats_screen.dart contains `TabController(length: 4`
    - staff_chats_screen.dart contains tabs `'Todos'`, `'Pendentes'`, `'Em atendimento'`, `'Concluídos'`
    - staff_chats_screen.dart contains `StaffSearchBar(`
    - staff_chats_screen.dart contains phone formatting logic or `displayIdentifier`
    - app_router.dart imports and routes to `StaffChatsScreen`
    - staff_chat_provider.dart contains `class StaffChatsSearch`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5</automated>
  </verify>
  <done>Unified chats screen with 4 sub-tabs shows all sessions with name+phone, search works, query param filter supported</done>
</task>

<task type="auto">
  <name>Task 2: Chat detail informative header with student context</name>
  <files>mobile/lib/features/staff/screens/staff_chat_detail_screen.dart</files>
  <read_first>
    - mobile/lib/features/staff/screens/staff_chat_detail_screen.dart
    - mobile/lib/features/staff/providers/staff_chat_provider.dart
  </read_first>
  <action>
1. In `staff_chat_detail_screen.dart` (per D-23):
   - Add an informative header section at the top of the chat (below AppBar, above messages):
   - Container with `colors.surfaceContainerLow` background, borderRadius bottom corners
   - Content: Row with CircleAvatar(initial) + Column:
     - Student name: `session.studentName ?? 'Aluno'` (titleSmall bold)
     - RA: `'RA: ${session.studentRa ?? 'N/A'}'` (bodySmall)
     - Session info: `'Início: ${formattedDate} • ${session.status}'` (bodySmall onSurfaceVariant)
   - Add a Divider below

2. Ensure the AppBar title shows the student name (not generic "Chat"):
   - `AppBar(title: Text(session.studentName ?? 'Chat'))`

3. If the chat model doesn't have `studentName` / `studentRa`, check the session data that's passed via `extra` in GoRouter or fetched from the provider. Add fields to the model if needed.

Per D-23.
  </action>
  <acceptance_criteria>
    - staff_chat_detail_screen.dart contains `studentName` reference in header/AppBar
    - staff_chat_detail_screen.dart contains `RA:` or `studentRa` display
    - staff_chat_detail_screen.dart contains session date display (formatted)
    - staff_chat_detail_screen.dart contains `surfaceContainerLow` for header background
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5</automated>
  </verify>
  <done>Chat detail shows informative header with student name, RA, and session data above messages</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| SearchBar→Provider | User search text (client-side only) |
| QueryParam→Tab | URL ?filter param sets initial tab state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-19-04 | Information Disclosure | Chat detail header | accept | Student info already visible to staff role (authorized view) |
| T-19-05 | Tampering | Query param filter | accept | Client-side date filter, no sensitive action |

</threat_model>

<verification>
- `flutter analyze --no-pub` passes with 0 errors
- Navigating to "Chats" tab shows unified screen with 4 sub-tabs
- Chat cards display student name + phone number
- Entering a chat shows header with RA and session date
- "Concluídos" tab shows resolved sessions
</verification>

<success_criteria>
Unified Chats screen replaces old Intervenção tab, showing all sessions with proper identification and sub-tab organization
</success_criteria>

<output>
After completion, create `.planning/phases/19-staff-ux-corrections/19-03-SUMMARY.md`
</output>
