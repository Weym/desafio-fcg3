---
phase: 19-staff-ux-corrections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/staff/screens/staff_documents_screen.dart
  - mobile/lib/features/staff/providers/staff_document_provider.dart
  - mobile/lib/features/staff/screens/widgets/send_document_sheet.dart
autonomous: true
requirements: [SFUX-14, SFUX-15, SFUX-16, SFUX-17, SFUX-18]
must_haves:
  truths:
    - "Document filter tabs show: Todos | Processando | Prontos (not 'Pendentes')"
    - "Secondary filter for document type exists (dropdown or pills)"
    - "Tapping a document card opens detail bottom sheet with full solicitation data"
    - "Send/edit document uses bottom sheet pattern (drawer)"
    - "Error SnackBar shown when trying to finalize without file attached"
  artifacts:
    - path: "mobile/lib/features/staff/screens/staff_documents_screen.dart"
      provides: "Corrected tabs, type filter, detail sheet, error messaging"
      contains: "Processando"
    - path: "mobile/lib/features/staff/providers/staff_document_provider.dart"
      provides: "Type filter provider"
      contains: "StaffDocumentTypeFilter"
  key_links:
    - from: "staff_documents_screen.dart"
      to: "showModalBottomSheet"
      via: "document detail on tap"
      pattern: "showModalBottomSheet"
    - from: "staff_documents_screen.dart"
      to: "staffDocumentFilterProvider"
      via: "filter state"
      pattern: "staffDocumentFilterProvider"
---

<objective>
Fix staff documents screen: correct filter tabs, add type filter, detail sheet, and error messaging

Purpose: Staff documents screen needs tabs renamed from "Pendentes/Prontos" to "Processando/Prontos" per requirements, a secondary type filter, detail bottom sheet on tap, and proper error messaging when finalizing without file.
Output: Corrected document tabs, new type filter provider, detail bottom sheet, error SnackBar.
</objective>

<context>
@.planning/phases/19-staff-ux-corrections/19-CONTEXT.md
@mobile/lib/features/staff/screens/staff_documents_screen.dart
@mobile/lib/features/staff/providers/staff_document_provider.dart
@mobile/lib/features/staff/screens/widgets/send_document_sheet.dart
@mobile/lib/features/staff/screens/widgets/update_status_sheet.dart
@mobile/lib/features/client/models/document_model.dart
@mobile/lib/features/client/screens/widgets/document_detail_sheet.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct filter tabs + add type filter + detail bottom sheet</name>
  <files>mobile/lib/features/staff/screens/staff_documents_screen.dart, mobile/lib/features/staff/providers/staff_document_provider.dart</files>
  <read_first>
    - mobile/lib/features/staff/screens/staff_documents_screen.dart
    - mobile/lib/features/staff/providers/staff_document_provider.dart
    - mobile/lib/features/client/models/document_model.dart
    - mobile/lib/features/client/screens/widgets/document_detail_sheet.dart
  </read_first>
  <action>
1. In `staff_documents_screen.dart` filter tabs (per D-28):
   - Rename the second filter tab from 'Pendentes' (filter: 'requested') to 'Processando' (filter: 'processing')
   - Keep 'Prontos' (filter: 'ready') as-is
   - Final tabs: Todos | Processando | Prontos
   - Update `_applyFilter` to handle 'processing' value

2. Add secondary type filter below status tabs (per D-29):
   - In `staff_document_provider.dart`, add:
     ```dart
     @riverpod
     class StaffDocumentTypeFilter extends _$StaffDocumentTypeFilter {
       @override
       String? build() => null;
       void setFilter(String? type) => state = type;
     }
     ```
   - In `staff_documents_screen.dart`, add a second row of filter pills below status tabs:
     - `ref.watch(staffDocumentTypeFilterProvider)`
     - Pills: Todos, Histórico, Declaração, Atestado, Diploma, Outros
     - Each pill sets the type filter
     - Modify filter logic: apply BOTH status filter AND type filter

3. Add detail bottom sheet on card tap (per D-30):
   - Import `showDocumentDetailSheet` from client feature (if it exists as a shared pattern)
   - OR create a `_showStaffDocumentDetailSheet(BuildContext context, DocumentModel doc)` method:
     - `showModalBottomSheet(isScrollControlled: true, useSafeArea: true, ...)`
     - Content: DraggableScrollableSheet with document fields:
       - Tipo, Status (badge), Aluno (nome), Data solicitação (DD/MM/YYYY HH:MM), Observações
       - Action buttons: "Atualizar Status" (opens updateStatusSheet), "Enviar Arquivo" if status is processing
   - Wire: each `_DocumentCard` `onTap` → calls `_showStaffDocumentDetailSheet`

4. Support query param `?filter=pendentes` from Dashboard navigation:
   - In `initState` (convert to ConsumerStatefulWidget if needed):
   - Read `GoRouterState.of(context).uri.queryParameters['filter']`
   - If 'pendentes' → set filter to 'processing' (maps pendentes to processing status)

Per D-28, D-29, D-30, D-31.
  </action>
  <acceptance_criteria>
    - staff_documents_screen.dart contains filter tab with label `'Processando'`
    - staff_documents_screen.dart does NOT contain filter label `'Pendentes'` (only 'Processando' and 'Prontos')
    - staff_document_provider.dart contains `class StaffDocumentTypeFilter`
    - staff_documents_screen.dart contains `showModalBottomSheet` for document detail
    - staff_documents_screen.dart contains type filter pills (e.g., `'Histórico'`, `'Declaração'`)
    - staff_documents_screen.dart reads query parameter `filter` from GoRouterState
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5</automated>
  </verify>
  <done>Document tabs show Todos/Processando/Prontos, type filter works, card tap opens detail sheet, query param pre-applies filter</done>
</task>

<task type="auto">
  <name>Task 2: Error SnackBar when finalizing without file + send document sheet compliance</name>
  <files>mobile/lib/features/staff/screens/widgets/send_document_sheet.dart, mobile/lib/features/staff/screens/widgets/update_status_sheet.dart</files>
  <read_first>
    - mobile/lib/features/staff/screens/widgets/send_document_sheet.dart
    - mobile/lib/features/staff/screens/widgets/update_status_sheet.dart
  </read_first>
  <action>
1. In `update_status_sheet.dart` or the widget handling status change to "ready/pronto" (per D-32):
   - When staff attempts to set status to 'ready' (finalizar/entregar):
   - Check if a file URL/attachment is present
   - If NO file attached: show error SnackBar BEFORE calling API:
     ```dart
     ScaffoldMessenger.of(context).showSnackBar(
       SnackBar(
         content: const Text('Anexe o arquivo antes de finalizar o documento'),
         backgroundColor: Theme.of(context).colorScheme.error,
       ),
     );
     return; // Don't call API
     ```
   - If file IS attached: proceed with status update

2. In `send_document_sheet.dart` (per D-31):
   - Verify it uses `showModalBottomSheet` with `isScrollControlled: true, useSafeArea: true`
   - If it doesn't follow this pattern, update to match Phase 18 drawer pattern
   - Ensure the sheet has proper form fields and a submit button

Per D-31, D-32.
  </action>
  <acceptance_criteria>
    - update_status_sheet.dart (or equivalent) contains `'Anexe o arquivo antes de finalizar'` or similar error message
    - The error check uses `colorScheme.error` for SnackBar backgroundColor
    - send_document_sheet.dart uses `showModalBottomSheet` with `isScrollControlled: true`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "error" -SimpleMatch | Select-Object -First 5</automated>
  </verify>
  <done>Error SnackBar prevents finalization without file, send document sheet follows drawer pattern</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Staff→API | Status update validation (file check is client-side + server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-19-06 | Tampering | Status update without file | mitigate | Client-side validation + backend should also validate (existing API contract) |

</threat_model>

<verification>
- `flutter analyze --no-pub` passes with 0 errors
- Document tabs show "Processando" instead of "Pendentes"
- Type filter pills filter by document type
- Card tap opens detail bottom sheet
- Attempting to finalize without file shows error SnackBar
</verification>

<success_criteria>
Staff documents screen has correct tabs, type filter, detail sheet on tap, and error messaging for missing file
</success_criteria>

<output>
After completion, create `.planning/phases/19-staff-ux-corrections/19-04-SUMMARY.md`
</output>
