---
phase: 12-frontend-backend-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/core/models/auth_tokens.dart
  - mobile/lib/core/models/user_model.dart
  - mobile/lib/features/client/models/document_model.dart
  - mobile/lib/features/client/models/chat_session_model.dart
  - mobile/lib/features/client/models/chat_message_model.dart
  - mobile/lib/features/client/models/appointment_model.dart
  - mobile/lib/features/client/models/action_log_model.dart
  - mobile/lib/features/staff/models/staff_dashboard_model.dart
  - docs/api.md
autonomous: false
requirements:
  - UI-INFRA-02
  - UI-NFR-03

must_haves:
  truths:
    - "All Dart models parse real backend JSON responses without exceptions"
    - "docs/api.md reflects actual backend response shapes"
    - "Auth flow returns TokenPair matching AuthResponse model fields"
    - "Document, Chat, Appointment models handle all nullable fields correctly"
  artifacts:
    - path: "mobile/lib/core/models/auth_tokens.dart"
      provides: "Auth response model matching backend TokenPair schema"
      contains: "access_token"
    - path: "docs/api.md"
      provides: "Accurate API documentation reflecting real implementation"
      contains: "access_token"
  key_links:
    - from: "mobile/lib/core/models/auth_tokens.dart"
      to: "backend/src/features/auth/schemas.py"
      via: "JSON field names must match exactly"
      pattern: "access_token.*refresh_token"
    - from: "mobile/lib/features/client/models/document_model.dart"
      to: "backend/src/features/documents/"
      via: "JSON field names and types"
      pattern: "JsonKey"
---

<objective>
Validate and fix all API contracts between Flutter Dart models and FastAPI backend responses.

Purpose: Ensure real data flows correctly from backend to frontend without serialization exceptions. Backend is source of truth (D-02) — fix Dart models to match, then update docs/api.md to reflect reality.
Output: Corrected Dart models, regenerated .g.dart files, accurate API documentation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-frontend-backend-integration/12-CONTEXT.md
@docs/api.md
@backend/src/features/auth/schemas.py
@backend/src/features/auth/routes.py
@mobile/lib/core/models/auth_tokens.dart
@mobile/lib/core/models/user_model.dart
@mobile/lib/features/client/models/document_model.dart
@mobile/lib/features/client/models/chat_session_model.dart
@mobile/lib/features/client/models/chat_message_model.dart
@mobile/lib/features/client/models/appointment_model.dart
@mobile/lib/features/client/models/action_log_model.dart
</context>

<interfaces>
<!-- Backend response schemas (source of truth per D-02) -->

From backend/src/features/auth/schemas.py:
```python
class TokenPair(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str = "bearer"
    expires_in: int  # access_token TTL seconds

class RequestCodeResponse(BaseModel):
    message: str = "Codigo enviado"
    expires_in: int

class MeResponse(BaseModel):
    id: str
    email: EmailStr
    name: str
    role: str  # 'student' | 'staff'
```

From mobile/lib/core/models/auth_tokens.dart (current Flutter model):
```dart
class AuthResponse {
  final String accessToken;   // @JsonKey(name: 'access_token')
  final String refreshToken;  // @JsonKey(name: 'refresh_token')
  final String tokenType;     // @JsonKey(name: 'token_type')
  final int expiresIn;        // @JsonKey(name: 'expires_in')
}
```

NOTE: The Flutter AuthResponse model ALREADY matches the backend TokenPair schema.
The docs/api.md verify-code section shows an OUTDATED response format (token + user object).
Fix: Update docs/api.md to match the real backend implementation.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit all backend response schemas and compare to Dart models</name>
  <files>mobile/lib/core/models/auth_tokens.dart, mobile/lib/core/models/user_model.dart, mobile/lib/features/client/models/document_model.dart, mobile/lib/features/client/models/chat_session_model.dart, mobile/lib/features/client/models/chat_message_model.dart, mobile/lib/features/client/models/appointment_model.dart, mobile/lib/features/client/models/action_log_model.dart, mobile/lib/features/staff/models/staff_dashboard_model.dart</files>
  <read_first>
    - backend/src/features/auth/schemas.py (TokenPair, MeResponse, RequestCodeResponse)
    - backend/src/features/documents/schemas.py (Document response shapes)
    - backend/src/features/chat/schemas.py (ChatSession, ChatMessage, ActionLog shapes)
    - backend/src/features/scheduling/schemas.py (Appointment, Slot shapes)
    - backend/src/features/students/schemas.py (Student, Grade shapes)
    - backend/src/features/staff/routes.py (Dashboard KPI response shape)
    - mobile/lib/core/models/auth_tokens.dart
    - mobile/lib/core/models/user_model.dart
    - mobile/lib/features/client/models/document_model.dart
    - mobile/lib/features/client/models/chat_session_model.dart
    - mobile/lib/features/client/models/chat_message_model.dart
    - mobile/lib/features/client/models/appointment_model.dart
    - mobile/lib/features/client/models/action_log_model.dart
    - mobile/lib/features/staff/models/staff_dashboard_model.dart
  </read_first>
  <action>
For each backend feature domain, read the Pydantic schemas (the actual response models, NOT docs/api.md), then compare field-by-field against the corresponding Dart model:

1. **Auth domain** (`backend/src/features/auth/schemas.py`):
   - Compare `TokenPair` → `AuthResponse` (mobile/lib/core/models/auth_tokens.dart)
   - Compare `MeResponse` → `UserModel` (mobile/lib/core/models/user_model.dart)
   - Fix any mismatches: field names, types, nullability

2. **Documents domain** (`backend/src/features/documents/`):
   - Find the Pydantic schema for document list/detail responses
   - Compare → `DocumentModel` (mobile/lib/features/client/models/document_model.dart)
   - Ensure all fields present in backend response are in Dart model (extra fields → add with nullable)

3. **Chat domain** (`backend/src/features/chat/`):
   - Compare session schema → `ChatSessionModel`
   - Compare message schema → `ChatMessageModel`
   - Compare action log schema → `ActionLogModel`

4. **Scheduling domain** (`backend/src/features/scheduling/`):
   - Compare appointment schema → `AppointmentModel`

5. **Staff domain** (`backend/src/features/staff/`):
   - Compare dashboard KPI response → `StaffDashboardModel`

For each mismatch found:
- If backend has a field Dart doesn't: ADD to Dart model as nullable (with `@JsonKey(name: 'snake_case_name')`)
- If Dart has wrong type: FIX type to match backend
- If field is required in backend but nullable in Dart: make required in Dart
- If field naming differs: ensure `@JsonKey(name: ...)` annotation matches backend's snake_case

After all fixes, run `dart run build_runner build --delete-conflicting-outputs` to regenerate .g.dart files.

PER D-03: Before making changes, document exactly what will be altered and why (list mismatches found). Since this is an autonomous task, document in a comment block at the top of the commit message.
  </action>
  <acceptance_criteria>
    - All Dart model files have @JsonKey annotations matching backend snake_case field names
    - Running `dart run build_runner build` in mobile/ completes without errors
    - No TODO or FIXME related to "contract mismatch" remains in model files
    - auth_tokens.dart contains access_token, refresh_token, token_type, expires_in fields
    - user_model.dart contains id, email, name, role fields matching MeResponse
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5</automated>
  </verify>
  <done>All Dart models align with backend Pydantic schemas; .g.dart files regenerated successfully</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/api.md to reflect actual backend implementation</name>
  <files>docs/api.md</files>
  <read_first>
    - docs/api.md (current documentation — has outdated verify-code response)
    - backend/src/features/auth/schemas.py (real TokenPair response)
    - backend/src/features/auth/routes.py (real endpoint behavior)
  </read_first>
  <action>
Update docs/api.md to match the REAL backend implementation (backend is source of truth per D-02):

1. **POST /auth/verify-code response** — currently shows:
   ```json
   { "token": "...", "user": { "id", "name", "type", "email" }, "expires_at": "..." }
   ```
   Replace with actual backend response:
   ```json
   { "access_token": "eyJ...", "refresh_token": "eyJ...", "token_type": "bearer", "expires_in": 3600 }
   ```

2. **Add POST /auth/refresh endpoint** — currently missing from docs:
   Request: `{ "refresh_token": "eyJ..." }`
   Response: same TokenPair shape

3. **GET /auth/me response** — update to match MeResponse:
   ```json
   { "id": "uuid", "email": "aluno@universidade.edu", "name": "Joao Silva", "role": "student" }
   ```
   (Remove `phone` and `type` — real response uses `role` not `type`)

4. **Verify other endpoint responses** against actual backend schemas found during Task 1 audit. Fix any discrepancies.

5. Add a note at the top: "Last validated: 2026-05-06 against backend source code"
  </action>
  <acceptance_criteria>
    - docs/api.md POST /auth/verify-code response contains "access_token" not "token"
    - docs/api.md POST /auth/verify-code response contains "refresh_token"
    - docs/api.md contains POST /auth/refresh section
    - docs/api.md GET /auth/me response contains "role" not "type"
    - docs/api.md contains "Last validated" date note
  </acceptance_criteria>
  <verify>
    <automated>grep -c "access_token" docs/api.md && grep -c "refresh_token" docs/api.md && grep -c "/auth/refresh" docs/api.md</automated>
  </verify>
  <done>docs/api.md accurately reflects real backend API responses; no outdated payload shapes remain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify contract alignment</name>
  <what-built>Audited and fixed all Dart model ↔ backend schema mismatches. Updated API documentation.</what-built>
  <how-to-verify>
    1. Review the list of mismatches found and corrections made (in commit message or summary)
    2. Confirm changes are sensible — backend is source of truth
    3. Check that no business logic was changed (only serialization models)
    4. Optional: `cd mobile && flutter analyze` should show no new errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| API response → Dart model | Untrusted JSON parsed into typed objects |
| docs/api.md → developer trust | Documentation accuracy affects integration decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-04 | Tampering | Malformed API response crashes Flutter | mitigate | All new/changed model fields use nullable types with @JsonKey for unknown backend fields; fromJson uses generated code with try/catch in service layer |
| T-12-05 | Information Disclosure | Outdated docs leaking old response shapes | mitigate | docs/api.md updated to match real backend; "Last validated" date added for freshness tracking |

</threat_model>

<verification>
- `dart run build_runner build` succeeds in mobile/
- `flutter analyze` shows no new errors from model changes
- docs/api.md grep for outdated patterns ("token":, "type": "student") returns 0 matches in auth section
</verification>

<success_criteria>
- Every Dart model field matches its corresponding backend Pydantic schema field name and type
- docs/api.md auth section uses access_token/refresh_token pattern (not legacy token+user)
- User confirms corrections are appropriate (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-backend-integration/12-02-SUMMARY.md`
</output>
