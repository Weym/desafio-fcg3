---
phase: 12-frontend-backend-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - mobile/integration_test/app_test.dart
  - mobile/integration_test/auth_flow_test.dart
  - mobile/integration_test/documents_flow_test.dart
  - mobile/integration_test/chat_flow_test.dart
  - mobile/integration_test/staff_flow_test.dart
  - mobile/integration_test/helpers/test_config.dart
  - mobile/pubspec.yaml
autonomous: true
requirements:
  - UI-INFRA-02
  - UI-NFR-03

must_haves:
  truths:
    - "Integration tests run against real Docker stack (not mocks)"
    - "Auth flow test: login with OTP bypass → navigate to dashboard → see real data"
    - "Documents flow: auth → list documents → request document → see status change"
    - "Chat flow: auth → see sessions → open session → see messages"
    - "Staff flow: auth as staff → see KPIs → manage appointments"
  artifacts:
    - path: "mobile/integration_test/auth_flow_test.dart"
      provides: "E2E auth test using DEV_MASTER_OTP bypass"
      contains: "000000"
    - path: "mobile/integration_test/documents_flow_test.dart"
      provides: "E2E document request and list test"
      contains: "DocumentModel"
    - path: "mobile/integration_test/chat_flow_test.dart"
      provides: "E2E chat sessions and messages test"
      contains: "ChatSession"
    - path: "mobile/integration_test/staff_flow_test.dart"
      provides: "E2E staff dashboard and appointments test"
      contains: "staff"
    - path: "mobile/integration_test/helpers/test_config.dart"
      provides: "Shared test configuration (base URL, test credentials)"
      contains: "baseUrl"
  key_links:
    - from: "mobile/integration_test/auth_flow_test.dart"
      to: "http://localhost:8000/api/v1/auth/verify-code"
      via: "DioClient making real HTTP call"
      pattern: "verify-code"
    - from: "mobile/integration_test/helpers/test_config.dart"
      to: ".env.example DEV_MASTER_OTP"
      via: "test uses 000000 as OTP code"
      pattern: "000000"
---

<objective>
Create Flutter integration tests that validate end-to-end data flow against the real Docker stack.

Purpose: Prove that the entire system works together — Flutter app authenticates, fetches real data, and renders it correctly. Tests use DEV_MASTER_OTP=000000 for auth bypass (per D-09, D-12).
Output: 4 integration test files covering auth, documents, chat, and staff flows.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-frontend-backend-integration/12-CONTEXT.md
@mobile/lib/core/config/env_config.dart
@mobile/lib/features/auth/providers/auth_provider.dart
@mobile/lib/features/client/services/document_service.dart
@mobile/lib/features/client/services/chat_service.dart
@mobile/lib/features/staff/services/staff_service.dart
@docs/api.md
@backend/scripts/seed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test infrastructure and auth flow test</name>
  <files>mobile/pubspec.yaml, mobile/integration_test/helpers/test_config.dart, mobile/integration_test/auth_flow_test.dart</files>
  <read_first>
    - mobile/pubspec.yaml (check if integration_test dependency exists)
    - mobile/lib/core/config/env_config.dart (understand AppConfig.apiBaseUrl pattern)
    - mobile/lib/features/auth/providers/auth_provider.dart (auth state management)
    - mobile/lib/features/auth/screens/login_screen.dart (UI elements to interact with)
    - mobile/lib/main.dart (app entry point for integration test bootstrapping)
    - backend/scripts/seed.py (understand seeded test data: email addresses, user types)
  </read_first>
  <action>
1. Add `integration_test` dependency to `mobile/pubspec.yaml` dev_dependencies if not present:
   ```yaml
   dev_dependencies:
     integration_test:
       sdk: flutter
   ```

2. Create `mobile/integration_test/helpers/test_config.dart`:
   ```dart
   /// Shared configuration for integration tests.
   /// Tests assume Docker stack is running: docker compose up
   class TestConfig {
     /// Backend API base URL (same as Flutter web in Docker)
     static const String baseUrl = 'http://localhost:8000/api/v1';
     
     /// DEV_MASTER_OTP bypass code (must match backend DEV_MASTER_OTP env var)
     static const String devOtpCode = '000000';
     
     /// Test student email (from seed.py — first seeded student)
     static const String studentEmail = 'joao.silva@universidade.edu';
     
     /// Test staff email (from seed.py — first seeded staff)
     static const String staffEmail = 'maria.coord@universidade.edu';
     
     /// Timeout for API calls in tests
     static const Duration apiTimeout = Duration(seconds: 10);
   }
   ```
   NOTE: The email addresses above are PLACEHOLDERS. The executor MUST read `backend/scripts/seed.py` to find the actual seeded email addresses and replace them.

3. Create `mobile/integration_test/auth_flow_test.dart`:
   - Import `package:integration_test/integration_test.dart`
   - Import app's main and required providers
   - Test flow per D-13 item 1:
     a. Launch app → verify login screen appears (find email TextField)
     b. Enter `TestConfig.studentEmail` in email field
     c. Tap "Send Code" button → verify OTP input screen appears
     d. Enter `TestConfig.devOtpCode` (000000) in OTP field
     e. Tap "Verify" → verify navigation to client dashboard
     f. Verify dashboard shows real data (at least one widget with seeded content)
     g. Test logout → returns to login screen
   - Second test: staff login
     a. Enter `TestConfig.staffEmail`
     b. Complete OTP flow with 000000
     c. Verify navigation to staff dashboard
     d. Verify KPI cards display numeric values (from seeded data)
  </action>
  <acceptance_criteria>
    - mobile/pubspec.yaml contains "integration_test:" under dev_dependencies
    - mobile/integration_test/helpers/test_config.dart exists and contains "baseUrl"
    - mobile/integration_test/helpers/test_config.dart contains "000000"
    - mobile/integration_test/auth_flow_test.dart exists and contains "testWidgets"
    - mobile/integration_test/auth_flow_test.dart contains studentEmail reference
    - mobile/integration_test/auth_flow_test.dart contains staffEmail reference
  </acceptance_criteria>
  <verify>
    <automated>Test-Path "mobile/integration_test/auth_flow_test.dart" && Test-Path "mobile/integration_test/helpers/test_config.dart" && grep -c "testWidgets" mobile/integration_test/auth_flow_test.dart</automated>
  </verify>
  <done>Integration test infrastructure set up; auth flow test covers student and staff login with OTP bypass</done>
</task>

<task type="auto">
  <name>Task 2: Create documents, chat, and staff integration tests</name>
  <files>mobile/integration_test/documents_flow_test.dart, mobile/integration_test/chat_flow_test.dart, mobile/integration_test/staff_flow_test.dart</files>
  <read_first>
    - mobile/integration_test/helpers/test_config.dart (shared config from Task 1)
    - mobile/integration_test/auth_flow_test.dart (auth pattern from Task 1 to reuse)
    - mobile/lib/features/client/screens/documents_screen.dart (document UI to find widgets)
    - mobile/lib/features/client/screens/chat_history_screen.dart (chat UI)
    - mobile/lib/features/staff/screens/staff_dashboard_screen.dart (staff dashboard UI)
    - mobile/lib/features/staff/screens/schedule_screen.dart (appointments UI)
    - backend/scripts/seed.py (what data is seeded — documents, chat sessions, appointments)
  </read_first>
  <action>
1. Create `mobile/integration_test/documents_flow_test.dart` per D-13 item 2:
   - Helper function `loginAsStudent(tester)` — reuse auth flow pattern (enter email → OTP → navigate)
   - Test: "Student sees document list"
     a. Login as student
     b. Navigate to Documents tab (tap bottom nav)
     c. Wait for loading to complete
     d. Verify at least one document card is visible (from seeded data)
     e. Verify document shows status text ('requested', 'processing', 'ready', or 'delivered')
   - Test: "Student requests new document"
     a. Login as student
     b. Navigate to Documents tab
     c. Tap FAB or "Request" button
     d. Select document type (e.g., 'transcript')
     e. Confirm request
     f. Verify success feedback (snackbar or status update)

2. Create `mobile/integration_test/chat_flow_test.dart` per D-13 item 3:
   - Test: "Student sees chat sessions"
     a. Login as student
     b. Navigate to Chat tab
     c. Wait for loading
     d. Verify at least one chat session card (from seeded data)
   - Test: "Student views chat messages"
     a. Login as student
     b. Navigate to Chat tab
     c. Tap first chat session
     d. Verify messages list appears (at least one message from seed)
     e. Verify action logs tab exists (for sessions with MCP actions)

3. Create `mobile/integration_test/staff_flow_test.dart` per D-13 item 4:
   - Helper function `loginAsStaff(tester)` — use staffEmail + OTP bypass
   - Test: "Staff sees dashboard KPIs"
     a. Login as staff
     b. Verify dashboard screen with KPI grid
     c. Verify numeric values are present (total_students > 0 from seed)
   - Test: "Staff views appointments"
     a. Login as staff
     b. Navigate to Schedule tab
     c. Verify appointment cards visible (from seeded scheduling data)

All tests should:
- Use `await tester.pumpAndSettle(Duration(seconds: 5))` after API calls
- Include meaningful `find.byType()` or `find.text()` assertions
- Handle loading states (wait for CircularProgressIndicator to disappear)
  </action>
  <acceptance_criteria>
    - mobile/integration_test/documents_flow_test.dart exists and contains "testWidgets"
    - mobile/integration_test/documents_flow_test.dart contains "Documents" or "document" text finder
    - mobile/integration_test/chat_flow_test.dart exists and contains "testWidgets"
    - mobile/integration_test/chat_flow_test.dart contains "Chat" or "session" text finder
    - mobile/integration_test/staff_flow_test.dart exists and contains "testWidgets"
    - mobile/integration_test/staff_flow_test.dart contains "staff" or "Staff" text finder
    - All 3 test files import helpers/test_config.dart
  </acceptance_criteria>
  <verify>
    <automated>Test-Path "mobile/integration_test/documents_flow_test.dart" && Test-Path "mobile/integration_test/chat_flow_test.dart" && Test-Path "mobile/integration_test/staff_flow_test.dart" && grep -c "testWidgets" mobile/integration_test/documents_flow_test.dart</automated>
  </verify>
  <done>All 4 E2E test files exist covering auth, documents, chat, and staff flows against real backend</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Integration test → Backend API | Tests make real HTTP calls to localhost:8000 |
| Test credentials | Hardcoded dev emails + OTP bypass code in test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-06 | Information Disclosure | Test config contains dev emails | accept | These are seeded test data emails, not real users. File is in integration_test/ (not shipped to prod builds). |
| T-12-07 | Elevation of Privilege | Tests use master OTP bypass | accept | DEV_MASTER_OTP only works when env var is set (dev only). Integration tests are dev-only tooling. Production builds have no DEV_MASTER_OTP env var. |

</threat_model>

<verification>
- All integration test files exist and contain valid Dart test code
- `cd mobile && flutter analyze` passes with no new errors
- Tests are runnable with: `cd mobile && flutter test integration_test/ --dart-define=API_BASE_URL=http://localhost:8000/api/v1`
  (Note: actual execution requires Docker stack running — verified in CI or manually)
</verification>

<success_criteria>
- 4 integration test files created: auth_flow, documents_flow, chat_flow, staff_flow
- Tests use DEV_MASTER_OTP=000000 for authentication bypass
- Tests reference seeded data (emails from seed.py)
- Test helper config is shared and maintainable
- All test files pass `flutter analyze` (syntax valid even if stack isn't running)
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-backend-integration/12-03-SUMMARY.md`
</output>
