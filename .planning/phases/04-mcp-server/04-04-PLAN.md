---
phase: 04-mcp-server
plan: 04
type: tdd
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - mcp_server/tests/__init__.py
  - mcp_server/tests/test_session_resolver.py
  - mcp_server/tests/test_middleware_logging.py
  - mcp_server/tests/test_api_client.py
  - mcp_server/tests/test_tool_schemas.py
  - mcp_server/tests/test_service_token.py
  - mcp_server/tests/conftest.py
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, MCP-05]

must_haves:
  truths:
    - "Session resolution test validates student_id comes from chat_sessions lookup, not from agent"
    - "Middleware logging test confirms mcp_action_logs row is created for each tool call"
    - "API client test validates 5xx retry once and 4xx no-retry behavior"
    - "Tool schema test confirms student_id NEVER appears in any of the 16 tool input schemas"
    - "Service token test confirms X-Service-Token header is present on all API calls"
  artifacts:
    - path: "mcp_server/tests/test_session_resolver.py"
      provides: "Tests for resolve_student_id dependency"
      min_lines: 40
    - path: "mcp_server/tests/test_middleware_logging.py"
      provides: "Tests for ToolLoggingMiddleware writing to mcp_action_logs"
      min_lines: 40
    - path: "mcp_server/tests/test_api_client.py"
      provides: "Tests for call_api retry behavior"
      min_lines: 50
    - path: "mcp_server/tests/test_tool_schemas.py"
      provides: "Tests that student_id is absent from all 16 tool schemas"
      min_lines: 30
    - path: "mcp_server/tests/test_service_token.py"
      provides: "Tests for X-Service-Token in outgoing requests"
      min_lines: 20
  key_links:
    - from: "mcp_server/tests/test_session_resolver.py"
      to: "mcp_server/dependencies.py"
      via: "Direct test of resolve_student_id"
      pattern: "resolve_student_id"
    - from: "mcp_server/tests/test_tool_schemas.py"
      to: "all tool modules"
      via: "Schema inspection of all 16 registered tools"
      pattern: "student_id.*not.*in.*schema"
---

<objective>
Write integration tests validating the MCP server's core security and operational properties: session resolution, middleware logging, retry behavior, schema validation, and service token.

Purpose: These tests verify the 5 success criteria from the ROADMAP and ensure the MCP server's security model (student_id isolation, service token auth, logging completeness) is correct.

Output: Test suite that validates all 5 MCP requirements (MCP-01 through MCP-05).
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-mcp-server/04-CONTEXT.md
@.planning/phases/04-mcp-server/04-RESEARCH.md
@.planning/phases/04-mcp-server/04-01-SUMMARY.md
@.planning/phases/04-mcp-server/04-02-SUMMARY.md
@.planning/phases/04-mcp-server/04-03-SUMMARY.md
@docs/mcp.md

<interfaces>
From mcp_server/dependencies.py:
```python
async def resolve_student_id(request: Request = CurrentRequest()) -> str:
    # Reads X-Chat-Session-ID, queries chat_sessions, returns student_id
```

From mcp_server/middleware.py:
```python
class ToolLoggingMiddleware(Middleware):
    async def on_call_tool(self, context, call_next) -> result
    # Writes to mcp_action_logs
```

From mcp_server/api_client.py:
```python
async def call_api(client, method, path, **kwargs) -> tuple[dict, bool]
    # Retry on 5xx/timeout, ToolError on 4xx
```

Test strategy:
- Unit tests with mocked asyncpg pool and httpx client
- Use pytest + pytest-asyncio
- Mock external dependencies (DB, HTTP)
- No real Docker containers needed for unit tests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test fixtures and session resolver + API client tests</name>
  <files>mcp_server/tests/__init__.py, mcp_server/tests/conftest.py, mcp_server/tests/test_session_resolver.py, mcp_server/tests/test_api_client.py</files>
  <behavior>
    - Test: resolve_student_id returns valid student_id when chat_session exists and is active
    - Test: resolve_student_id raises ToolError when X-Chat-Session-ID header is missing
    - Test: resolve_student_id raises ToolError when session is not found in DB
    - Test: resolve_student_id raises ToolError when session status is not 'active'
    - Test: call_api returns data on successful 200 response
    - Test: call_api retries once on 500 and succeeds → returns (data, True)
    - Test: call_api retries once on timeout and fails → raises ToolError
    - Test: call_api raises ToolError immediately on 400 without retry
    - Test: call_api raises ToolError immediately on 404 without retry
    - Test: call_api extracts Portuguese error message from API error response
  </behavior>
  <action>
**mcp_server/tests/__init__.py** — empty.

**mcp_server/tests/conftest.py:**
1. Add `pytest-asyncio` and `pytest-httpx` (or `respx`) to test dependencies.
2. Create fixtures:
   - `mock_db_pool`: AsyncMock simulating asyncpg pool with `fetchval` method.
   - `mock_http_client`: AsyncMock simulating httpx.AsyncClient.
   - `mock_request`: Starlette Request mock with configurable headers.
   - `mock_context`: FastMCP Context mock with `lifespan_context` containing mock pool and client.

**mcp_server/tests/test_session_resolver.py:**
Test `resolve_student_id` and `resolve_chat_session_id` functions.
- Mock `CurrentRequest()` to inject headers.
- Mock `get_context()` to provide `lifespan_context["db_pool"]`.
- Test 4 cases: success, missing header, session not found, session inactive.
- Each test asserts either correct student_id return or ToolError with Portuguese message.

**mcp_server/tests/test_api_client.py:**
Test `call_api` function.
- Use `respx` or manual httpx mocking to simulate API responses.
- Test 6 cases: success, retry-success, retry-fail, 400 no-retry, 404 no-retry, error message extraction.
- Verify retry=True when second attempt succeeds.
- Verify ToolError message is in Portuguese.
  </action>
  <verify>
    <automated>python -m pytest mcp_server/tests/test_session_resolver.py mcp_server/tests/test_api_client.py -v --tb=short 2>&1 || echo "Tests need Docker DB - verify syntax: python -c \"import ast; ast.parse(open('mcp_server/tests/test_session_resolver.py').read()); ast.parse(open('mcp_server/tests/test_api_client.py').read()); print('OK')\"" </automated>
  </verify>
  <done>test_session_resolver.py has 4 test cases covering all session resolution paths. test_api_client.py has 6 test cases covering retry/no-retry/error extraction. conftest.py provides mock fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Middleware logging, tool schema validation, and service token tests</name>
  <files>mcp_server/tests/test_middleware_logging.py, mcp_server/tests/test_tool_schemas.py, mcp_server/tests/test_service_token.py</files>
  <behavior>
    - Test: ToolLoggingMiddleware writes correct fields to mcp_action_logs on successful tool call
    - Test: ToolLoggingMiddleware writes status="error" and output_result=null on failed tool call
    - Test: ToolLoggingMiddleware records correct latency_ms (> 0)
    - Test: ToolLoggingMiddleware includes chat_session_id from headers
    - Test: None of the 16 tool schemas contain "student_id" as a property
    - Test: Tools that need student_id have it as a Depends() parameter (hidden)
    - Test: Tool names match the 16 names defined in docs/mcp.md
    - Test: httpx client default headers include X-Service-Token
    - Test: X-Service-Token value matches MCP_SERVICE_TOKEN env var
  </behavior>
  <action>
**mcp_server/tests/test_middleware_logging.py:**
Test `ToolLoggingMiddleware.on_call_tool`.
- Create middleware instance.
- Mock `context.message` with tool_name and arguments.
- Mock `context.fastmcp_context.lifespan_context["db_pool"]` for INSERT.
- Mock `call_next` to return a result (or raise an exception).
- Test 4 cases: success, error, latency, chat_session_id inclusion.
- Verify the INSERT query contains correct fields: tool_name, input_params, output_result, reasoning=NULL, latency_ms, retry, status, chat_session_id.

**mcp_server/tests/test_tool_schemas.py:**
This test imports the FastMCP app and inspects registered tools.
- Import `mcp` from `mcp_server.main` (or create a test app that registers all tools).
- For each tool, get its input schema.
- Assert `"student_id"` is NOT in the schema's `properties` keys.
- Assert all 16 expected tool names are present.
- The list of 16 tool names comes from docs/mcp.md summary table:
  `get_student_info, get_grades, get_transcript, get_available_courses, create_enrollment, confirm_enrollment, drop_course, lock_enrollment, request_document, get_document_status, get_available_slots, book_appointment, cancel_appointment, get_curriculum, get_course_prerequisites, get_enrollment_period`

Note: If importing the full mcp instance requires DB/env setup, create a lighter test that inspects tool function signatures and Depends() annotations directly using `inspect` module.

**mcp_server/tests/test_service_token.py:**
Test that the httpx client is configured with correct headers.
- Mock settings.MCP_SERVICE_TOKEN = "test-token-123".
- Verify the httpx client constructor is called with `headers` containing `X-Service-Token: test-token-123`.
- Alternative: Test call_api sends the token by mocking httpx and inspecting request headers.
  </action>
  <verify>
    <automated>python -m pytest mcp_server/tests/test_middleware_logging.py mcp_server/tests/test_tool_schemas.py mcp_server/tests/test_service_token.py -v --tb=short 2>&1 || echo "Tests need setup - verify syntax: python -c \"import ast; [ast.parse(open(f).read()) for f in ['mcp_server/tests/test_middleware_logging.py','mcp_server/tests/test_tool_schemas.py','mcp_server/tests/test_service_token.py']]; print('OK')\"" </automated>
  </verify>
  <done>test_middleware_logging.py has 4 test cases for log writing. test_tool_schemas.py validates all 16 tool names and student_id absence. test_service_token.py confirms X-Service-Token in outgoing requests. Total: ~14 test cases covering MCP-01 through MCP-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Production code | Tests validate security properties; test failures = security regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | student_id in schema | mitigate | test_tool_schemas.py explicitly asserts student_id absent from all 16 schemas |
| T-04-15 | Spoofing | Missing X-Service-Token | mitigate | test_service_token.py validates token is always present in outgoing requests |
</threat_model>

<verification>
1. `python -m pytest mcp_server/tests/ -v` runs all tests (may need mocks for DB)
2. All test files parse without syntax errors
3. Test count: ~14 test cases covering 5 MCP requirements
4. `grep "student_id" mcp_server/tests/test_tool_schemas.py` shows assertion that student_id is NOT in schemas
</verification>

<success_criteria>
- Test suite covers all 5 ROADMAP success criteria for Phase 4:
  1. MCP-01: 16 tools registered and callable (test_tool_schemas)
  2. MCP-02: student_id absent from all tool schemas (test_tool_schemas)
  3. MCP-03: mcp_action_logs written on every tool call (test_middleware_logging)
  4. MCP-04: X-Service-Token validated (test_service_token)
  5. MCP-05: Retry on 5xx, no retry on 4xx (test_api_client)
- Additional tests: session resolver (4 cases), error message translation
- All test files syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/04-mcp-server/04-04-SUMMARY.md`
</output>
