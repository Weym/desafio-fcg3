---
phase: 04-mcp-server
plan: 05
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - mcp_server/main.py
  - mcp_server/healthcheck.py
  - mcp_server/Dockerfile
  - docker-compose.yml
  - mcp_server/tests/test_runtime_entrypoint.py
autonomous: true
gap_closure: true
requirements: [MCP-01]

must_haves:
  truths:
    - "The checked-in MCP container boots the real FastMCP package on port 8002 instead of a missing uvicorn app object"
    - "Importing mcp_server.main from an active event loop no longer crashes"
    - "Runtime wiring still exposes the same 16 registered tools and /health route after the startup fix"
  artifacts:
    - path: "mcp_server/main.py"
      provides: "Import-safe FastMCP entrypoint and package runner"
      min_lines: 25
    - path: "mcp_server/Dockerfile"
      provides: "Container startup that copies the full package and launches python -m mcp_server.main"
      min_lines: 12
    - path: "docker-compose.yml"
      provides: "mcp-server command aligned with the package entrypoint"
      contains: "python -m mcp_server.main"
    - path: "mcp_server/tests/test_runtime_entrypoint.py"
      provides: "Regression coverage for import-safe startup and runtime command alignment"
      min_lines: 40
  key_links:
    - from: "mcp_server/Dockerfile"
      to: "mcp_server/main.py"
      via: "python -m mcp_server.main container command"
      pattern: "python -m mcp_server\.main"
    - from: "docker-compose.yml"
      to: "mcp_server/main.py"
      via: "mcp-server command"
      pattern: "python -m mcp_server\.main"
    - from: "mcp_server/main.py"
      to: "mcp_server/healthcheck.py"
      via: "register_healthcheck\(mcp\) without asyncio.run"
      pattern: "register_healthcheck\(mcp\)"
---

<objective>
Close the MCP-01 runtime blocker by aligning the package entrypoint, Docker image, and compose command with the actual FastMCP server implementation.

Purpose: Phase 04 cannot be complete while the checked-in MCP container cannot boot or while importing the server inside an active event loop crashes.

Output: A runnable package-based MCP startup path plus focused regressions proving the container/runtime wiring is correct.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mcp-server/04-CONTEXT.md
@.planning/phases/04-mcp-server/04-RESEARCH.md
@.planning/phases/04-mcp-server/04-VERIFICATION.md
@.planning/phases/04-mcp-server/04-01-SUMMARY.md
@.planning/phases/04-mcp-server/04-02-SUMMARY.md
@.planning/phases/04-mcp-server/04-03-SUMMARY.md
@.planning/phases/04-mcp-server/04-04-SUMMARY.md
@docs/mcp.md
@mcp_server/main.py
@mcp_server/healthcheck.py
@mcp_server/Dockerfile
@docker-compose.yml

<interfaces>
From mcp_server/main.py:
```python
mcp = FastMCP("academic-mcp", lifespan=app_lifespan)
mcp.add_middleware(ToolLoggingMiddleware())
register_student_tools(mcp)
register_grade_tools(mcp)
register_curriculum_tools(mcp)
register_enrollment_tools(mcp)
register_document_tools(mcp)
register_scheduling_tools(mcp)
```

From mcp_server/healthcheck.py:
```python
async def register_healthcheck(mcp: FastMCP) -> None:
    @mcp.custom_route("/health", methods=["GET"])
    async def healthcheck(request: Request) -> JSONResponse:
        ...
```

From docker-compose.yml today:
```yaml
mcp-server:
  build: ./mcp_server
  volumes:
    - ./mcp_server:/app
  command: uvicorn main:app --host 0.0.0.0 --port 8002 --reload
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the MCP package startup path</name>
  <files>mcp_server/main.py, mcp_server/healthcheck.py, mcp_server/Dockerfile, docker-compose.yml</files>
  <behavior>
    - Test: importing `mcp_server.main` inside an already-running event loop does not raise `RuntimeError: asyncio.run()`
    - Test: the checked-in Dockerfile and compose command both launch `python -m mcp_server.main`, not `uvicorn main:app`
    - Test: the runtime still exposes the shared `mcp` server with the existing 16 tool registrations and `/health` route
  </behavior>
  <action>Per D-01, D-02, D-03, and D-20, keep FastMCP as the standalone server and preserve HTTP/8002 transport, but remove the import-time `asyncio.run(...)` boot hazard. Convert healthcheck registration to an import-safe pattern, keep one shared exported `mcp` instance, and add an explicit package runner such as `main()`/`run()` that starts `mcp.run(transport="http", host="0.0.0.0", port=8002)`. Update `mcp_server/Dockerfile` to install requirements from the package directory, copy the full `mcp_server/` package (not just `main.py`), and start the service with `python -m mcp_server.main`. Update the `mcp-server` service command in `docker-compose.yml` to the same package entrypoint so bind-mounted local development matches the image. Do not introduce a FastAPI wrapper, do not rename the exported `mcp` object, and do not change any tool registration order unless required for import safety.</action>
  <verify>
    <automated>python -m pytest mcp_server/tests/test_runtime_entrypoint.py -q</automated>
  </verify>
  <done>The repo's MCP runtime path is package-based and import-safe: Dockerfile + compose no longer reference `uvicorn main:app`, importing `mcp_server.main` is safe under an active event loop, and the exported FastMCP server still represents the full 16-tool surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add startup regressions that lock the runtime fix in place</name>
  <files>mcp_server/tests/test_runtime_entrypoint.py</files>
  <behavior>
    - Test: module import under `asyncio.run(...)` succeeds without nested-loop failure
    - Test: `asyncio.run(mcp.list_tools())` still returns all 16 documented tool names after the startup refactor
    - Test: `mcp_server/Dockerfile` contains a full-package copy step and a `python -m mcp_server.main` command
    - Test: `docker-compose.yml` uses the same package entrypoint for the `mcp-server` service
  </behavior>
  <action>Create `mcp_server/tests/test_runtime_entrypoint.py` as the focused regression suite for the Phase 04 startup gap. Make the tests inspect the exported `mcp` instance and the checked-in runtime manifests instead of depending on a live container. Assert the 16 tool names listed in `docs/mcp.md`, assert import safety from an active event loop, and assert both Docker runtime manifests point at the package entrypoint and not the old `uvicorn main:app` stub path.</action>
  <verify>
    <automated>python -m pytest mcp_server/tests/test_runtime_entrypoint.py -q</automated>
  </verify>
  <done>A focused regression file proves the MCP runtime fix and will fail if Docker/compose drift back to the broken `main:app` startup path or if import-time event-loop crashes return.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker/Compose → MCP runtime | Container startup hands control to the Python entrypoint; wrong command breaks transport availability |
| MCP runtime → FastMCP registration | Import-time side effects can crash the process before any tool call reaches the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | D | `mcp_server/Dockerfile`, `docker-compose.yml` | mitigate | Replace `uvicorn main:app` with `python -m mcp_server.main` and copy the full package so the HTTP transport can actually start |
| T-04-05-02 | D | `mcp_server/main.py` | mitigate | Remove import-time `asyncio.run(...)` so active-event-loop imports used by tests and future AI integration cannot crash the server module |
| T-04-05-03 | T | MCP runtime manifests | mitigate | Add regression tests that assert both runtime manifests keep the package entrypoint and preserve the 16-tool registration surface |

</threat_model>

<verification>
1. `python -m pytest mcp_server/tests/test_runtime_entrypoint.py -q`
2. `python -c "from mcp_server.main import mcp; print(type(mcp).__name__)"`
3. `docker compose config --services`
</verification>

<success_criteria>
- The checked-in MCP image and compose service both start from `python -m mcp_server.main`.
- Importing `mcp_server.main` no longer crashes under an active event loop.
- The exported FastMCP server still lists the same 16 tools documented in `docs/mcp.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mcp-server/04-05-SUMMARY.md`
</output>
