---
phase: 16-auth-router-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/core/network/auth_interceptor.dart
  - mobile/lib/core/router/app_router.dart
autonomous: true
requirements:
  - UI-INFRA-03
  - UI-NFR-03

must_haves:
  truths:
    - "auth_interceptor.dart uses QueuedInterceptor with async onRequest that awaits token read before handler.next"
    - "auth_interceptor.dart onError properly awaits refresh flow before resolving or passing error"
    - "app_router.dart uses Riverpod provider pattern — no module-level GlobalKey<NavigatorState>"
    - "debugLogDiagnostics is guarded behind kDebugMode check"
    - "All existing Flutter tests pass (244+ tests, excluding pre-existing failures)"
  artifacts:
    - path: "mobile/lib/core/network/auth_interceptor.dart"
      provides: "QueuedInterceptor with proper async handlers"
      contains: "class AuthInterceptor extends QueuedInterceptor"
    - path: "mobile/lib/core/router/app_router.dart"
      provides: "Riverpod-managed GoRouter without GlobalKey"
      contains: "GoRouter appRouter(Ref ref)"
  key_links:
    - from: "mobile/lib/core/network/auth_interceptor.dart"
      to: "flutter_secure_storage"
      via: "async read/write in onRequest/onError"
      pattern: "await _storage\\.read"
    - from: "mobile/lib/core/router/app_router.dart"
      to: "auth_provider"
      via: "ref.listen triggers refreshListenable"
      pattern: "ref\\.listen\\(authProvider"
---

<objective>
Verify and confirm that Phase 16 tech debt fixes (CR-01, CR-02, CR-03, debugLogDiagnostics) are already implemented correctly in the codebase.

Purpose: The auth interceptor async void patterns, GlobalKey router issue, and debug logging leak were identified in the M2 audit. Code review shows all four fixes are already applied — this plan confirms via automated verification and ensures test suite stability.

Output: Verified auth_interceptor.dart and app_router.dart pass all criteria; tests green.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mobile/lib/core/network/auth_interceptor.dart
@mobile/lib/core/router/app_router.dart
</context>

<interfaces>
<!-- Key contracts the executor needs to verify against -->

From mobile/lib/core/network/auth_interceptor.dart:
```dart
class AuthInterceptor extends QueuedInterceptor {
  @override
  Future<void> onRequest(RequestOptions options, RequestInterceptorHandler handler) async {
    final token = await _storage.read(key: _accessTokenKey);
    // ...
    handler.next(options);
  }

  @override
  Future<void> onError(DioException err, ErrorInterceptorHandler handler) async {
    // 401 handling with refresh
    // ...
    handler.next(err);
  }
}
```

From mobile/lib/core/router/app_router.dart:
```dart
@riverpod
GoRouter appRouter(Ref ref) {
  final authNotifier = ValueNotifier<AuthState>(ref.read(authProvider));
  ref.listen(authProvider, (_, next) { authNotifier.value = next; });
  // ...
  return GoRouter(
    debugLogDiagnostics: kDebugMode,
    refreshListenable: authNotifier,
    // ...
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify CR-01/CR-02 — Auth Interceptor async patterns are correct</name>
  <files>mobile/lib/core/network/auth_interceptor.dart</files>
  <read_first>
    - mobile/lib/core/network/auth_interceptor.dart
  </read_first>
  <action>
Verify that auth_interceptor.dart satisfies all four criteria:

1. **CR-01 (onRequest):** Class extends `QueuedInterceptor` (not `Interceptor`). Method signature is `Future<void> onRequest(...)` with `async` keyword. Token is read with `await _storage.read(key: _accessTokenKey)` BEFORE `handler.next(options)` is called. There is no fire-and-forget pattern.

2. **CR-02 (onError):** Method signature is `Future<void> onError(...)` with `async` keyword. Refresh token read uses `await`. The `_dio.post('/auth/refresh', ...)` call uses `await`. Token storage writes use `await`. Retry fetch uses `await`. All async operations complete before `handler.next(err)` or `handler.resolve(retryResponse)`.

If all criteria pass, no code change needed — output confirmation.

If any criterion fails (unexpected — code review shows they pass):
- CR-01 fix: Change `extends Interceptor` to `extends QueuedInterceptor`, change `void onRequest` to `Future<void> onRequest(...) async`, ensure `await` before storage read.
- CR-02 fix: Change `void onError` to `Future<void> onError(...) async`, ensure all storage/network calls use `await`.
  </action>
  <acceptance_criteria>
    - auth_interceptor.dart contains `class AuthInterceptor extends QueuedInterceptor`
    - auth_interceptor.dart contains `Future<void> onRequest(`
    - auth_interceptor.dart contains `Future<void> onError(`
    - auth_interceptor.dart contains `await _storage.read(key: _accessTokenKey)`
    - auth_interceptor.dart does NOT contain `void onRequest(` (without Future)
    - auth_interceptor.dart does NOT contain `void onError(` (without Future)
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && grep -c "extends QueuedInterceptor" lib/core/network/auth_interceptor.dart && grep -c "Future<void> onRequest" lib/core/network/auth_interceptor.dart && grep -c "Future<void> onError" lib/core/network/auth_interceptor.dart && grep -c "await _storage.read" lib/core/network/auth_interceptor.dart</automated>
  </verify>
  <done>auth_interceptor.dart uses QueuedInterceptor with Future<void> onRequest and onError, all storage reads are awaited — CR-01 and CR-02 confirmed fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CR-03 + debugLogDiagnostics — Router patterns are correct, run test suite</name>
  <files>mobile/lib/core/router/app_router.dart</files>
  <read_first>
    - mobile/lib/core/router/app_router.dart
  </read_first>
  <action>
Verify that app_router.dart satisfies criteria:

1. **CR-03 (GlobalKey):** The file does NOT contain `final.*GlobalKey<NavigatorState>` at module level. Instead, GoRouter is created inside a `@riverpod` provider function `GoRouter appRouter(Ref ref)`. Auth state changes use `ValueNotifier` + `ref.listen` + `refreshListenable` — NOT router recreation that causes GlobalKey reuse crashes.

2. **debugLogDiagnostics:** The `GoRouter(...)` constructor passes `debugLogDiagnostics: kDebugMode` — this ensures debug logging ONLY appears in debug builds. The file imports `package:flutter/foundation.dart` which provides `kDebugMode`.

3. **Test suite:** Run `flutter test` in the mobile directory. Confirm 238+ tests pass. Pre-existing failures in `staff_models_test.dart` (toJson snake_case) and `shared_widgets_test.dart` (ResponsiveContainer) are known and unrelated to this phase.

If all criteria pass, no code change needed — output confirmation.

If CR-03 fails (unexpected): Remove module-level GlobalKey, wrap router creation in @riverpod provider, use ValueNotifier pattern for auth state listening.
If debugLogDiagnostics fails: Change `debugLogDiagnostics: true` to `debugLogDiagnostics: kDebugMode`, add `import 'package:flutter/foundation.dart'`.
  </action>
  <acceptance_criteria>
    - app_router.dart contains `@riverpod` annotation
    - app_router.dart contains `GoRouter appRouter(Ref ref)`
    - app_router.dart contains `debugLogDiagnostics: kDebugMode`
    - app_router.dart contains `import 'package:flutter/foundation.dart'`
    - app_router.dart does NOT contain `final.*navigatorKey.*=.*GlobalKey` at module level
    - app_router.dart contains `ValueNotifier<AuthState>`
    - app_router.dart contains `refreshListenable: authNotifier`
    - `flutter test` exits with 236+ passing tests (pre-existing failures in staff_models and shared_widgets are acceptable)
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && grep -c "@riverpod" lib/core/router/app_router.dart && grep -c "debugLogDiagnostics: kDebugMode" lib/core/router/app_router.dart && grep -c "ValueNotifier" lib/core/router/app_router.dart && flutter test --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>app_router.dart uses Riverpod provider (no GlobalKey), debugLogDiagnostics guarded by kDebugMode, and test suite confirms 236+ tests passing — CR-03 and debugLogDiagnostics confirmed fixed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Token storage → HTTP headers | JWT read from secure storage must complete before request fires |
| 401 response → Refresh flow | Refresh must complete atomically (QueuedInterceptor serializes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-01 | Spoofing | auth_interceptor onRequest | mitigate | QueuedInterceptor ensures token is attached before request proceeds — no race condition where request fires without auth header |
| T-16-02 | Information Disclosure | app_router debugLogDiagnostics | mitigate | kDebugMode guard prevents route logging in release builds — validated via grep |
| T-16-03 | Denial of Service | auth_interceptor onError | accept | Concurrent 401s are serialized by QueuedInterceptor — only one refresh attempt at a time, preventing token refresh storms |

</threat_model>

<verification>
1. `grep "extends QueuedInterceptor" mobile/lib/core/network/auth_interceptor.dart` returns match
2. `grep "Future<void> onRequest" mobile/lib/core/network/auth_interceptor.dart` returns match
3. `grep "Future<void> onError" mobile/lib/core/network/auth_interceptor.dart` returns match
4. `grep "debugLogDiagnostics: kDebugMode" mobile/lib/core/router/app_router.dart` returns match
5. `grep "GlobalKey<NavigatorState>" mobile/lib/core/router/app_router.dart` returns NO match (only comment reference)
6. `cd mobile && flutter test --no-pub` passes 236+ tests
</verification>

<success_criteria>
- All 4 audit items verified: CR-01 (async onRequest), CR-02 (async onError), CR-03 (no GlobalKey), debugLogDiagnostics (kDebugMode guard)
- Flutter test suite stable (236+ passing, only pre-existing unrelated failures)
- No code changes required — fixes already applied
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-router-tech-debt/16-01-SUMMARY.md`
</output>
