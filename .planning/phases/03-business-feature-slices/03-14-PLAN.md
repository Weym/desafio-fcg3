---
phase: 03-business-feature-slices
plan: 14
type: execute
wave: 7
depends_on: ["03-03", "03-13"]
files_modified:
  - backend/src/features/courses/services.py
  - backend/tests/unit/test_course_prerequisite_tree.py
autonomous: true
gap_closure: true
requirements:
  - COURSE-03
must_haves:
  truths:
    - "The recursive prerequisite endpoint never emits the root course inside its own descendant tree, even when prerequisite data contains a cycle."
    - "Acyclic prerequisite chains still return nested descendants in the documented tree shape for COURSE-03."
    - "The supported in-container pytest workflow proves the cycle-safety regression is closed before Phase 03 is marked complete."
  artifacts:
    - path: "backend/src/features/courses/services.py"
      provides: "Cycle-safe prerequisite tree builder that excludes the root course from descendants"
      contains: "_build_prerequisite_tree"
    - path: "backend/tests/unit/test_course_prerequisite_tree.py"
      provides: "Focused regression coverage for cyclic and acyclic prerequisite trees"
      contains: "root course"
  key_links:
    - from: "backend/src/features/courses/services.py"
      to: "backend/tests/unit/test_course_prerequisite_tree.py"
      via: "unit regression coverage for flat-row to tree conversion"
      pattern: "_build_prerequisite_tree|root course"
---

<objective>
Close the last Phase 03 verification blocker by making the course prerequisite tree builder cycle-safe for COURSE-03 and adding a focused regression that proves the root course can never reappear under its own descendants.

Purpose: `03-VERIFICATION.md` is blocked by a real correctness bug in `CourseService._build_prerequisite_tree()`, not by missing docs or runtime proof. Fixing the tree builder and locking it with regression coverage is the final step needed before Phase 03 can be re-verified complete.
Output: Updated course prerequisite-tree logic plus a focused pytest regression runnable through the documented `fastapi-app` Docker workflow.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-business-feature-slices/03-VERIFICATION.md
@.planning/phases/03-business-feature-slices/03-03-SUMMARY.md
@.planning/phases/03-business-feature-slices/03-13-SUMMARY.md
@backend/src/features/courses/services.py
@backend/src/features/courses/schemas.py
@backend/tests/conftest.py

<interfaces>
From `backend/src/features/courses/schemas.py`:
```python
class PrerequisiteTreeNode(BaseModel):
    id: UUID
    code: str
    name: str
    prerequisites: list[PrerequisiteTreeNode] = []
```

From `backend/src/features/courses/services.py`:
```python
async def get_prerequisite_tree(
    db: AsyncSession,
    course_id: UUID,
) -> PrerequisiteTreeNode

@staticmethod
def _build_prerequisite_tree(
    root_id: UUID,
    rows: list[Any],
) -> list[PrerequisiteTreeNode]
```

Current gap site from `03-VERIFICATION.md`:
```text
_build_prerequisite_tree() starts recursion with an empty visited set
(return _build_children(root_id, set())), allowing the root course to appear
again under a descendant before recursion stops.
```
</interfaces>

## Gap Source
- `03-VERIFICATION.md` marks one remaining failed truth: COURSE-03 is incorrect on cyclic prerequisite graphs.
- The verifier identified a precise root cause in `backend/src/features/courses/services.py`: recursion starts with `set()` instead of a visited set already containing the root course.
- `03-13-SUMMARY.md` established `docker compose exec -T fastapi-app ... python -m pytest ...` as the supported regression path, so use that exact verification workflow here rather than inventing a new environment.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused COURSE-03 cycle-safety regression coverage</name>
  <files>backend/tests/unit/test_course_prerequisite_tree.py</files>
  <behavior>
    - Test 1: A cyclic flat prerequisite graph `A -> B -> C -> A` returns descendants for `A` without ever emitting `A` again below the root.
    - Test 2: An acyclic chain still returns nested descendants in order (`A -> B -> C`) so the fix does not flatten or truncate valid prerequisite trees.
  </behavior>
  <action>
    Create `backend/tests/unit/test_course_prerequisite_tree.py` with focused unit coverage against `CourseService._build_prerequisite_tree()`.

    Implement the tests using synthetic flat rows shaped exactly like the service consumes today: `(id, code, name, parent_id, depth)`. Keep them database-free and fast; this is a pure tree-builder regression, not an HTTP or SQLAlchemy integration test.

    Cover the verifier's failed scenario directly:
    - Build UUID-backed rows for a cycle where the root course becomes reachable again through descendants.
    - Assert the returned descendants never contain the original root UUID/code anywhere below the top-level response.
    - Add one acyclic control case so the regression proves valid nesting still works after the fix.

    Do not add network calls, app bootstrapping, or Docker-specific logic inside the test file. Keep the scope limited to COURSE-03 correctness.
  </action>
  <verify>
    <automated>docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; python -m pytest tests/unit/test_course_prerequisite_tree.py -q"</automated>
  </verify>
  <done>
    The new test file fails against the current bug, names the root-reinsertion scenario explicitly, and can be run through the supported `fastapi-app` container workflow.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the prerequisite tree builder exclude the root from descendants</name>
  <files>backend/src/features/courses/services.py</files>
  <behavior>
    - Test 1: When a cycle points back to the root, `_build_prerequisite_tree()` skips the root descendant instead of rendering it.
    - Test 2: Non-cyclic descendants still recurse normally and preserve the `PrerequisiteTreeNode` output contract used by `get_prerequisite_tree()`.
  </behavior>
  <action>
    Update `CourseService._build_prerequisite_tree()` in `backend/src/features/courses/services.py` to close the verifier's reported logic hole.

    Required fix:
    - Seed recursion with the root course already marked visited (`{root_id}`) or an equivalent guard that guarantees the root can never appear below itself, per the missing item in `03-VERIFICATION.md`.
    - Preserve the existing recursive CTE depth limit and the current public response contract from `PrerequisiteTreeNode`; this gap plan is a correctness repair, not a redesign of COURSE-03.
    - Keep the builder defensive for other cycles as well by continuing to propagate visited descendants through recursive calls.
    - Update any nearby inline comment/docstring so the next reader can see that the Python-side guard now prevents root reinsertion on cyclic graphs.

    Do not change controller routes, schema names, or dual-auth wiring. Limit the production change to the course service logic needed to make the new regression pass.
  </action>
  <verify>
    <automated>docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; python -m pytest tests/unit/test_course_prerequisite_tree.py -q"</automated>
  </verify>
  <done>
    `CourseService._build_prerequisite_tree()` can no longer emit the root course under its own descendants, the focused regression passes, and COURSE-03 remains structurally unchanged for valid acyclic data.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Recursive SQL result rows → Python tree builder | Untrusted/dirty prerequisite graph data crosses from database rows into recursive in-memory structure building |
| Regression tests → release decision | Phase 03 completion depends on the focused regression accurately proving the cycle-safety fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-14-01 | T | `backend/src/features/courses/services.py` | mitigate | Seed the visited set with `root_id` and continue visited propagation so cyclic prerequisite data cannot tamper with COURSE-03 output by reinserting the root below itself |
| T-03-14-02 | D | Recursive prerequisite traversal | mitigate | Preserve the existing CTE depth limit and Python visited-set guard so cyclic graphs cannot cause runaway recursion or oversized trees |
| T-03-14-03 | R | `backend/tests/unit/test_course_prerequisite_tree.py` | mitigate | Add explicit regression assertions for both cyclic and acyclic graphs so re-verification has reproducible proof tied to the exact failed truth |

</threat_model>

<verification>
1. Run `docker compose exec -T fastapi-app sh -lc "cd /app && python -m pytest tests/unit/test_course_prerequisite_tree.py -q"`.
2. Confirm the cyclic case proves the root course UUID/code is absent from all descendants.
3. Confirm the acyclic control case still returns nested prerequisites instead of truncating valid tree depth.
4. Re-run Phase 03 verification after execution so `03-VERIFICATION.md` can be updated from `gaps_found` if this was the last blocker.
</verification>

<success_criteria>
- The remaining Phase 03 blocker in `03-VERIFICATION.md` is directly addressed by code, not by documentation-only changes.
- COURSE-03 returns a cycle-safe tree where the root never appears in its own descendants.
- The focused Docker-based pytest regression passes and becomes the proof artifact for re-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-feature-slices/03-14-SUMMARY.md`
</output>
