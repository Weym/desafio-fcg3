---
phase: 03-business-feature-slices
plan: 13
type: execute
wave: 6
depends_on: ["03-12"]
files_modified:
  - backend/Dockerfile
  - docker-compose.yml
  - .planning/phases/03-business-feature-slices/03-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - STU-06
  - STU-07
  - GRADES-03
must_haves:
  truths:
    - "The `fastapi-app` verification container can run the documented Phase 03 pytest commands without missing-package or missing-tests failures."
    - "The live container exposes the `tests/` tree and pytest configuration needed for the focused Phase 03 regressions already used in verification artifacts."
    - "Phase 03 validation docs name the supported in-container pytest workflow instead of implying an unsupported environment."
  artifacts:
    - path: "backend/Dockerfile"
      provides: "FastAPI dev image with runtime + dev/test dependencies installed"
      contains: "requirements-dev.txt"
    - path: "docker-compose.yml"
      provides: "Live `fastapi-app` mounts for tests and pytest config"
      contains: "./backend/tests:/app/tests"
    - path: ".planning/phases/03-business-feature-slices/03-VALIDATION.md"
      provides: "Phase 03 command documentation aligned to the supported verification container workflow"
      contains: "pytest"
  key_links:
    - from: "backend/Dockerfile"
      to: "backend/requirements-dev.txt"
      via: "dev/test dependency installation for `fastapi-app`"
      pattern: "requirements-dev.txt|pytest"
    - from: "docker-compose.yml"
      to: "backend/tests"
      via: "bind-mounted `/app/tests` tree for live pytest execution"
      pattern: "backend/tests:/app/tests"
    - from: ".planning/phases/03-business-feature-slices/03-VALIDATION.md"
      to: "docker compose exec -T fastapi-app"
      via: "documented supported regression command path"
      pattern: "fastapi-app|pytest"
---

<objective>
Close the Phase 03 test-container gap by making `fastapi-app` a supported verification container for the documented pytest regressions, then align the phase validation docs to that actual execution path.

Purpose: UAT already established that the current stack is a runtime-only container while Phase 03 verification expects in-container regression commands. The smallest fix is to align `fastapi-app` with that expectation, not to invent a second backend test service during gap closure.
Output: Updated backend image/runtime config for pytest execution plus Phase 03 validation docs that reference the supported container command path.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-business-feature-slices/03-UAT.md
@.planning/debug/phase-03-pytest-missing.md
@.planning/phases/03-business-feature-slices/03-09-SUMMARY.md
@.planning/phases/03-business-feature-slices/03-10-SUMMARY.md
@.planning/phases/03-business-feature-slices/03-11-SUMMARY.md
@.planning/phases/03-business-feature-slices/03-VALIDATION.md
@backend/Dockerfile
@backend/requirements.txt
@backend/requirements-dev.txt
@backend/pyproject.toml
@docker-compose.yml

<interfaces>
From `backend/Dockerfile` today:
```dockerfile
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY alembic.ini ./
COPY alembic/ ./alembic/
COPY src/ ./src/
COPY scripts/ ./scripts/
```

From `backend/requirements-dev.txt`:
```text
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
freezegun>=1.5.0
```

From `backend/pyproject.toml`:
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
```

From `03-VERIFICATION.md`:
```markdown
| STU-06 regression | `pytest tests/unit/test_students_academic_summary.py -q` |
| STU-07 raw-list contract | `pytest tests/integration/test_students_available_courses.py -q` |
| GRADES CRA behavior | `pytest tests/unit/test_grades_cra.py -q` |
```
</interfaces>

## Gap Source
- `03-UAT.md` diagnoses `fastapi-app` as a runtime-only image: no `pytest`, no `tests/`, and therefore no supported in-container regression path.
- `03-VALIDATION.md` and `03-VERIFICATION.md` already encode pytest as the Phase 03 regression mechanism, so the dev stack should support those commands directly.
- Keep this gap separate from `03-12`: that plan restores migration/runtime sync for ENROLL-06; this plan restores the pytest verification environment.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `fastapi-app` a supported pytest verification container</name>
  <files>backend/Dockerfile, docker-compose.yml</files>
  <action>
    Update the backend image/runtime so the existing `fastapi-app` service can execute the focused Phase 03 pytest commands documented in validation and verification artifacts.

    Implement the smallest environment fix:
    - In `backend/Dockerfile`, install `requirements-dev.txt` on top of the runtime requirements used by `fastapi-app`; keep `requirements.txt` as the base layer instead of moving runtime deps into dev-only files.
    - Ensure the image has access to the pytest configuration and test tree needed by the documented commands (`pyproject.toml` and `tests/`).
    - In `docker-compose.yml`, mount `./backend/tests:/app/tests` and `./backend/pyproject.toml:/app/pyproject.toml` for the live dev/UAT container, alongside the existing code mounts.
    - Preserve the current FastAPI runtime command, ports, env vars, and hot-reload behavior.
    - Do not create a second backend test container or a parallel verification service in this gap plan unless the current `fastapi-app` shape makes in-container verification impossible. The default choice here is to honor the documented UAT expectation with the smallest config change.
  </action>
  <verify>
    <automated>docker compose up -d --build fastapi-app &amp;&amp; docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; python -m pytest tests/unit/test_students_academic_summary.py -q &amp;&amp; python -m pytest tests/integration/test_students_available_courses.py -q &amp;&amp; python -m pytest tests/unit/test_grades_cra.py -q"</automated>
  </verify>
  <done>
    - `python -m pytest ...` works inside `fastapi-app` for the documented Phase 03 regressions.
    - The live container has both the pytest package set and the `tests/` tree available.
    - The fix stays limited to verification-environment enablement, not business endpoint behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align Phase 03 validation docs to the supported in-container pytest workflow</name>
  <files>.planning/phases/03-business-feature-slices/03-VALIDATION.md</files>
  <action>
    Update `03-VALIDATION.md` so the documented regression workflow matches the environment supported after Task 1.

    Make the documentation explicit and operational:
    - Update the quick/full run guidance to name the supported `docker compose exec -T fastapi-app ... pytest ...` path for Docker-based verification.
    - Preserve the existing test intent and command coverage for the focused regressions already referenced by `03-VERIFICATION.md` (STU-06, STU-07, GRADES-03) rather than inventing new ad-hoc checks.
    - If you keep a host-side `cd backend && pytest ...` command for local non-Docker work, clearly mark it as optional/local-only so UAT no longer implies an unsupported container environment.
    - Make those labels literal in the file so automation can assert them: the supported Docker workflow must mention `docker compose exec -T fastapi-app`, and any host-side pytest command retained for convenience must be marked `optional` or `local-only` in the surrounding text.
    - Do not rewrite `03-UAT.md`'s historical diagnosis; only align the forward-looking validation instructions.
  </action>
  <verify>
    <automated>docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; python -m pytest tests/unit/test_students_academic_summary.py -q"</automated>
    <automated>python -c "from pathlib import Path; text = Path(r'.planning/phases/03-business-feature-slices/03-VALIDATION.md').read_text(encoding='utf-8').lower(); assert 'docker compose exec -t fastapi-app' in text or 'docker compose exec -t fastapi-app' in text.replace('-t','-T').lower() or 'docker compose exec -T fastapi-app'.lower() in text; assert 'pytest' in text; assert 'optional' in text or 'local-only' in text; assert 'cd backend && pytest' not in text or 'optional' in text[text.index('cd backend && pytest')-80:text.index('cd backend && pytest')+120] or 'local-only' in text[text.index('cd backend && pytest')-80:text.index('cd backend && pytest')+120]"</automated>
  </verify>
  <done>
    - `03-VALIDATION.md` names a real supported Docker verification workflow.
    - Automated verification proves the doc now names `docker compose exec -T fastapi-app` as the supported workflow and marks any retained host-side pytest command as optional/local-only.
    - A fast focused in-container pytest smoke command still proves the documented container path works.
    - Phase 03 verification guidance is aligned with the container behavior used in UAT.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repo test/dependency config → `fastapi-app` container | The verification container must receive dev dependencies, test files, and pytest config from the workspace |
| Validation docs → operator execution path | Documented commands must match a real supported environment or regressions will be missed/misreported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-13-01 | D | `fastapi-app` verification environment | mitigate | Install `requirements-dev.txt` and expose `/app/tests` + `/app/pyproject.toml` so documented pytest commands do not fail before exercising the code under test |
| T-03-13-02 | R | `.planning/phases/03-business-feature-slices/03-VALIDATION.md` | mitigate | Update validation instructions to the supported `docker compose exec -T fastapi-app` workflow so future UAT/verification evidence is reproducible |
| T-03-13-03 | T | Docker runtime config for tests | mitigate | Keep the change scoped to dev/test enablement in the existing `fastapi-app` service and avoid introducing a second divergent backend runtime during gap closure |

</threat_model>

<verification>
1. Rebuild the backend container with `docker compose up -d --build fastapi-app`.
2. Run `docker compose exec -T fastapi-app sh -lc "cd /app && python -m pytest tests/unit/test_students_academic_summary.py -q"`.
3. Run `docker compose exec -T fastapi-app sh -lc "cd /app && python -m pytest tests/integration/test_students_available_courses.py -q"`.
4. Run `docker compose exec -T fastapi-app sh -lc "cd /app && python -m pytest tests/unit/test_grades_cra.py -q"`.
5. Run a doc-content assertion against `.planning/phases/03-business-feature-slices/03-VALIDATION.md` confirming it documents `docker compose exec -T fastapi-app` as the supported Docker workflow.
6. If `cd backend && pytest ...` remains in the file, confirm the same assertion proves it is labeled optional/local-only rather than presented as the default UAT path.
</verification>

<success_criteria>
- `fastapi-app` can execute the documented Phase 03 pytest regressions in Docker.
- Missing-package and missing-test-tree failures are eliminated from the UAT path.
- Phase 03 validation guidance matches the actual supported verification container workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-feature-slices/03-13-SUMMARY.md`
</output>
