---
phase: 03-business-feature-slices
plan: 12
type: execute
wave: 5
depends_on: ["03-10"]
files_modified:
  - backend/Dockerfile
  - docker-compose.yml
  - backend/scripts/verify_enrollment_lock_gap.py
autonomous: true
gap_closure: true
requirements:
  - ENROLL-06
must_haves:
  truths:
    - "A rebuilt `fastapi-app` container sees Alembic revision `009a` from the repo instead of staying pinned to a stale baked-in migration tree."
    - "Running `alembic upgrade head` in the live Docker stack advances the database to the same Alembic head visible in the container."
    - "The enrollment lock verifier fails fast with explicit migration/runtime-sync guidance before any D-11 lock call hits PostgreSQL in a stale state."
  artifacts:
    - path: "backend/Dockerfile"
      provides: "FastAPI image layout that remains compatible with runtime-mounted Alembic assets"
      contains: "COPY alembic/ ./alembic/"
    - path: "docker-compose.yml"
      provides: "Development runtime mounts that keep `/app/alembic` and `/app/alembic.ini` synchronized with the repo"
      contains: "./backend/alembic:/app/alembic"
    - path: "backend/scripts/verify_enrollment_lock_gap.py"
      provides: "Preflighted PostgreSQL verifier for confirm -> lock persistence"
      contains: "alembic"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/alembic/versions/009_add_locked_status_to_enrollments.py"
      via: "bind-mounted `/app/alembic` tree in `fastapi-app`"
      pattern: "backend/alembic:/app/alembic|009_add_locked_status_to_enrollments"
    - from: "backend/scripts/verify_enrollment_lock_gap.py"
      to: "backend/src/features/enrollment/services.py"
      via: "preflight then `confirm_enrollment`/`lock_enrollment` execution"
      pattern: "confirm_enrollment|lock_enrollment|alembic"
    - from: "backend/scripts/verify_enrollment_lock_gap.py"
      to: "alembic_version"
      via: "head/current runtime check before exercising the lock flow"
      pattern: "current|head|009a"
---

<objective>
Close the Phase 03 UAT blocker caused by migration/runtime drift: make the live `fastapi-app` container stay synchronized with the repo Alembic tree, then harden the lock verifier so stale container/database state is diagnosed explicitly before the D-11 lock flow runs.

Purpose: `03-10` added the correct migration and PostgreSQL verifier, but UAT proved the dev stack can still run an older container whose `/app/alembic` tree stops at `008a`. This plan fixes the runtime-sync path, not the enrollment business logic.
Output: Updated Docker runtime sync for Alembic assets plus a preflighted enrollment-lock verifier.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-business-feature-slices/03-UAT.md
@.planning/debug/phase-03-enrollment-lock-gap.md
@.planning/phases/03-business-feature-slices/03-04-SUMMARY.md
@.planning/phases/03-business-feature-slices/03-10-SUMMARY.md
@backend/Dockerfile
@docker-compose.yml
@backend/alembic.ini
@backend/alembic/versions/009_add_locked_status_to_enrollments.py
@backend/scripts/verify_enrollment_lock_gap.py

<interfaces>
From `backend/Dockerfile` today:
```dockerfile
COPY alembic.ini ./
COPY alembic/ ./alembic/
COPY src/ ./src/
COPY scripts/ ./scripts/
```

From `docker-compose.yml` today:
```yaml
fastapi-app:
  volumes:
    - ./backend/src:/app/src
    - ./backend/scripts:/app/scripts
```

From `backend/scripts/verify_enrollment_lock_gap.py` today:
```python
async def main() -> None:
    enrollment_id, student_id, course_id = await seed_runtime_entities()
    await verify_confirm_and_lock(enrollment_id, student_id)
    await verify_persisted_state(enrollment_id, course_id)
    await verify_drop_rejected(enrollment_id, course_id, student_id)
```

From `backend/src/features/enrollment/services.py` via `03-10-PLAN.md` context:
```python
async def lock_enrollment(...):
    enrollment.status = "locked"
```
</interfaces>

## Gap Source
- `03-UAT.md` diagnoses a stale `fastapi-app` image/container whose Alembic tree stops at `008a`, so PostgreSQL never sees migration `009a`.
- `03-10-SUMMARY.md` already recorded the underlying symptom: the verifier only passed after manually copying the migration into the running container.
- Per D-11 and D-12, keep the enrollment lifecycle logic unchanged; fix the runtime schema-sync path instead.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep Alembic assets synchronized in the dev Docker runtime</name>
  <files>backend/Dockerfile, docker-compose.yml</files>
  <action>
    Update the FastAPI Docker/runtime setup so new Alembic revisions are visible inside `fastapi-app` without relying on a stale baked-in image.

    Implement the smallest runtime-sync fix for the diagnosed blocker:
    - In `docker-compose.yml`, add bind mounts for `./backend/alembic` -> `/app/alembic` and `./backend/alembic.ini` -> `/app/alembic.ini` alongside the existing `src/` and `scripts/` mounts.
    - Keep the image build copying `alembic/` and `alembic.ini` so clean builds still work, but make the live dev/UAT container follow the repo copy at runtime.
    - Preserve the current FastAPI reload workflow and existing env/network/healthcheck behavior.
    - Do not broaden this task into pytest/test mounts; the Phase 03 container-verification gap is handled separately in `03-13`.
    - In task notes/comments, explicitly call out that this change closes the `009a` drift diagnosed in `03-UAT.md` and preserves D-11 behavior by fixing environment sync rather than mutating enrollment logic.
  </action>
  <verify>
    <automated>docker compose up -d --build fastapi-app &amp;&amp; docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; python -c \"from pathlib import Path; assert Path('alembic/versions/009_add_locked_status_to_enrollments.py').exists()\" &amp;&amp; alembic heads"</automated>
  </verify>
  <done>
    - `fastapi-app` sees the repo's `009a` migration at runtime.
    - Alembic assets no longer depend on an old image rebuild history to stay current.
    - The change stays scoped to migration/runtime synchronization only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fail the enrollment-lock verifier early when runtime/head state is stale</name>
  <files>backend/scripts/verify_enrollment_lock_gap.py</files>
  <action>
    Strengthen `backend/scripts/verify_enrollment_lock_gap.py` so it diagnoses the exact stale-runtime problem before attempting the confirm -> lock flow.

    Add a preflight section that:
    - Checks the runtime Alembic script tree/head visible from `/app/alembic` and confirms revision `009a` is available.
    - Checks the live database revision state before running the enrollment workflow; if `current != head` or `009a` is missing, exit non-zero with a clear message instructing the operator to rebuild/recreate `fastapi-app` and run `alembic upgrade head`.
    - Make the stale-state remediation text explicit enough to assert in automation: it must mention both the rebuild/recreate step and `alembic upgrade head`, and it must happen before any `confirm_enrollment()` / `lock_enrollment()` work begins.
    - Only after a healthy preflight, run the existing `confirm_enrollment()` -> `lock_enrollment()` -> persisted-state assertions.
    - Preserve the current D-11/D-12 behavioral proof: successful lock persistence plus draft-only drop rejection after lock.
    - Keep the script narrowly focused on this gap; do not turn it into a general migration utility.
  </action>
  <verify>
    <automated>docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; alembic upgrade head &amp;&amp; python -m scripts.verify_enrollment_lock_gap"</automated>
    <automated>docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; mv alembic/versions/009_add_locked_status_to_enrollments.py alembic/versions/009_add_locked_status_to_enrollments.py.bak &amp;&amp; (python -m scripts.verify_enrollment_lock_gap &gt; /tmp/enrollment-lock-gap-negative.log 2&gt;&amp;1; test $? -ne 0) &amp;&amp; python -c \"from pathlib import Path; text = Path('/tmp/enrollment-lock-gap-negative.log').read_text(); assert 'rebuild/recreate' in text.lower(); assert 'alembic upgrade head' in text.lower(); assert 'confirm succeeded' not in text.lower(); assert 'lock persisted status' not in text.lower()\" ; status=$?; mv alembic/versions/009_add_locked_status_to_enrollments.py.bak alembic/versions/009_add_locked_status_to_enrollments.py; exit $status"</automated>
  </verify>
  <done>
    - The verifier emits explicit remediation guidance when the runtime/container schema state is stale.
    - Automated verification proves the negative path exits non-zero and prints remediation guidance before the lock flow runs.
    - On a synced stack, the verifier still proves confirm -> lock persistence and post-lock drop rejection.
    - The script remains focused on ENROLL-06 runtime proof per D-11/D-12.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repo Alembic tree → live `fastapi-app` container | New migration files must cross from the host workspace into the running verification/runtime container |
| Verifier script → live PostgreSQL schema | Runtime proof must detect stale migration state before exercising enrollment writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-12-01 | D | `fastapi-app` migration/runtime sync | mitigate | Bind-mount `backend/alembic` and `backend/alembic.ini` into `/app` so live containers cannot silently stay on a stale migration tree after repo changes |
| T-03-12-02 | T | `backend/scripts/verify_enrollment_lock_gap.py` | mitigate | Add a preflight that checks visible Alembic head/current state and fails with explicit remediation instructions before running the D-11 lock flow |
| T-03-12-03 | R | Phase 03 UAT evidence for ENROLL-06 | mitigate | Require `alembic upgrade head` plus the verifier command as the automated proof path so the summary captures synchronized runtime evidence, not a manual container patch |

</threat_model>

<verification>
1. Run `docker compose up -d --build fastapi-app` after the config change.
2. Run `docker compose exec -T fastapi-app sh -lc "cd /app && alembic heads && alembic current"` and confirm the container sees `009a` and the database can be advanced to that same head.
3. Run `docker compose exec -T fastapi-app sh -lc "cd /app && alembic upgrade head && python -m scripts.verify_enrollment_lock_gap"`.
4. Simulate a stale runtime by temporarily hiding `alembic/versions/009_add_locked_status_to_enrollments.py`, then rerun `python -m scripts.verify_enrollment_lock_gap` and confirm it exits non-zero with remediation guidance mentioning rebuild/recreate + `alembic upgrade head` before any lock-flow success text appears.
5. Restore the migration file and confirm the verifier passes again on the synced stack.
</verification>

<success_criteria>
- The Phase 03 Docker runtime no longer hides new Alembic revisions from `fastapi-app`.
- ENROLL-06 verification is blocked only by real schema issues, not by stale container filesystem state.
- The runtime verifier gives explicit, actionable failure messages for migration drift and still proves the locked-state behavior on success.
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-feature-slices/03-12-SUMMARY.md`
</output>
