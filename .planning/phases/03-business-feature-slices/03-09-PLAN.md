---
phase: 03-business-feature-slices
plan: 09
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - backend/src/features/students/controllers.py
  - backend/tests/integration/test_students_available_courses.py
  - docs/api.md
autonomous: true
gap_closure: true
requirements:
  - STU-07
must_haves:
  truths:
    - "GET /api/v1/students/{id}/available-courses returns HTTP 200 instead of ResponseValidationError."
    - "The endpoint response body is a raw JSON array of AvailableCourseItem objects, not an object with a top-level data key."
    - "The list still represents only courses whose prerequisites are already satisfied for that student per STU-07."
  artifacts:
    - path: "backend/src/features/students/controllers.py"
      provides: "Available-courses route contract aligned with its FastAPI response_model"
      contains: "return courses"
    - path: "backend/tests/integration/test_students_available_courses.py"
      provides: "Regression coverage for HTTP 200 + raw list response shape"
      contains: "available-courses"
    - path: "docs/api.md"
      provides: "Available-courses API example matching the implemented raw list response"
      contains: "GET /students/{id}/available-courses"
  key_links:
    - from: "backend/src/features/students/controllers.py"
      to: "backend/src/features/students/services.py"
      via: "student_service.get_available_courses(db, student_id)"
      pattern: "get_available_courses\(db, student_id\)"
    - from: "backend/tests/integration/test_students_available_courses.py"
      to: "backend/src/features/students/controllers.py"
      via: "GET /api/v1/students/{id}/available-courses assertion"
      pattern: "status_code == 200|isinstance\(body, list\)"
    - from: "docs/api.md"
      to: "backend/src/features/students/controllers.py"
      via: "documented response example matches actual response_model=list[AvailableCourseItem]"
      pattern: "\[\s*\{"
---

<objective>
Close the UAT blocker on `GET /students/{id}/available-courses` with the smallest correct fix: keep the existing `response_model=list[AvailableCourseItem]`, stop wrapping the service result in `{\"data\": ...}`, and align the docs and regression coverage to that raw-list contract.

Purpose: UAT Test 3 is blocked by a controller/docs contract mismatch, not by prerequisite filtering logic. Fixing only the response shape preserves the existing service behavior while restoring a stable MCP-facing endpoint.
Output: One regression test, one controller contract fix, and updated API docs for the intended raw-list response.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-business-feature-slices/03-UAT.md
@.planning/debug/available-courses-500-validation.md
@.planning/phases/03-business-feature-slices/03-02-SUMMARY.md
@docs/api.md
@backend/src/features/students/controllers.py
@backend/src/features/students/services.py
@backend/src/features/students/schemas.py
@backend/tests/conftest.py

<interfaces>
From `backend/src/features/students/schemas.py`:
```python
class AvailableCourseItem(BaseModel):
    id: UUID
    code: str
    name: str
    credits: int
    prerequisites_met: bool
    semester: int
```

From `backend/src/features/students/services.py`:
```python
async def get_available_courses(
    db: AsyncSession,
    student_id: UUID,
) -> list[AvailableCourseItem]
```

From `backend/src/features/students/controllers.py` (current mismatch to remove):
```python
@router.get("/{student_id}/available-courses", response_model=list[AvailableCourseItem])
async def get_available_courses(...) -> dict:
    courses = await student_service.get_available_courses(db, student_id)
    return {"data": [c.model_dump() for c in courses]}
```
</interfaces>

## Gap Source
- UAT Test 3 failed because FastAPI validated the route as `list[AvailableCourseItem]` while the handler returned a wrapped dict.
- `student_service.get_available_courses()` already returns the correct list shape; do not rewrite the filtering algorithm.
- `docs/api.md` currently documents the wrong wrapped shape and must be brought into sync with the controller per this gap fix.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a regression test for the raw-list contract</name>
  <files>backend/tests/integration/test_students_available_courses.py</files>
  <behavior>
    - Test: a valid authenticated request to `GET /api/v1/students/{student_id}/available-courses` returns HTTP 200.
    - Test: `response.json()` is a list, not an object containing `data`.
    - Test: the first returned item matches the `AvailableCourseItem` fields (`id`, `code`, `name`, `credits`, `prerequisites_met`, `semester`).
  </behavior>
  <action>
    Create `backend/tests/integration/test_students_available_courses.py` as a focused regression for the UAT blocker.

    Use the existing `client`, `seed_users`, and DB/session patterns from `backend/tests/conftest.py` and token/session issuance pattern from the existing auth integration tests. Keep this test narrow: it must prove route contract correctness, not re-test the prerequisite algorithm already owned by `student_service`.

    Stub or monkeypatch `student_service.get_available_courses` to return a known `list[AvailableCourseItem]` for the seeded student, then call the real FastAPI route. This keeps the test deterministic and isolates the controller contract bug. Preserve D-02 and D-05 by authenticating through the normal route path and keeping the ownership check in place; do not bypass auth or route wiring.
  </action>
  <verify>
    <automated>cd backend && pytest tests/integration/test_students_available_courses.py -x</automated>
  </verify>
  <done>
    The new test fails against the pre-fix controller because the endpoint returns HTTP 500 / wrong shape, and it explicitly asserts the intended HTTP 200 raw-list contract for STU-07.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align controller and docs to the existing list response model</name>
  <files>backend/src/features/students/controllers.py, docs/api.md</files>
  <action>
    In `backend/src/features/students/controllers.py`, make the smallest correct fix for the diagnosed bug:
    - Keep `response_model=list[AvailableCourseItem]`.
    - Change the handler return annotation from `dict` to `list[AvailableCourseItem]`.
    - Return the service result directly (`courses`) instead of wrapping it in `{\"data\": ...}`.
    - Do NOT change `student_service.get_available_courses`; the UAT/debug evidence says the filtering logic is already the correct layer and already returns `list[AvailableCourseItem]`.
    - Do NOT change dual-auth or ownership behavior; preserve D-02 and D-05 exactly.

    In `docs/api.md`, update the `GET /students/{id}/available-courses` example so it documents a raw JSON array of course objects. Do not introduce a wrapper schema just to preserve the outdated docs — the smallest correct fix is to align docs to the implemented FastAPI contract already declared in the route.
  </action>
  <verify>
    <automated>cd backend && pytest tests/integration/test_students_available_courses.py -x</automated>
  </verify>
  <done>
    - `GET /api/v1/students/{id}/available-courses` returns HTTP 200 with a raw JSON array.
    - The controller no longer triggers FastAPI `ResponseValidationError` for this route.
    - `docs/api.md` matches the raw-list contract.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| client/service-token caller → available-courses route | Untrusted request hits a student-scoped endpoint that must preserve auth + ownership checks while changing response serialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-09-01 | D | `GET /students/{id}/available-courses` | mitigate | Remove the response-model mismatch that raises `ResponseValidationError` and add regression coverage asserting HTTP 200 + raw list shape |
| T-03-09-02 | E | `backend/src/features/students/controllers.py` | mitigate | Preserve `get_current_user_or_service` and `check_ownership(student_id, user)` flow per D-02 and D-05; limit the code change to response serialization only |

</threat_model>

<verification>
1. Run `cd backend && pytest tests/integration/test_students_available_courses.py -x`.
2. Confirm the test asserts `response.status_code == 200`.
3. Confirm the test asserts `isinstance(response.json(), list)` and that `"data"` is not the top-level response shape.
4. Confirm `docs/api.md` now shows a raw array example for `GET /students/{id}/available-courses`.
</verification>

<success_criteria>
- The UAT blocker for Test 3 is closed by a controller/docs contract fix only.
- STU-07 keeps its existing prerequisite-filtering service behavior.
- A regression test proves HTTP 200 and the intended raw-list response shape.
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-feature-slices/03-09-SUMMARY.md`
</output>
