---
phase: 03-business-feature-slices
plan: 10
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - backend/alembic/versions/009_add_locked_status_to_enrollments.py
  - backend/scripts/verify_enrollment_lock_gap.py
autonomous: true
gap_closure: true
requirements:
  - ENROLL-06
must_haves:
  truths:
    - "A confirmed enrollment can be locked without PostgreSQL raising ck_enrollments_status violations."
    - "After locking, the enrollment row remains persisted with status='locked' in the database."
    - "Draft-only actions such as drop-course remain blocked after lock per D-12."
  artifacts:
    - path: "backend/alembic/versions/009_add_locked_status_to_enrollments.py"
      provides: "Alembic migration that updates ck_enrollments_status to accept locked"
      contains: "ck_enrollments_status"
    - path: "backend/scripts/verify_enrollment_lock_gap.py"
      provides: "PostgreSQL-backed verification of confirm -> lock -> persisted locked status"
      contains: "lock_enrollment"
  key_links:
    - from: "backend/alembic/versions/009_add_locked_status_to_enrollments.py"
      to: "backend/src/features/enrollment/models.py"
      via: "check constraint definition for enrollments.status"
      pattern: "locked|ck_enrollments_status"
    - from: "backend/scripts/verify_enrollment_lock_gap.py"
      to: "backend/src/features/enrollment/services.py"
      via: "EnrollmentService.confirm_enrollment and lock_enrollment"
      pattern: "confirm_enrollment|lock_enrollment"
    - from: "backend/scripts/verify_enrollment_lock_gap.py"
      to: "enrollments table"
      via: "direct post-commit query of persisted status"
      pattern: "select\(Enrollment.status\)|status == \"locked\""
---

<objective>
Close the UAT blocker from Test 6 with the smallest correct fix: add the missing Alembic migration so PostgreSQL accepts `enrollments.status='locked'`, then prove on the runtime PostgreSQL path that confirm -> lock succeeds and the locked state persists.

Purpose: The service and ORM already implement D-11 correctly; the failure is schema drift in migrated databases. Fixing the database constraint, not the business logic, restores the enrollment lock flow without widening scope.
Output: One Alembic migration and one repeatable PostgreSQL-backed verification script for the enrollment lock gap.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-business-feature-slices/03-UAT.md
@.planning/debug/enrollment-lock-checkviolation.md
@.planning/phases/03-business-feature-slices/03-04-SUMMARY.md
@backend/src/features/enrollment/models.py
@backend/src/features/enrollment/services.py
@backend/alembic/versions/004_create_enrollment_tables.py
@backend/alembic/versions/008_add_notes_to_documents.py
@backend/tests/conftest.py
@docker-compose.yml

<interfaces>
From `backend/src/features/enrollment/models.py`:
```python
CheckConstraint(
    "status IN ('draft', 'confirmed', 'cancelled', 'locked')",
    name="ck_enrollments_status",
)
```

From `backend/alembic/versions/004_create_enrollment_tables.py`:
```python
sa.CheckConstraint(
    "status IN ('draft', 'confirmed', 'cancelled')",
    name="ck_enrollments_status",
)
```

From `backend/src/features/enrollment/services.py`:
```python
async def confirm_enrollment(...):
    enrollment.status = "confirmed"

async def lock_enrollment(...):
    enrollment.status = "locked"
    for ec in enrollment.enrollment_courses:
        if ec.status != "dropped":
            ec.status = "locked"
```
</interfaces>

## Gap Source
- UAT Test 6 failed because PostgreSQL still enforces the old `ck_enrollments_status` constraint from migration `004a`.
- `03-04-SUMMARY.md` already documented that a follow-up DB migration was still needed after the model-level auto-fix.
- The smallest correct fix is to sync the migrated schema to the already-shipped D-11 lifecycle instead of changing service behavior.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing Alembic constraint migration for locked enrollments</name>
  <files>backend/alembic/versions/009_add_locked_status_to_enrollments.py</files>
  <action>
    Create `backend/alembic/versions/009_add_locked_status_to_enrollments.py` as the next migration after `008a`, using revision `009a` and `down_revision = "008a"`.

    Implement the smallest schema-only repair for the diagnosed gap:
    - In `upgrade()`, drop `ck_enrollments_status` on `enrollments` and recreate it so the database allows `('draft', 'confirmed', 'cancelled', 'locked')`, matching the existing ORM model and D-11.
    - In `downgrade()`, reverse the change back to the original three-value constraint.
    - Keep the migration scoped to `enrollments.status` only. Do not modify service code, route code, or unrelated enrollment constraints.
    - Add a short module docstring explaining that this closes the schema drift called out by UAT Test 6 / debug diagnosis.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; alembic check</automated>
  </verify>
  <done>
    - A new migration exists with revision `009a`.
    - `ck_enrollments_status` is explicitly altered to include `locked` in upgrade and removed again in downgrade.
    - The change is limited to the diagnosed enrollment lock schema mismatch per D-11.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a PostgreSQL-backed verification script for confirm -> lock persistence</name>
  <files>backend/scripts/verify_enrollment_lock_gap.py</files>
  <behavior>
    - Test 1: after applying migration `009a`, confirming a draft enrollment succeeds and creates the confirmed state needed for locking.
    - Test 2: locking that confirmed enrollment succeeds and a direct database read after commit returns `enrollments.status == 'locked'`.
    - Test 3: after lock, attempting `drop_course` raises the existing draft-only conflict per D-12 instead of allowing mutation.
  </behavior>
  <action>
    Create `backend/scripts/verify_enrollment_lock_gap.py` as a focused runtime verifier for this gap.

    The script must run inside `fastapi-app` against PostgreSQL and should:
    - Open a real async DB session using the project runtime configuration (not the SQLite pytest fixture).
    - Seed only the minimal entities needed for a deterministic enrollment flow: one student, one prerequisite-free course, one active enrollment period, and the required enrollment/enrollment_course records if not using `create_enrollment()`.
    - Exercise the real enrollment service path by running `confirm_enrollment()` and `lock_enrollment()` from `backend/src/features/enrollment/services.py`.
    - Commit, then query the database again to prove the persisted `enrollments.status` is `locked` and the non-dropped `enrollment_courses` rows are also `locked`.
    - Attempt `drop_course()` after lock and assert the operation is rejected with the existing draft-only conflict behavior per D-12.
    - Exit non-zero on any failed assertion and print a short PASS summary on success so `/gsd-execute-phase` can treat it as automated evidence.

    Keep this script narrowly scoped to Test 6. Do not turn it into a generic enrollment test harness.
  </action>
  <verify>
    <automated>docker compose exec -T fastapi-app sh -lc "cd /app &amp;&amp; alembic upgrade head &amp;&amp; python -m scripts.verify_enrollment_lock_gap"</automated>
  </verify>
  <done>
    - Running the verification script after `alembic upgrade head` proves confirm -> lock succeeds against PostgreSQL.
    - The script directly verifies the database persisted `status='locked'`.
    - The script proves draft-only drop remains blocked after lock per D-12.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| FastAPI enrollment service → PostgreSQL constraint enforcement | Application code writes lifecycle status values that the database must accept and persist correctly |
| Runtime verification script → Dockerized PostgreSQL | Automated proof must exercise the real migrated schema, not the SQLite test harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-10-01 | D | `POST /enrollments/{id}/lock` / `ck_enrollments_status` | mitigate | Add migration `009a` that explicitly recreates `ck_enrollments_status` with `locked`, eliminating the runtime 500 caused by schema drift |
| T-03-10-02 | T | `backend/scripts/verify_enrollment_lock_gap.py` | mitigate | Verify through the real service methods and a direct post-commit DB query so the fix proves persisted state, not just in-memory status changes |

</threat_model>

<verification>
1. Run `cd backend && alembic check` to confirm the migration chain is recognized cleanly after adding `009a`.
2. Run `docker compose exec -T fastapi-app sh -lc "cd /app && alembic upgrade head && python -m scripts.verify_enrollment_lock_gap"`.
3. Confirm the script output shows confirm succeeded, lock succeeded, and a direct database read observed `status='locked'`.
4. Confirm the script also proves `drop_course` is rejected after lock per D-12.
</verification>

<success_criteria>
- UAT Test 6's root cause is fixed by a schema migration, not by changing already-correct enrollment business logic.
- The runtime PostgreSQL schema accepts `enrollments.status='locked'` after migration `009a`.
- Automated verification proves confirm -> lock succeeds and the database persists the locked state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-feature-slices/03-10-SUMMARY.md`
</output>
