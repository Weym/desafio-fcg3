---
phase: 03-business-feature-slices
plan: 11
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - backend/tests/unit/test_students_academic_summary.py
  - backend/src/features/students/services.py
autonomous: true
gap_closure: true
requirements:
  - STU-06
must_haves:
  truths:
    - "Academic summary returns the upcoming appointment timestamp from the scheduled slot, not from appointment creation time."
    - "When a student has multiple scheduled appointments, academic summary surfaces the earliest slot by slot date/start_time ordering."
    - "When no scheduled future slot exists, academic summary still returns next_appointment=null."
  artifacts:
    - path: "backend/tests/unit/test_students_academic_summary.py"
      provides: "Focused regression coverage for slot-based next_appointment semantics"
      contains: "get_academic_summary"
    - path: "backend/src/features/students/services.py"
      provides: "STU-06 academic summary aggregation with slot-based next_appointment"
      contains: "next_appointment"
  key_links:
    - from: "backend/src/features/students/services.py"
      to: "backend/src/features/scheduling/models.py"
      via: "SchedulingSlot.date + SchedulingSlot.start_time combined into returned datetime"
      pattern: "SchedulingSlot\.date|SchedulingSlot\.start_time"
    - from: "backend/tests/unit/test_students_academic_summary.py"
      to: "backend/src/features/students/services.py"
      via: "direct service regression exercising get_academic_summary"
      pattern: "student_service\.get_academic_summary|get_academic_summary"
---

<objective>
Close the remaining Phase 03 verification gap for STU-06 with the smallest correct fix: make academic summary return a slot-based `next_appointment` datetime and lock that behavior with automated regression coverage.

Purpose: Verification already proved the endpoint finds the right slot candidate but returns the wrong field. Fixing only that semantic mismatch restores the user-visible academic summary contract without expanding scope.
Output: One focused regression test file and one narrow service-layer update in `students/services.py`.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-business-feature-slices/03-VERIFICATION.md
@.planning/phases/03-business-feature-slices/03-UAT.md
@.planning/phases/03-business-feature-slices/03-02-PLAN.md
@.planning/phases/03-business-feature-slices/03-02-SUMMARY.md
@backend/src/features/students/services.py
@backend/src/features/students/schemas.py
@backend/src/features/students/controllers.py
@backend/src/features/scheduling/models.py
@backend/tests/conftest.py
@backend/tests/integration/test_students_available_courses.py

<interfaces>
From `backend/src/features/students/schemas.py`:
```python
class AcademicSummaryResponse(BaseModel):
    student_id: UUID
    name: str
    semester: int
    completed_courses: int
    total_courses: int
    gpa: float
    status: str
    pending_documents: int
    next_appointment: datetime | None
```

From `backend/src/features/scheduling/models.py`:
```python
class SchedulingSlot(Base):
    date: Mapped[date]
    start_time: Mapped[time]
    end_time: Mapped[time]

class Appointment(Base):
    student_id: Mapped[uuid.UUID]
    slot_id: Mapped[uuid.UUID]
    status: Mapped[str]
    created_at: Mapped[datetime]
```

From `backend/src/features/students/services.py` today:
```python
next_appt_result = await db.execute(
    select(Appointment)
    .join(SchedulingSlot, Appointment.slot_id == SchedulingSlot.id)
    ...
    .order_by(SchedulingSlot.date.asc(), SchedulingSlot.start_time.asc())
    .limit(1)
)
next_appointment_row = next_appt_result.scalar_one_or_none()
next_appointment = next_appointment_row.created_at if next_appointment_row else None
```
</interfaces>

## Gap Source
- `03-VERIFICATION.md` marks STU-06 partial because the query orders by slot timing but returns `Appointment.created_at`.
- The missing work is explicitly limited to: return a slot-based timestamp and add automated coverage.
- Preserve the existing endpoint shape and auth/ownership behavior from D-05/D-06 by keeping this fix in the students service + test layer only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused regression coverage for slot-based next_appointment</name>
  <files>backend/tests/unit/test_students_academic_summary.py</files>
  <action>
    Create a new async unit test module that exercises `student_service.get_academic_summary()` directly against the existing SQLite test session.

    Cover the exact diagnosed semantics, no more:
    - Seed one student plus the minimum related rows needed for academic summary.
    - Seed at least two `SchedulingSlot` + `Appointment` pairs for that student where the earlier slot has a deliberately later `Appointment.created_at`, proving the service must use slot timing rather than creation timing.
    - Assert `next_appointment` equals the earliest slot's combined `date + start_time`, not either appointment row's `created_at`.
    - Add a second assertion path for `next_appointment is None` when the student has no scheduled future appointment rows.

    Follow the existing backend pytest pattern from `backend/tests/integration/test_students_available_courses.py`: focused fixture usage, no broad end-to-end harness, and deterministic values.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/unit/test_students_academic_summary.py --collect-only -q</automated>
  </verify>
  <done>
    - A dedicated regression test exists for STU-06 slot semantics.
    - The test file encodes the slot-vs-created-at regression and the null case without requiring a passing service implementation yet.
    - The test also covers the null case when there is no upcoming scheduled slot.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return slot-based datetime from academic summary with the smallest service-only fix</name>
  <files>backend/src/features/students/services.py</files>
  <behavior>
    - Test 1: if the earliest slot is tomorrow at 08:00 but its appointment row was created later than another appointment, `next_appointment` still returns tomorrow 08:00.
    - Test 2: if there is no qualifying scheduled appointment, `next_appointment` is `None`.
  </behavior>
  <action>
    Update `StudentService.get_academic_summary()` so STU-06 returns the scheduled slot datetime instead of `Appointment.created_at`.

    Keep the fix as narrow as possible:
    - Preserve the existing student summary schema and controller contract.
    - Preserve current ordering/filtering semantics unless required to build the returned datetime.
    - Select the slot fields needed for the winning appointment row and combine `SchedulingSlot.date` + `SchedulingSlot.start_time` into the returned `datetime` for `AcademicSummaryResponse.next_appointment`.
    - Use a timezone-aware datetime if needed to stay compatible with the existing schema/runtime expectations.
    - Do not alter unrelated CRA, pending-documents, auth, or ownership logic; this must remain a service-layer STU-06 repair only, preserving D-05/D-06 behavior already enforced in the controller.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/unit/test_students_academic_summary.py -q</automated>
  </verify>
  <done>
    - `next_appointment` is derived from the earliest scheduled slot's date/time.
    - The regression test passes and proves the result is not sourced from `Appointment.created_at`.
    - The code change is limited to the diagnosed STU-06 semantic mismatch.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Students service → scheduling data | Academic summary crosses from student-facing aggregation into appointment slot timing data that must reflect the real scheduled time |
| Regression test → service behavior | Automated proof must detect field-source regressions in `next_appointment` semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-11-01 | T | `backend/src/features/students/services.py` `next_appointment` mapping | mitigate | Return a datetime built from `SchedulingSlot.date` + `SchedulingSlot.start_time` instead of `Appointment.created_at`, matching the actual scheduled slot semantics |
| T-03-11-02 | D | `backend/tests/unit/test_students_academic_summary.py` | mitigate | Add a deterministic regression where slot ordering conflicts with appointment creation time so future semantic drift fails fast in CI |

</threat_model>

<verification>
1. Run `cd backend && pytest tests/unit/test_students_academic_summary.py -q`.
2. Confirm the passing test proves `next_appointment` equals the earliest slot datetime rather than an appointment creation timestamp.
3. Confirm the same test file also verifies the `None` case when no future scheduled slot exists.
</verification>

<success_criteria>
- STU-06 is fully closed in the students academic summary path.
- Automated verification exists specifically for slot-based `next_appointment` behavior.
- The fix stays scoped to the smallest correct service/test change.
</success_criteria>

<output>
After completion, create `.planning/phases/03-business-feature-slices/03-11-SUMMARY.md`
</output>
