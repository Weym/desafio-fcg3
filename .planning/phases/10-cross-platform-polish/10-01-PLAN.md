---
phase: 10-cross-platform-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/core/theme/app_theme.dart
  - mobile/lib/core/theme/app_colors.dart
  - mobile/lib/core/theme/app_spacing.dart
  - mobile/lib/core/responsive/breakpoints.dart
  - mobile/lib/main.dart
  - mobile/pubspec.yaml
autonomous: true
requirements:
  - UI-NFR-02
  - UI-NFR-04

must_haves:
  truths:
    - "App supports light and dark themes via Material 3 ColorScheme.fromSeed"
    - "Theme mode toggle persists choice in SharedPreferences"
    - "Default theme follows system OS preference"
    - "Spacing tokens (xs=4, sm=8, md=16, lg=24, xl=32) are centralized constants"
    - "Breakpoint constants defined for phone (<600dp), tablet (600-1023dp), desktop (>=1024dp)"
    - "Desktop headings scale 20% larger than standard Material 3 sizes"
  artifacts:
    - path: "mobile/lib/core/theme/app_spacing.dart"
      provides: "Spacing token constants"
      contains: "AppSpacing"
    - path: "mobile/lib/core/responsive/breakpoints.dart"
      provides: "Breakpoint constants and helpers"
      contains: "AppBreakpoints"
    - path: "mobile/lib/core/theme/app_theme.dart"
      provides: "Light and dark themes with responsive typography"
      exports: ["light", "dark"]
  key_links:
    - from: "mobile/lib/main.dart"
      to: "mobile/lib/core/theme/app_theme.dart"
      via: "ThemeData dark theme + ThemeMode"
      pattern: "darkTheme.*AppTheme"
---

<objective>
Create the cross-platform design foundation: dark mode theming, spacing tokens, breakpoint constants, and responsive typography.

Purpose: All subsequent plans depend on these foundational constants and theme definitions. Without centralized breakpoints, spacing, and dark mode, responsive layouts and polished UX states cannot be implemented consistently.
Output: 4 Dart files (app_theme.dart modified, app_spacing.dart new, breakpoints.dart new, app_colors.dart reviewed), pubspec.yaml updated with shared_preferences.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-platform-polish/10-CONTEXT.md
@.planning/phases/10-cross-platform-polish/10-UI-SPEC.md
@mobile/lib/core/theme/app_theme.dart
@mobile/lib/core/theme/app_colors.dart
@mobile/lib/main.dart
@mobile/pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spacing tokens and breakpoint constants</name>
  <files>mobile/lib/core/theme/app_spacing.dart, mobile/lib/core/responsive/breakpoints.dart</files>
  <read_first>mobile/lib/core/theme/app_theme.dart, mobile/lib/core/theme/app_colors.dart</read_first>
  <action>
Create `mobile/lib/core/responsive/` directory (new).

Create `mobile/lib/core/responsive/breakpoints.dart`:
```dart
class AppBreakpoints {
  static const double phone = 600;    // < 600dp = phone
  static const double tablet = 1024;  // 600-1023dp = tablet, >= 1024dp = desktop
  
  static bool isPhone(double width) => width < phone;
  static bool isTablet(double width) => width >= phone && width < tablet;
  static bool isDesktop(double width) => width >= tablet;
}
```

Create `mobile/lib/core/theme/app_spacing.dart`:
```dart
class AppSpacing {
  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
  static const double xl = 32;
}
```

Both files are pure constants — no imports needed except flutter/material.dart if used for EdgeInsets helpers.
  </action>
  <acceptance_criteria>
    - File `mobile/lib/core/responsive/breakpoints.dart` exists and contains `class AppBreakpoints`
    - File `mobile/lib/core/responsive/breakpoints.dart` contains `static const double phone = 600`
    - File `mobile/lib/core/responsive/breakpoints.dart` contains `static const double tablet = 1024`
    - File `mobile/lib/core/responsive/breakpoints.dart` contains `static bool isPhone`
    - File `mobile/lib/core/responsive/breakpoints.dart` contains `static bool isTablet`
    - File `mobile/lib/core/responsive/breakpoints.dart` contains `static bool isDesktop`
    - File `mobile/lib/core/theme/app_spacing.dart` exists and contains `class AppSpacing`
    - File `mobile/lib/core/theme/app_spacing.dart` contains `static const double xs = 4`
    - File `mobile/lib/core/theme/app_spacing.dart` contains `static const double xl = 32`
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && grep -l "AppBreakpoints" lib/core/responsive/breakpoints.dart && grep -l "AppSpacing" lib/core/theme/app_spacing.dart</automated>
  </verify>
  <done>Both constant files exist with all required values matching D-02, D-23 from CONTEXT.md</done>
</task>

<task type="auto">
  <name>Task 2: Implement dark mode theme + responsive typography + SharedPreferences persistence</name>
  <files>mobile/lib/core/theme/app_theme.dart, mobile/lib/core/theme/app_colors.dart, mobile/lib/main.dart, mobile/pubspec.yaml</files>
  <read_first>mobile/lib/core/theme/app_theme.dart, mobile/lib/core/theme/app_colors.dart, mobile/lib/main.dart, mobile/pubspec.yaml, mobile/lib/core/responsive/breakpoints.dart</read_first>
  <action>
1. Add `shared_preferences: ^2.3.0` to `mobile/pubspec.yaml` under dependencies.

2. Modify `mobile/lib/core/theme/app_theme.dart` to:
   - Add a `static ThemeData get dark` getter using `ColorScheme.fromSeed(seedColor: AppColors.primary, brightness: Brightness.dark)` with same component themes (AppBar, InputDecoration, ElevatedButton, SnackBar).
   - Add a `static TextTheme responsiveTextTheme(TextTheme base, double screenWidth)` static method that scales headlineMedium and displaySmall by ~20% when `screenWidth >= 1024` (import breakpoints.dart), and increases body line height to 1.6 on desktop. Returns a modified TextTheme using `copyWith`.
   - Keep existing `light` getter unchanged but ensure it already uses `AppColors.primary` as seedColor (it does).

3. Review `mobile/lib/core/theme/app_colors.dart` — WCAG AA compliance. The existing colors use `Color(0xFF1565C0)` primary on white background which passes 4.5:1 contrast. The error red `0xFFD32F2F` on white also passes. No changes needed to color values. Add a comment documenting the contrast ratios were verified.

4. Create a `ThemeModeNotifier` Riverpod provider in a new file `mobile/lib/core/theme/theme_provider.dart`:
   - Uses `shared_preferences` to read/write key `theme_mode` (values: "light", "dark", "system")
   - Default is `ThemeMode.system` per D-18
   - Exposes `ThemeMode` state and `setThemeMode(ThemeMode)` method
   - Uses `@riverpod` codegen annotation (consistent with project pattern)

5. Modify `mobile/lib/main.dart`:
   - Import `theme_provider.dart`
   - Watch the theme mode provider
   - Set `themeMode:` on MaterialApp.router from the provider
   - Set `darkTheme: AppTheme.dark`
   - Keep existing `theme: AppTheme.light`

6. Run `dart run build_runner build --delete-conflicting-outputs` in mobile/ to generate `.g.dart` for the new provider.
  </action>
  <acceptance_criteria>
    - `mobile/pubspec.yaml` contains `shared_preferences:`
    - `mobile/lib/core/theme/app_theme.dart` contains `static ThemeData get dark`
    - `mobile/lib/core/theme/app_theme.dart` contains `Brightness.dark`
    - `mobile/lib/core/theme/app_theme.dart` contains `responsiveTextTheme`
    - `mobile/lib/core/theme/app_theme.dart` contains `1024` or `AppBreakpoints.tablet`
    - `mobile/lib/core/theme/theme_provider.dart` exists and contains `ThemeModeNotifier` or `themeModeProvider`
    - `mobile/lib/core/theme/theme_provider.dart` contains `shared_preferences`
    - `mobile/lib/core/theme/theme_provider.dart` contains `ThemeMode.system`
    - `mobile/lib/main.dart` contains `darkTheme`
    - `mobile/lib/main.dart` contains `themeMode`
    - `dart analyze lib/core/theme/` exits 0 (no errors)
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && dart analyze lib/core/theme/ && grep "darkTheme" lib/main.dart && grep "ThemeMode" lib/main.dart</automated>
  </verify>
  <done>Dark mode fully functional with persistent theme toggle; responsive typography scales headings 20% on desktop; SharedPreferences persists user's theme choice</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SharedPreferences storage | User theme preference stored locally — low sensitivity data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | SharedPreferences theme_mode key | accept | Low-value data (cosmetic preference only), no security impact if tampered |

</threat_model>

<verification>
- `cd mobile && flutter analyze` passes with 0 errors
- `mobile/lib/core/theme/app_theme.dart` exports both `light` and `dark` ThemeData
- `mobile/lib/core/responsive/breakpoints.dart` has phone/tablet/desktop helpers
- `mobile/lib/core/theme/app_spacing.dart` has xs through xl constants
- Theme provider compiles and defaults to ThemeMode.system
</verification>

<success_criteria>
Foundation artifacts exist and compile: dark theme, spacing tokens, breakpoints, responsive typography, theme persistence provider. All subsequent plans can import these constants without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-platform-polish/10-01-SUMMARY.md`
</output>
