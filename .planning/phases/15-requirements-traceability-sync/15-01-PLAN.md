---
phase: 15-requirements-traceability-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - UI-INFRA-01
  - UI-INFRA-03
  - RES-01
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - RES-06
  - RES-07
  - RES-08
  - RES-09
  - RES-10
  - RES-11
  - RES-12
  - RES-13
  - RES-14
  - HI-01
  - HI-02
  - HI-03
  - HI-04
  - HI-05
  - HI-06
  - HI-07
  - HI-08
  - HI-09
  - HI-10
  - HI-11
  - HI-12
  - HI-13
  - HI-14
  - HI-15
  - HI-16
must_haves:
  truths:
    - "All 47 requirements have [x] checkbox in REQUIREMENTS.md"
    - "RES-01 through RES-14 appear in traceability table mapped to Phase 13 with status Complete"
    - "HI-01 through HI-16 appear in traceability table mapped to Phase 14 with status Complete"
    - "Coverage header reads 47/47 requirements mapped"
    - "ROADMAP.md Phase 15 entry shows completion"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete traceability table with all 47 requirements"
      contains: "Coverage: 47/47 requirements mapped"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase 15 status"
      contains: "Phase 15"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase references in traceability table match roadmap phases"
      pattern: "Phase 13|Phase 14"
---

<objective>
Verify and confirm requirements traceability sync is complete — validate that all 47 requirements
have correct checkboxes, the traceability table includes RES-01..14 and HI-01..16 with proper
phase mappings, and coverage count is 47/47. Mark phase as done in ROADMAP.

Purpose: Close process debt from M2 audit so milestone audit can pass cleanly.
Output: Verified REQUIREMENTS.md (no changes needed — work done in audit commit 18c5a33) + updated ROADMAP.md with phase completion.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify traceability table completeness and fix any remaining gaps</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Verify all 4 success criteria for Phase 15 are satisfied in the current REQUIREMENTS.md:

1. **Checkboxes**: Confirm UI-INFRA-01 and UI-INFRA-03 both have `[x]` (not `[ ]`).
2. **RES requirements**: Confirm all 14 entries (RES-01 through RES-14) appear in both:
   - The "Resource Allocation (Phase 13)" section with `[x]` checkboxes
   - The "Traceability" table at bottom with Phase 13 mapping and "Complete" status
3. **HI requirements**: Confirm all 16 entries (HI-01 through HI-16) appear in both:
   - The "Human Intervention (Phase 14)" section with `[x]` checkboxes
   - The "Traceability" table at bottom with Phase 14 mapping and "Complete" status
4. **Coverage count**: Header must read `Coverage: 47/47 requirements mapped`

If any item is missing or incorrectly marked, fix it. Expected current state (from audit commit 18c5a33) is that all items are already correct.

Verification commands to run:
- Count `[x]` occurrences — should be 47 total requirement checkboxes
- Count traceability table rows — should be 47 data rows (excluding header)
- Search for any `[ ]` (unchecked) — should find zero
- Verify "RES-14" and "HI-16" both exist in traceability table (last items in each set)
  </action>
  <verify>
    <automated>cd ".planning" && (Select-String -Pattern "\[x\]" -Path "REQUIREMENTS.md").Count -eq 47 && (Select-String -Pattern "\[ \]" -Path "REQUIREMENTS.md").Count -eq 0 && (Select-String -Pattern "RES-14" -Path "REQUIREMENTS.md").Count -ge 2 && (Select-String -Pattern "HI-16" -Path "REQUIREMENTS.md").Count -ge 2 && (Select-String -Pattern "47/47" -Path "REQUIREMENTS.md").Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains exactly 47 `[x]` marked requirements
    - REQUIREMENTS.md contains zero `[ ]` unchecked requirements
    - REQUIREMENTS.md header contains "Coverage: 47/47 requirements mapped"
    - RES-01 through RES-14 each appear at least twice (section + traceability table)
    - HI-01 through HI-16 each appear at least twice (section + traceability table)
    - All traceability table entries show "Complete" status
  </acceptance_criteria>
  <done>All 47 requirements verified with correct checkboxes and traceability mappings. Zero unchecked items.</done>
</task>

<task type="auto">
  <name>Task 2: Mark Phase 15 complete in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
  </read_first>
  <action>
Update ROADMAP.md to reflect Phase 15 completion:

1. In the phases list at top, change:
   `- [ ] **Phase 15: Requirements Traceability Sync**` → `- [x] **Phase 15: Requirements Traceability Sync** — Fix checkboxes, add 30 untracked requirements (RES-01..14, HI-01..16) to traceability table, update coverage`
   Add completion date: `(completed {today's date in YYYY-MM-DD format})`

2. In the Phase 15 detail section, add:
   ```
   **Plans:** 1 plan
   
   Plans:
   - [x] 15-01-PLAN.md — Verify traceability completeness and mark phase done
   ```

3. In the Progress table at bottom, update Phase 15 row:
   `| 15. Requirements Traceability Sync | 1/1 | Complete | {today} |`

4. Update the header status line from `8/11 phases complete (73%)` to `9/11 phases complete (82%)`
  </action>
  <verify>
    <automated>Select-String -Pattern "\[x\].*Phase 15" -Path ".planning/ROADMAP.md" | Select-Object -First 1</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 15 checkbox is `[x]` in the phases list
    - ROADMAP.md Phase 15 has "Plans: 1 plan" and plan list entry
    - ROADMAP.md Progress table shows Phase 15 as "Complete" with date
    - ROADMAP.md header shows "9/11 phases complete (82%)"
  </acceptance_criteria>
  <done>ROADMAP.md reflects Phase 15 as complete with 1/1 plans, correct progress percentage, and completion date.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase modifies only planning documentation (.planning/ directory). No runtime code, no endpoints, no user-facing changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | N/A | Documentation only | accept | No code changes, no trust boundaries crossed. Phase modifies markdown files in .planning/ directory only. No security implications. |

</threat_model>

<verification>
1. Run: `(Select-String -Pattern "\[x\]" -Path ".planning/REQUIREMENTS.md").Count` → must equal 47
2. Run: `(Select-String -Pattern "\[ \]" -Path ".planning/REQUIREMENTS.md").Count` → must equal 0
3. Run: `Select-String -Pattern "47/47" -Path ".planning/REQUIREMENTS.md"` → must find match
4. Run: `Select-String -Pattern "\[x\].*Phase 15" -Path ".planning/ROADMAP.md"` → must find match
5. Run: `Select-String -Pattern "9/11 phases complete" -Path ".planning/ROADMAP.md"` → must find match
</verification>

<success_criteria>
- All 47 requirements marked [x] with no unchecked items remaining
- Traceability table has 47 rows, all status "Complete"
- RES-01..14 mapped to Phase 13, HI-01..16 mapped to Phase 14
- Coverage header: 47/47
- ROADMAP.md shows Phase 15 complete (9/11 phases, 82%)
- Milestone audit can pass — no traceability gaps
</success_criteria>

<output>
After completion, create `.planning/phases/15-requirements-traceability-sync/15-01-SUMMARY.md`
</output>
