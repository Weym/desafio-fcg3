---
phase: 05-ai-service
plan: 07
type: execute
wave: 5
depends_on: [05-01, 05-05, 05-06]
files_modified:
  - ai_service/main.py
  - ai_service/Dockerfile
  - docker-compose.yml
  - ai_service/tests/test_runtime_entrypoint.py
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05]
gap_closure: true

must_haves:
  truths:
    - "The checked-in langchain-service runtime boots the packaged AI service instead of failing on `ModuleNotFoundError: No module named 'ai_service'`"
    - "The live AI service serves `/health` on port 8001 in Docker after the runtime fix"
    - "Regression coverage fails if Docker or compose drift back to the broken flat-module startup path"
  artifacts:
    - path: "ai_service/main.py"
      provides: "Package-safe AI service runner that starts the FastAPI app from `ai_service.main:app`"
      min_lines: 120
    - path: "ai_service/Dockerfile"
      provides: "Container startup aligned to the packaged AI service entrypoint"
      min_lines: 12
    - path: "docker-compose.yml"
      provides: "langchain-service bind mount and command aligned with the package layout"
      contains: "python -m ai_service.main"
    - path: "ai_service/tests/test_runtime_entrypoint.py"
      provides: "Runtime regressions for package import, routes, Dockerfile, and compose alignment"
      min_lines: 40
  key_links:
    - from: "ai_service/Dockerfile"
      to: "ai_service/main.py"
      via: "python -m ai_service.main container command"
      pattern: "python -m ai_service\.main"
    - from: "docker-compose.yml"
      to: "ai_service/main.py"
      via: "langchain-service command"
      pattern: "python -m ai_service\.main"
    - from: "docker-compose.yml"
      to: "ai_service/main.py"
      via: "bind-mounted package path"
      pattern: "\./ai_service:/app/ai_service"
---

<objective>
Close the remaining Phase 05 runtime gap by aligning the AI-service package entrypoint, Docker image, and compose development wiring so the real FastAPI app boots in Docker.

Purpose: `05-VERIFICATION.md` now reports only AI-RUNTIME-01; Phase 05 cannot be accepted until `langchain-service` serves the live `/health` endpoint and real `/chat` requests again.

Output: A package-based AI runtime path plus focused regressions that lock the startup fix in place.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-service/05-CONTEXT.md
@.planning/phases/05-ai-service/05-RESEARCH.md
@.planning/phases/05-ai-service/05-VERIFICATION.md
@.planning/phases/05-ai-service/05-01-SUMMARY.md
@.planning/phases/05-ai-service/05-05-SUMMARY.md
@.planning/phases/05-ai-service/05-06-SUMMARY.md
@ai_service/main.py
@ai_service/Dockerfile
@docker-compose.yml

<interfaces>
From `ai_service/main.py`:
```python
app = FastAPI(title="AI Service", version="0.1.0", lifespan=lifespan)

@app.get("/health")
async def health_check(request: Request) -> dict[str, str]:
    ...

@app.post("/chat", response_model=ChatResponse)
async def chat(request: ChatRequest) -> ChatResponse:
    ...

if __name__ == "__main__":
    uvicorn.run("ai_service.main:app", host="0.0.0.0", port=8001, reload=True)
```

From `ai_service/Dockerfile`:
```dockerfile
WORKDIR /app
COPY ai_service ./ai_service
CMD ["uvicorn", "ai_service.main:app", "--host", "0.0.0.0", "--port", "8001", "--reload"]
```

From the current `docker-compose.yml` `langchain-service` section:
```yaml
langchain-service:
  build: ./ai_service
  volumes:
    - ./ai_service:/app
  command: uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused regressions for the AI runtime entrypoint gap</name>
  <files>ai_service/tests/test_runtime_entrypoint.py</files>
  <behavior>
    - Test 1: Importing `ai_service.main` succeeds and still exposes FastAPI routes for `/health` and `/chat`.
    - Test 2: `ai_service/Dockerfile` launches the packaged module path and does not reference the broken flat `main:app` entrypoint.
    - Test 3: The `langchain-service` compose section uses `python -m ai_service.main` and bind-mounts `./ai_service` at `/app/ai_service`, not `/app`.
  </behavior>
  <action>Create `ai_service/tests/test_runtime_entrypoint.py` as the focused regression suite for AI-RUNTIME-01. Make the tests inspect imported source and checked-in manifests only — no live container, no LLM calls, no MCP network calls. Assert that the module import keeps `/health` and `/chat` registered, that `ai_service/Dockerfile` points at the packaged entrypoint, and that the `langchain-service` compose section contains the package-aligned command/mount while explicitly rejecting `uvicorn main:app` and `./ai_service:/app`.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('ai_service/tests/test_runtime_entrypoint.py').read_text(encoding='utf-8'); assert 'test_import_preserves_health_and_chat_routes' in text; assert 'test_dockerfile_uses_package_entrypoint' in text; assert 'test_compose_uses_package_entrypoint_and_bind_mount' in text"</automated>
  </verify>
  <done>The repository contains a focused regression module that names the broken startup path and will fail if the AI runtime manifests drift back to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the AI-service package startup path without changing chat behavior</name>
  <files>ai_service/main.py, ai_service/Dockerfile, docker-compose.yml, ai_service/tests/test_runtime_entrypoint.py</files>
  <behavior>
    - Test 1: `python -m ai_service.main` is the shared runtime entrypoint for both the image and the compose service.
    - Test 2: The compose bind mount keeps the package importable in development by mounting `./ai_service` at `/app/ai_service`.
    - Test 3: Importing `ai_service.main` still exposes the same FastAPI app with `/health` and `/chat`, and does not alter the 05-06 persistence fix.
  </behavior>
  <action>Add an explicit package runner in `ai_service/main.py` (for example `main()`) that starts `uvicorn.run('ai_service.main:app', ...)` while preserving the existing FastAPI app object per D-05. Update `ai_service/Dockerfile` and the `langchain-service` section of `docker-compose.yml` to use the same package-based entrypoint (`python -m ai_service.main`) so the image and bind-mounted development runtime match. Mount the local AI package at `/app/ai_service` instead of `/app` so imports like `from ai_service.agent import invoke_agent` remain valid in Docker. Preserve D-01/D-02 tool wiring and D-08/D-09 chat lifecycle by limiting the fix to runtime/bootstrap files; do not revert the 05-06 `/chat` persistence behavior and do not switch back to flat-module `uvicorn main:app` startup.</action>
  <verify>
    <automated>python -m pytest ai_service/tests/test_runtime_entrypoint.py -q</automated>
  </verify>
  <done>The checked-in AI runtime is package-safe across source, Dockerfile, and compose: `langchain-service` can import `ai_service.*` in Docker, the new regression passes, and the runtime fix leaves the existing `/chat` behavior intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker/Compose → AI runtime | Container startup hands control to the Python entrypoint; a wrong command or mount prevents the service from booting |
| AI package import path → FastAPI app availability | Package import failure blocks `/health` and `/chat` before any request reaches the app |
| Runtime manifests → future verification | Drift between Dockerfile and compose can silently reintroduce the startup failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | D | `docker-compose.yml` langchain-service command | mitigate | Replace the broken flat-module `uvicorn main:app` command with `python -m ai_service.main` so the packaged FastAPI app can boot in Docker. |
| T-05-07-02 | D | `docker-compose.yml` bind mount | mitigate | Mount `./ai_service` at `/app/ai_service` so bind-mounted development matches the image's package layout and `ai_service.*` imports remain resolvable. |
| T-05-07-03 | T | `ai_service/Dockerfile`, `docker-compose.yml`, `ai_service/main.py` | mitigate | Add focused runtime regressions that assert the package entrypoint, preserved routes, and manifest alignment so future edits cannot silently drift back to the broken startup path. |

</threat_model>

<verification>
1. `python -m pytest ai_service/tests/test_runtime_entrypoint.py -q`
2. `python -c "from pathlib import Path; text=Path('docker-compose.yml').read_text(encoding='utf-8'); section=text.split('langchain-service:', 1)[1]; assert 'python -m ai_service.main' in section; assert './ai_service:/app/ai_service' in section; assert 'uvicorn main:app' not in section"`
3. `docker compose up -d postgres langchain-service && curl -sf http://localhost:8001/health`
</verification>

<success_criteria>
- `langchain-service` no longer fails with `ModuleNotFoundError: No module named 'ai_service'` in the containerized runtime.
- The live AI service responds successfully on `http://localhost:8001/health` after Docker startup.
- A focused regression suite exists and passes for the package entrypoint, preserved routes, and compose/Dockerfile alignment.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-service/05-07-SUMMARY.md`
</output>
