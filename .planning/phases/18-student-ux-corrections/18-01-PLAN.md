---
phase: 18-student-ux-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/client/screens/client_home_screen.dart
  - mobile/lib/shared/widgets/app_bar_actions.dart
  - mobile/lib/features/client/screens/client_shell.dart
  - mobile/lib/core/router/route_names.dart
autonomous: true
requirements:
  - STUX-01
  - STUX-02
  - STUX-03
  - STUX-04
must_haves:
  truths:
    - "Student taps 'Agendamentos' quick action and arrives at the agendamento detail (nearest appointment)"
    - "Student taps 'Solicitar documentos' quick action and arrives at documents screen with add drawer already open"
    - "'Conversar com Mentor' quick action is removed from the home screen"
    - "Support/Suporte is accessible from a header icon (not bottom nav or quick actions)"
  artifacts:
    - path: "mobile/lib/features/client/screens/client_home_screen.dart"
      provides: "Corrected quick actions (Agendamentos -> nearest, Docs -> open drawer, no Mentor)"
      contains: "showDocumentRequestSheet"
    - path: "mobile/lib/shared/widgets/app_bar_actions.dart"
      provides: "Support icon added to header actions bar"
      contains: "Icons.support_agent"
  key_links:
    - from: "client_home_screen.dart quick action Agendamentos"
      to: "nearest appointment detail drawer"
      via: "reads appointmentsProvider, navigates to detail"
      pattern: "showModalBottomSheet.*appointment"
    - from: "client_home_screen.dart quick action Docs"
      to: "documents screen with drawer open"
      via: "navigation with auto-open parameter"
      pattern: "showDocumentRequestSheet"
---

<objective>
Fix home screen quick actions and add support header icon for student view.

Purpose: Students currently have incorrect quick action destinations and cannot access support from the header.
Output: Corrected navigation targets, removed "Conversar com Mentor", added support icon to AppBar.
</objective>

<execution_context>
@./desafio-fcg3/.opencode/get-shit-done/workflows/execute-plan.md
@./desafio-fcg3/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@mobile/lib/features/client/screens/client_home_screen.dart
@mobile/lib/shared/widgets/app_bar_actions.dart
@mobile/lib/features/client/screens/client_shell.dart
@mobile/lib/core/router/route_names.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix quick actions and add support header icon</name>
  <files>
    mobile/lib/features/client/screens/client_home_screen.dart
    mobile/lib/shared/widgets/app_bar_actions.dart
  </files>
  <read_first>
    mobile/lib/features/client/screens/client_home_screen.dart
    mobile/lib/shared/widgets/app_bar_actions.dart
    mobile/lib/features/client/providers/appointment_provider.dart
    mobile/lib/features/client/screens/widgets/document_request_sheet.dart
    mobile/lib/core/router/route_names.dart
  </read_first>
  <action>
**In `client_home_screen.dart`:**

1. **STUX-01 — "Agendamentos" quick action:** Change the first quick action from "Solicitar documento" to "Agendamentos". Its icon should be `Icons.calendar_today_outlined`, color `colors.secondary`. On tap: read `appointmentsProvider`, find the nearest upcoming appointment (`appointments.where((a) => a.isUpcoming).toList()..sort((a,b) => ...)` by slotDate), then show a `showModalBottomSheet` with that appointment's details (reason, slotDate, slotStartTime, endTime, status). If no upcoming appointment, show a snackbar "Sem agendamentos próximos".

2. **STUX-02 — "Solicitar documentos" quick action:** Change the second quick action to "Solicitar documentos" with icon `Icons.description_outlined`, color `colors.primary`. On tap: navigate to `RoutePaths.clientDocuments` AND then immediately call `showDocumentRequestSheet(context, ref)` — use a `WidgetsBinding.instance.addPostFrameCallback` approach. Implement this by adding a query parameter `?openDrawer=true` to the route or by using a Riverpod StateProvider `documentAutoOpenDrawerProvider` that the documents screen reads on build. The StateProvider approach is simpler: create a `final documentAutoOpenDrawerProvider = StateProvider<bool>((ref) => false);` in `document_provider.dart`. Set it to `true` before navigating, and in `ClientDocumentsScreen.build()` check it and call `showDocumentRequestSheet` if true (then reset to false).

3. **STUX-03 — Remove "Conversar com Mentor":** Delete the quick action item with label 'Conversar com Mentor' from the `actions` list entirely. The list should now have exactly 3 items: "Agendamentos", "Solicitar documentos", "Notificações". Remove "Suporte" from quick actions too (it moves to header per STUX-04).

4. **STUX-04 — Support in header:** In `app_bar_actions.dart`, add a support icon button BEFORE the theme toggle. Icon: `Icons.support_agent_outlined`, color: `colors.primary`. On tap: `context.go(RoutePaths.clientSupport)`. Import `go_router` and `route_names.dart`. Wrap with a `Builder` or pass context properly. The widget is a `ConsumerWidget` so use `ref` for navigation context — but since `AppBarActions` doesn't receive a BuildContext for navigation directly, wrap the button tap in `Builder(builder: (ctx) => IconButton(..., onPressed: () => GoRouter.of(ctx).go(RoutePaths.clientSupport)))`. Actually simpler: the `build` method already has `context`, so just use `GoRouter.of(context).go(...)`.

**In `app_bar_actions.dart`:**
Add before the theme toggle IconButton:
```dart
IconButton(
  icon: Icon(Icons.support_agent_outlined, color: colors.primary),
  onPressed: () => GoRouter.of(context).go(RoutePaths.clientSupport),
  tooltip: 'Suporte',
),
```
Add imports: `import 'package:go_router/go_router.dart';` and `import '../../core/router/route_names.dart';`.

**Quick actions final list (3 items):**
- "Agendamentos" — Icons.calendar_today_outlined — colors.secondary — shows nearest appointment drawer
- "Solicitar documentos" — Icons.description_outlined — colors.primary — navigates to docs + opens add drawer
- "Notificações" — Icons.notifications_outlined — colors.tertiary — navigates to notifications
  </action>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "(error|warning)" | Select-Object -First 20</automated>
  </verify>
  <acceptance_criteria>
    - client_home_screen.dart does NOT contain 'Conversar com Mentor'
    - client_home_screen.dart does NOT contain 'Suporte' in the quick actions list
    - client_home_screen.dart contains 'Agendamentos' as a quick action label
    - client_home_screen.dart contains 'showModalBottomSheet' for appointment details
    - client_home_screen.dart contains 'showDocumentRequestSheet' or 'documentAutoOpenDrawerProvider'
    - app_bar_actions.dart contains 'Icons.support_agent_outlined'
    - app_bar_actions.dart contains 'import.*go_router'
    - app_bar_actions.dart contains 'clientSupport'
  </acceptance_criteria>
  <done>
    - "Agendamentos" quick action shows nearest appointment details in a drawer
    - "Solicitar documentos" quick action navigates to documents and opens the add drawer
    - "Conversar com Mentor" removed entirely
    - Support icon appears in the app bar header on all client screens
    - Quick actions grid has exactly 3 items
    - `flutter analyze` passes with no errors on modified files
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Avisos from bottom navigation (STUX-14)</name>
  <files>
    mobile/lib/features/client/screens/client_shell.dart
    mobile/lib/core/router/route_names.dart
  </files>
  <read_first>
    mobile/lib/features/client/screens/client_shell.dart
    mobile/lib/core/router/route_names.dart
    mobile/lib/core/router/app_router.dart
  </read_first>
  <action>
**STUX-14 — Notifications moved to header button, removed from bottom nav:**

The notifications/avisos screen is already accessible via a quick action on home and will now be accessible via the header. Remove it from the bottom navigation bar.

**In `client_shell.dart`:**

1. Remove the "Avisos" (notifications) item from `_destinations` list. The bottom nav should have: Início (0), Chat (1), Docs (2), Recursos (3). That's 4 items.

2. Update `_currentIndex` method to remove the notifications case:
```dart
int _currentIndex(BuildContext context) {
  final location = GoRouterState.of(context).matchedLocation;
  if (location.startsWith(RoutePaths.clientChat)) return 1;
  if (location.startsWith(RoutePaths.clientDocuments)) return 2;
  if (location.startsWith(RoutePaths.clientResources)) return 3;
  if (location.startsWith(RoutePaths.clientSupport)) return 3; // fallback
  return 0;
}
```

3. Update `_onTap` method accordingly:
```dart
void _onTap(BuildContext context, int index) {
  switch (index) {
    case 0: context.go(RoutePaths.clientHome);
    case 1: context.go(RoutePaths.clientChat);
    case 2: context.go(RoutePaths.clientDocuments);
    case 3: context.go(RoutePaths.clientResources);
  }
}
```

4. Remove the notifications `NavigationRailDestination` from `_railDestinations` too.

5. Add a notifications icon button to `app_bar_actions.dart` (in the same task or verify it was added in Task 1). Actually, per STUX-14, notifications should be accessible from a header button. Add an `IconButton` with `Icons.notifications_outlined` in `app_bar_actions.dart` that navigates to `RoutePaths.clientNotifications`. Place it AFTER the support icon and BEFORE the theme toggle.

**In `app_bar_actions.dart`:** Add notifications icon between support and theme toggle:
```dart
IconButton(
  icon: Icon(Icons.notifications_outlined, color: colors.primary),
  onPressed: () => GoRouter.of(context).go(RoutePaths.clientNotifications),
  tooltip: 'Notificações',
),
```
  </action>
  <verify>
    <automated>cd mobile && flutter analyze --no-pub 2>&1 | Select-String -Pattern "(error|warning)" | Select-Object -First 20</automated>
  </verify>
  <acceptance_criteria>
    - client_shell.dart `_destinations` list has exactly 4 items (no 'Avisos')
    - client_shell.dart `_onTap` method has cases 0-3 only (no case 4 or 5)
    - app_bar_actions.dart contains 'Icons.notifications_outlined'
    - app_bar_actions.dart contains 'clientNotifications'
  </acceptance_criteria>
  <done>
    - Bottom navigation has 4 items: Início, Chat, Docs, Recursos
    - Notifications accessible via header icon button
    - Navigation indices are correct (no index mismatch)
    - `flutter analyze` passes with no errors
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Client navigation | Internal Flutter routing — no external data crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-18-01 | Tampering | Quick action navigation targets | accept | Navigation is client-side only; no server data is exposed or modified by route changes |

</threat_model>

<verification>
- `flutter analyze` passes with 0 errors
- Home screen quick actions list renders exactly 3 items
- AppBar shows support + notifications icons
- Bottom nav has 4 items
</verification>

<success_criteria>
- Students see corrected quick actions (Agendamentos, Solicitar documentos, Notificações)
- "Conversar com Mentor" no longer appears on home screen
- Support is accessible from header icon on all client screens
- Notifications accessible from header icon (not bottom nav)
</success_criteria>

<output>
After completion, create `.planning/phases/18-student-ux-corrections/18-01-SUMMARY.md`
</output>
