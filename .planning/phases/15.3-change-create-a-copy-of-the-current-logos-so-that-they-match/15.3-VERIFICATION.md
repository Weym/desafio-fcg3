---
phase: 15.3-change-create-a-copy-of-the-current-logos-so-that-they-match
verified: 2026-05-10T22:30:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Toggle between light and dark theme; verify logo α mark and text are clearly visible on both"
    expected: "Dark fills (#111317) on light backgrounds, light fills (#E6E6EA) on dark backgrounds — no invisible or washed-out elements"
    why_human: "Visual contrast perception and SVG rendering fidelity cannot be verified programmatically"
  - test: "Check short logo (α mark) at 36px in AppBar on both themes"
    expected: "α mark clearly visible and recognizable at 36px on both light and dark surfaces"
    why_human: "Small-size legibility is a visual judgment"
---

# Phase 15.3: Light/Dark Logo Variants — Verification Report

**Phase Goal:** Create light-mode and dark-mode copies of the current logos so they match the app's theme brightness.
**Verified:** 2026-05-10T22:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Logo α mark is clearly visible on light-mode surfaces (F5F5F7 background) | ✓ VERIFIED | `alpha_connect_logo_light.svg` and `alpha_connect_shortlogo_light.svg` contain `fill="#111317"` (61 and 1 occurrences respectively); 0 old colors (`#124b5d`, `#1d1d1b`) remain |
| 2 | Logo α mark is clearly visible on dark-mode surfaces (111317 background) | ✓ VERIFIED | `alpha_connect_logo_dark.svg` and `alpha_connect_shortlogo_dark.svg` contain `fill="#E6E6EA"` (61 and 1 occurrences respectively); 0 old colors remain |
| 3 | Full logo text (ALPHA CONNECT + tagline) is legible on light-mode surfaces | ✓ VERIFIED | `alpha_connect_logo_light.svg` has 74 lines (matches original structure), all fills replaced with `#111317` (onSurface); widget selects this via `isDark` check |
| 4 | Full logo text (ALPHA CONNECT + tagline) is legible on dark-mode surfaces | ✓ VERIFIED | `alpha_connect_logo_dark.svg` has 74 lines (matches original structure), all fills replaced with `#E6E6EA` (darkOnSurface); widget selects this via `isDark` check |
| 5 | Short logo (α mark only) is visible in both modes at size 36px (AppBar) | ✓ VERIFIED | `client_home_screen.dart` uses `AlphaConnectLogo(size: 36)` — size ≤ 40, so widget selects shortlogo variants; both `_light` and `_dark` short variants exist (4 lines each, matching original) |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mobile/assets/logos/alpha_connect_logo_light.svg` | Full logo SVG with dark fills for light-mode surfaces | ✓ VERIFIED | 74 lines, 61× `fill="#111317"`, 0× old colors |
| `mobile/assets/logos/alpha_connect_logo_dark.svg` | Full logo SVG with light fills for dark-mode surfaces | ✓ VERIFIED | 74 lines, 61× `fill="#E6E6EA"`, 0× old colors |
| `mobile/assets/logos/alpha_connect_shortlogo_light.svg` | Short logo SVG with dark fill for light-mode surfaces | ✓ VERIFIED | 4 lines, 1× `fill="#111317"`, 0× old colors |
| `mobile/assets/logos/alpha_connect_shortlogo_dark.svg` | Short logo SVG with light fill for dark-mode surfaces | ✓ VERIFIED | 4 lines, 1× `fill="#E6E6EA"`, 0× old colors |
| `mobile/lib/shared/widgets/alpha_connect_logo.dart` | Brightness-aware logo widget that selects correct SVG variant | ✓ VERIFIED | 65 lines; contains `Brightness.dark` check, 4 SVG path references, conditional `ColorFilter` |
| `mobile/pubspec.yaml` | Asset directory declared | ✓ VERIFIED | Line 43: `- assets/logos/` (directory declaration covers all 6 SVGs) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `alpha_connect_logo.dart` | `mobile/assets/logos/` | `SvgPicture.asset` with brightness-conditional path | ✓ WIRED | Lines 48-54: `isDark` ternary selects `_dark.svg` / `_light.svg` for both full and short variants |
| `splash_screen.dart` | `alpha_connect_logo.dart` | import + `AlphaConnectLogo(size: 100, showTagline: true)` | ✓ WIRED | Import at line 4, usage at line 43 |
| `login_screen.dart` | `alpha_connect_logo.dart` | import + `AlphaConnectLogo(size: 80)` | ✓ WIRED | Import at line 10, usage at line 256 |
| `client_home_screen.dart` | `alpha_connect_logo.dart` | import + `AlphaConnectLogo(size: 36)` | ✓ WIRED | Import at line 6, usage at line 57 |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces static SVG assets and a widget that reads `Theme.of(context).brightness`. No dynamic data (API, DB, state) is involved.

### Behavioral Spot-Checks

Step 7b: SKIPPED — Flutter widget rendering requires a running app or widget test harness. The phase's `flutter analyze` (0 errors) and `flutter test` (42/42 pass) were executed during plan execution and confirmed in commit `d2e2834`.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| UI-NFR-02 | 15.3-01-PLAN.md | Aplicação Flutter adaptável a smartphones, tablets e web | ✓ SATISFIED | Logo widget is theme-adaptive via `Brightness.dark` check — renders correctly sized variants for any screen. Marked `[x]` Complete in REQUIREMENTS.md traceability table. |

No orphaned requirements found — REQUIREMENTS.md maps UI-NFR-02 to Phase 10 (original implementation), and this phase adds brightness adaptation as an enhancement.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | No anti-patterns detected in any modified files |

No TODO/FIXME, no placeholders, no empty implementations, no hardcoded empty data found in any phase-modified file.

### Human Verification Required

### 1. Visual Contrast Check — Light Mode

**Test:** Open the app in light mode. Navigate to splash screen (full logo at 100px), login screen (full logo at 80px), and client home screen (short logo at 36px in AppBar). Verify logo is clearly visible with dark fills on the light background.
**Expected:** All logo elements (α mark, "ALPHA CONNECT" text, tagline) render in dark Obsidian (#111317) on the light surface (#F5F5F7). No elements are invisible or low-contrast.
**Why human:** SVG rendering quality and visual contrast perception cannot be verified programmatically — requires eyeball check on actual device/emulator.

### 2. Visual Contrast Check — Dark Mode

**Test:** Toggle the app to dark mode. Navigate to the same 3 screens. Verify logo is clearly visible with light fills on the dark background.
**Expected:** All logo elements render in light (#E6E6EA) on the dark surface (#111317). No elements are invisible or low-contrast.
**Why human:** Same as above — visual fidelity requires human inspection.

### 3. Short Logo Legibility at 36px

**Test:** In client home screen AppBar, verify the α mark is recognizable at 36px on both light and dark themes.
**Expected:** α mark is crisp and identifiable at small size. Not blurry or indistinguishable.
**Why human:** Small-size legibility is a perceptual judgment that grep/code analysis cannot determine.

### Gaps Summary

No code-level gaps found. All 5 must-have truths are verified through code analysis:
- 4 SVG variants exist with correct brightness-appropriate fill colors
- Widget selects correct variant based on `Theme.of(context).brightness`
- ColorFilter is conditional only (applied when `color` param is non-null)
- All 3 usage sites compile without changes (constructor API preserved)
- No old hardcoded colors remain in any new SVG file
- Both commits (`8a1d207`, `d2e2834`) verified in git log

The only remaining verification is **visual** — confirming that the SVGs actually render with good contrast on-screen. This requires running the app on a device or emulator.

---

_Verified: 2026-05-10T22:30:00Z_
_Verifier: the agent (gsd-verifier)_
